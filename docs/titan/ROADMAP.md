# Roadmap — CyberDudeBivash Compliance Platform (Project Titan)

Phased, sequential, each phase's "done" tied to the quality gates the original brief specified: build passes, lint passes, typecheck passes, unit tests pass, integration tests pass, e2e tests pass, accessibility passes, performance passes, security review passes, documentation complete. **No phase is reported complete without evidence against every applicable gate** — the same discipline this repo's `docs/audit/` work has maintained for five stages carries forward here.

A module marked "not this phase" is not forgotten — it's in `PROGRAM_BACKLOG.md`-equivalent tracking once Phase 1 starts (see below), sequenced deliberately rather than attempted in parallel with everything else.

## How the Phase 2 master prompt's sub-phases (A–L) map onto this roadmap

A later master prompt ("Project Titan — Phase 2 — Enterprise DPDP Platform Integration & Production Evolution") introduced its own internal sequence, Phase A through Phase L, scoped to turning the already-existing scanner asset (`dpdpriskscan.html`, uploaded as part of that prompt) into Titan's Module 1. It doesn't replace the phases below — Phase 0's foundation work already happened, and this document's overall structure stands — but its Phase A–D content lands inside what Phase 1 originally scoped as Module 2/3 (questionnaire + risk engine), because that content already exists as a working asset rather than needing to be authored from scratch. `DECISION_LOG.md` has the full record; this table is the status snapshot, kept current here rather than in a second competing roadmap:

| Sub-phase | Covers | Status |
|---|---|---|
| A — Discovery | Review the scanner, document findings, no rewriting | ✅ Done — `ARCHITECTURE.md`'s Module 1 discovery section |
| B — Modularization | Break the scanner into production modules | ✅ Done for the DPDP flow — `titan/packages/assessment-core` (data/logic) + `titan/apps/web/src/features/dpdp-assessment` (UI), the original `dpdpriskscan.html` untouched alongside it |
| C — Question Engine | Data-driven question config, no hardcoded logic | ✅ Done for the DPDP v1 bank — typed, versioned, tested |
| D — Risk Engine | Separated business rules/scoring, tested | ✅ Done — the source asset's `maxScore` bug fixed, regression-tested, 100% statement coverage |
| E — Cloudflare Backend | Workers API, D1, repository layer, audit logging | ✅ Done for this stage's scope — `titan/packages/platform`: 5 repositories (Lead, Organization, Assessment, UserProfile, Audit), each in-memory + D1, contract-tested against real SQLite; 7 migration files applied against a **real local D1 instance**; a Worker with 5 endpoints, audit logging on every write. Verified via real `wrangler dev` HTTP requests, not just tests — see Stage 4's `PLATFORM_FOUNDATION.md` section |
| F — Authentication | Auth.js integration, orgs/users/RBAC/sessions | ✅ Done for this stage's scope — `titan/packages/platform/src/auth/`: D1-adapter database sessions, RBAC + org-membership foundation, Email (dev-mode)/Google/GitHub providers (Google/GitHub inactive without real credentials). Enterprise SSO explicitly deferred |
| G — Lead Management | Real persistence, pipeline, admin search | 🟡 Started — real persistence now works end to end (`@titan/web`'s `leadStore.ts` calls the real Worker API, no more `localStorage`); status tracking, ownership, notes, activity history, admin search/filtering are all still unbuilt (need an Admin Portal, Stage 5+) |
| H — Reporting | Server-side PDF support, storage, email workflow | ⛔ Not started — PDF generation is still real but client-side only (`pdfReport.ts`, lazy-loaded jsPDF). A `reports` table exists (migrations/0006) but nothing writes to it yet. **Distinct from EAP-8 ("Enterprise Reporting & Analytics")**, which is Module 10 (Analytics), not this module — see the "Recommended next EAP phase" note below and `DECISION_LOG.md`'s EAP-8 entry for why the similar name doesn't mean the same scope |
| I — Admin Portal | Dashboard, assessments, companies, leads, reports | 🟡 **Started — EAP-1 Phase 1 + EAP-2 + EAP-3 + EAP-4 + EAP-5 + EAP-6 + EAP-7 + EAP-8.** Application shell, Dashboard, full **Lead Management** (search/filter/sort/paginate, detail view, lifecycle, risk intelligence, audit history), full **Assessment Management** (search/filter/sort/paginate, detail view, results/severity-breakdown/category-coverage/question-responses, compliance intelligence, audit history, lead linkage), full **Organization Management** (search/filter/sort/paginate, create/archive/restore, detail view with Health/Relationships/Administration, audit history), full **Enterprise Identity & User Management** (real user directory search/sort/paginate, identity profile, Role Assignment — grant/change/revoke, Relationships, audit history), full **Enterprise Audit Center** (cross-entity search/filter/sort/paginate over every audit event, Audit Details, Investigation View grouping, CSV/JSON export), full **Enterprise Operations Center** (real per-service reachability, real request/repository-operation metrics, static system overview), and now full **Enterprise Reporting & Analytics** (a live Executive Dashboard/Business Reports/Analytics composed from every prior module's own data, plus CSV/JSON export). Every module the original EAP-1 brief named for the Admin Portal now exists, plus this eighth, roadmap-recommended one. Server-side generated *reports* (Module H, the `reports` table) remain unbuilt and are a different thing from this Admin Portal module's own name |
| J — Customer Portal | Org history, reports, roadmaps, bookings | 🟡 **Started — CPP-1.** A real, separate `/portal` shell for an organization member: Dashboard (organization overview, compliance summary, recent activity), Assessments (read-only, own organization's history + detail), Reports (compliance summary, CSV/JSON export), Support (submit + view own history), and Account (profile, session). Roadmaps and bookings remain unbuilt — neither has a real backing concept in this data model yet, and CPP-1's own brief explicitly excluded them |
| K — Security | Input validation, XSS/CSRF, rate limiting, headers | ✅ Done for what's actually deployed-testable — input/schema validation, route-scoped security headers (strict CSP for the JSON API, relaxed-but-locked-down for Auth.js's own HTML pages, HSTS, Permissions-Policy, Cross-Origin-Opener/Resource-Policy), CSRF (Origin validation on the custom JSON endpoints, extended to `POST`/`PATCH /api/organizations/:id` EAP-4), rate limiting (general + a separate, stricter limiter for `/api/auth/*`), cookie hardening reviewed, secret rotation support, **authorization gates on `GET /api/leads`/`GET /api/assessments/:id` and server-side risk-score recomputation (Security Release Blocker Sprint — both of ERP-1's named findings, fixed and verified)**, and a real cross-origin CORS gap found and fixed by EAP-6's own Playwright verification (`Access-Control-Expose-Headers` was never set, silently hiding `Content-Disposition` from browser JS) — reused, not re-fixed, for EAP-8's own Report Export. A full threat model + OWASP ASVS review exists (`SECURITY_GUIDE.md`), updated this pass |
| L — Quality | Full build/typecheck/lint/test/a11y/perf/security/regression pass | ✅ Passes for everything that exists — 1025 tests across 4 packages, freshly verified this pass (up from 119 at Stage 4's start, 222 as of ERP-1, 244 as of the Security Release Blocker Sprint, 300 as of EAP-1 Phase 1, 432 as of EAP-2, 497 as of EAP-3, 609 as of EAP-4, 713 as of EAP-5, 769 as of EAP-6, 789 as of EAP-7, 823 as of EAP-8, 881 as of CPP-1, 1011 as of COM-1; `@titan/platform` is 587, up from 573, and `@titan/web` is unchanged at 351, both directly confirmed by this pass's own fresh run), full CI green, real `wrangler dev` + real Chromium browser verification (both direct D1/metrics inspection and eleven committed Playwright E2E specs, 39 tests total, run strictly serially — `workers: 1` — re-run this pass as a regression check after PRD-1's own backend changes, unaffected) |

## RC1 pass: what it covered, what it explicitly didn't

The "Titan RC1 — Ultra Enterprise Master Implementation Program" master prompt asked for twelve workstreams in one pass: architecture audit, security hardening, Admin Portal, Customer Portal, a reporting platform, observability, a deployment pipeline, performance, enterprise testing, compliance (SOC 2/ISO 27001/GDPR/DPDP), commercial readiness (billing/licensing/plans), and documentation. Given the choice between shallow progress across all twelve or real depth on a scoped subset, the chosen scope was: architecture audit, security hardening, observability, testing, documentation (`DECISION_LOG.md` has the full reasoning). Status:

- **Architecture audit, security hardening, observability, testing, documentation** — done this pass, see `PLATFORM_FOUNDATION.md`'s RC1 section and `SECURITY_GUIDE.md`.
- **Admin Portal (I), Customer Portal (J)** — real product surfaces needing real UX decisions; `auth/rbac.ts`/`auth/authorize.ts` exist so whichever is built first has a real authorization mechanism to call. Both are now real (EAP-1–EAP-8, CPP-1) — see below.
- **Reporting Platform (H, server-side)** — not started. Blocked on the still-undecided email provider.
- **Deployment Pipeline, Cloudflare Turnstile** — not started. Need a real Cloudflare account, which has never existed in any environment this project has run in.
- **Commercial Readiness** (plans/licensing/billing/tenant isolation) — the provider-agnostic parts (plans, subscriptions, licenses, entitlements) are now real (COM-1) — see below. A real payment gateway integration remains blocked on the still-undecided payments provider; nothing in COM-1 processes a real transaction.
- **Compliance** (SOC 2/ISO 27001 readiness, formal GDPR/DPDP compliance) — not started. Organizational/legal work (external audits, legal review, retention-policy decisions), not something a code session completes.
- **Performance workstream** (bundle analysis, Lighthouse, Core Web Vitals beyond what Stage 3 already measured) — not attempted this pass; no new evidence to report beyond what `PLATFORM_FOUNDATION.md` already documents from Stage 3.

## ERP-1 pass: an independent audit, not a features pass

The "ERP-1 — Enterprise Release Program" prompt asked an independent reviewer to determine, with evidence, whether Titan had earned a release stage beyond Internal Alpha — explicitly not to make it look complete. It found one finding that outweighs everything else this pass verified: **`GET /api/leads` has no authentication** (HIGH — `SECURITY_GUIDE.md`, `DECISION_LOG.md`). It also closed two real WCAG AA failures a real Lighthouse audit caught (jsdom+axe could not), and ran a supply-chain review (clean). Same engineering-only scope as RC1 — see `DECISION_LOG.md`'s ERP-1 entry for why the rest of the RC1 prompt's deferred workstreams still aren't attempted. Full detail: `PLATFORM_FOUNDATION.md`'s ERP-1 section; release-stage conclusion: the ERP-1 Final Report communicated alongside this pass.

## Security Release Blocker Sprint: ERP-1's two named findings, fixed

Scoped narrowly to exactly ERP-1's two findings — no new features, no Admin/Customer Portal. **`GET /api/leads`'s missing authentication (HIGH)** is fixed: gated behind a new Platform Administrator role, verified against a real running Worker (401 anonymous → 403 authenticated-unprivileged → 200 Platform Administrator). **The client-submitted-risk-score tampering finding** is fixed: `POST /api/leads`/`POST /api/assessments` now recompute `result` server-side and discard the client's value, verified the same way (a tampered `score: 0` submission persisted the real computed score). A resume prompt described a prior session having already done this work; repository evidence (`git log`, reflog, every remote branch, direct file inspection) showed it was never committed, so this pass implemented it from a verified-clean baseline rather than assuming it existed — full reasoning in `DECISION_LOG.md`. Detail: `PLATFORM_FOUNDATION.md`'s Security Release Blocker Sprint section, `SECURITY_GUIDE.md`'s "Authorization model."

## EAP: the Admin Portal as an explicitly phased program, not one pass

The "EAP-1 — Enterprise Administration Platform" prompt requested a full admin application in one pass: shell, navigation, dashboard, Lead/Assessment/Organization/User Management, an Audit Center, and an Operations Center. Given the choice between shallow progress across all of that or real depth on a scoped subset, each EAP prompt since has been built as its own phase against this same shell — full reasoning in `DECISION_LOG.md`'s EAP-1, EAP-2, EAP-3, EAP-4, EAP-5, EAP-6, EAP-7, and EAP-8 entries. As of EAP-7, every module the original EAP-1 brief named was real; EAP-8 (Enterprise Reporting & Analytics) adds an eighth module beyond that original list — this program's own recommended next phase once all seven were done, not something the original brief asked for by name.

### Phase 1 — Enterprise Administration Foundation (done)

The authenticated application shell (session lifecycle via real Auth.js sign-in/sign-out, protected-route middleware, role-aware navigation built for pluggability, reusable admin UI framework — `MetricCard`/`Panel`), and one complete module: **Dashboard** (executive overview, organization/lead/assessment metrics, risk-level breakdown, recent activity, audit summary, platform health/system status). Four new read endpoints (`GET /api/me`, `GET /api/organizations`, `GET /api/assessments` list, `GET /api/audit`) back it, reusing the existing Platform Administrator role — no new authorization concept. Found and fixed one real, non-obvious bug that only real-browser verification could catch: CSP's `form-action` directive also restricts the redirect a form submission causes, which silently broke cross-origin sign-out until `authPagesCsp` widened it. Full detail: `ARCHITECTURE.md`'s "Admin Application architecture" section, `DECISION_LOG.md`'s EAP-1 entry, `PLATFORM_FOUNDATION.md`'s EAP-1 Phase 1 section.

### Phase 2 — Lead Intelligence Platform (done)

The first complete business module: a Lead Workspace (real server-side search/filter/sort/pagination, saved filters and column selection), Lead Details, Lead Lifecycle management (status/priority/assign-to-me-or-unassign/tags/notes — real `PATCH /api/leads/:id` writes, no optimistic-only updates), a Risk Intelligence panel (real score/breakdown/findings-by-severity from the lead's own server-recomputed result), and audit integration (a single lead's real activity/audit trail, server-filtered via `GET /api/audit`'s new `entityType`/`entityId` params). Three new/extended endpoints, all reusing the existing Platform Administrator role. Found and fixed two real bugs only real-browser verification could catch: CORS `Access-Control-Allow-Methods` never included `PATCH` (silently blocked the entire Lifecycle panel's write path), and a `useLeadDetail` data-loading effect missing the request-cancellation guard every sibling hook already has (a real React StrictMode race that could revert a fresh update to stale data). Full detail: `ARCHITECTURE.md`'s "Lead Intelligence Platform architecture" section, `DECISION_LOG.md`'s EAP-2 entry, `PLATFORM_FOUNDATION.md`'s EAP-2 section.

### Phase 3 — Enterprise Assessment Center (done)

The second complete business module, built read-only end to end since an assessment (unlike a lead) is one immutable row per completed run with no lifecycle to manage: an Assessment Workspace (real server-side search/filter by framework and risk level/sort/pagination, saved filters and column selection), Assessment Details (metadata, an honest "Completed" status rather than a fabricated workflow, organization/owner), Assessment Results (risk badge/score, severity breakdown, findings by severity, category coverage by real DPDP section, full question-by-question responses with Pass/Gap status — all still traced to the assessment's own server-computed result, never recomputed client-side), a Compliance Intelligence panel (aggregate risk distribution/framework status/outstanding findings by section/risk trend, computed client-side from the existing full-list endpoint, mirroring how the Dashboard already handles aggregate views), Lead linkage (real leads produced by an assessment, via a new `assessmentId` filter on `GET /api/leads/search`), and audit integration (a real `assessment.viewed` event added to the pre-existing `GET /api/assessments/:id`, alongside the pre-existing `assessment.created`). One new/extended endpoint (`GET /api/assessments/search`), reusing the existing Platform Administrator role — no new authorization concept, no new CORS method, no new CSRF-checked write, since nothing in this module accepts a write. Found and fixed two real bugs while building it: a pre-existing `LeadWorkspacePage.tsx` sort-column bug (an invalid `sortBy=risk` that 400'd the whole table) and a pre-existing `useLeadDetail` audit-trail race (fixed there and built correctly from the start in the new `useAssessmentDetail`). Full detail: `ARCHITECTURE.md`'s "Enterprise Assessment Center architecture" section, `DECISION_LOG.md`'s EAP-3 entry, `PLATFORM_FOUNDATION.md`'s EAP-3 section.

### Phase 4 — Enterprise Organization Management Platform (done)

The third complete business module, and the first with a real administrative write surface (unlike Assessments): an Organization Workspace (real server-side search/filter by status/industry/region/sort/pagination, saved filters and column selection, plus a real create-organization form — the first admin-facing creation UI in this app), Organization Details (Metadata, Health — current risk/average score/risk distribution/score trend, all derived from this organization's own real linked assessments, never fabricated — Relationships — real linked leads/assessments deep-linked into the existing Lead Intelligence/Assessment Center modules, never duplicating either — Administration — real name/industry/region/tags edits, archive/restore, and notes, all real `PATCH` writes — and Activity & audit history). Two new/extended endpoints beyond the pre-existing `GET /api/organizations` (`POST /api/organizations`, `GET /api/organizations/search`, `GET /api/organizations/:id`, `PATCH /api/organizations/:id`), reusing the existing Platform Administrator role. Found and fixed one real, pre-existing bug in EAP-3's own shipped frontend code: `leadApi.ts`'s `searchLeads` accepted an `assessmentId` filter but never forwarded it into the request URL, so Assessment Details' "Lead linkage" panel had been silently unfiltered since EAP-3 shipped. Full detail: `ARCHITECTURE.md`'s "Enterprise Organization Management Platform architecture" section, `DECISION_LOG.md`'s EAP-4 entry, `PLATFORM_FOUNDATION.md`'s EAP-4 section.

### Phase 5 — Enterprise Identity & User Management (done)

The fourth complete business module: an Enterprise User Directory (real server-side search by name/email/sort/pagination over Auth.js's own `users` table — no create-form, since identity comes only from a real sign-in), User Details (Identity, Role Assignment — grant/change/revoke a user's organization or platform-wide role, every action a real server round-trip — Relationships — real assigned leads and real created assessments, deep-linked into the existing Lead Intelligence/Assessment Center modules — and Activity & audit history). Five new endpoints (`GET /api/users/search`, `GET /api/users/:id`, `POST /api/users/:id/profiles`, `PATCH`/`DELETE /api/users/:id/profiles/:profileId`), reusing the existing Platform Administrator role. Closes, for every grant after the first, the "no self-service way to grant Platform Administrator" gap named since the Security Release Blocker Sprint, guarded by a new `wouldRemoveLastPlatformAdministrator` check preventing the system from ever being left with zero Platform Administrators. `UserProfileRepository.remove()` is this system's first real deletion — a deliberate, reasoned exception to every other repository's archive-not-delete pattern, since a role grant (unlike an organization or a lead) has no dependent data of its own to preserve. Found and fixed one real environmental gap, not a code defect: this session's container had never had `AUTH_SECRET` provisioned (`.dev.vars` didn't exist), so every authenticated real-browser scenario — for this new module and, reproduced deliberately, for already-shipped EAP-4 too — silently resolved every caller as anonymous until fixed by following `OPERATIONAL_RUNBOOK.md`'s own documented setup step. Full detail: `ARCHITECTURE.md`'s "Enterprise Identity & User Management architecture (EAP-5)" section, `DECISION_LOG.md`'s EAP-5 entry, `PLATFORM_FOUNDATION.md`'s EAP-5 section.

**Explicitly not done across Phases 1–5** (deferred to later phases, not forgotten, not stubbed ahead of time): an Audit Center and an Operations Center as full modules — no dedicated, cross-module-filterable view over `audit_events` beyond what each module's own Details page already renders as one slice of. Also explicitly not done: a self-service way to create a user's own identity (Auth.js's real sign-in remains the only path), an Organization Details "Members" panel (User Details shows a user's own memberships; the reciprocal organization-side view is real, deferred follow-up), and a real assignee picker wired into Lead Lifecycle's already-shipped UI (EAP-5 closes the reason it was deferred — no user directory existed — but not the picker itself). The shell continues to be architected so each remaining module plugs in without rework: `adminNavItems` takes the caller's real role and is where a new module's nav entry gets added; `AdminLayout`'s `<Outlet/>` is the mount point new module routes nest under; `SectionState` (loading/ready/forbidden/error) is now used by eight independent hooks (`useDashboardData`, `useLeadSearch`, `useLeadDetail`, `useAssessmentSearch`, `useAssessmentDetail`, `useOrganizationSearch`, `useOrganizationDetail`, `useUserSearch`/`useUserDetail`) and is meant to keep being reused by each new module's own data-loading hook rather than reinvented.

### Phase 6 — Enterprise Audit Center (done)

The sixth complete business module, and the first that consolidates existing data rather than introducing a new entity: an Enterprise Audit Workspace (real server-side search/filter by entity type, action, actor id, organization id, and date range/sort/pagination, saved filters and column selection, a Table/Investigation view toggle), Audit Details (an inline panel — actor/action/timestamp/entity/organization/raw metadata/related events — over an already-loaded record, not a new route or endpoint), Investigation View (groups the currently-loaded page of results by entity/actor/organization, reusing the existing `Timeline` component), and Audit Export (CSV/JSON file download of the same filtered set, capped at 10,000 rows). Two new endpoints (`GET /api/audit/search`, `GET /api/audit/export`), reusing the existing Platform Administrator role and the pre-existing `GET /api/audit` endpoint's own policy — no new authorization concept, no new writer to `audit_events` (every event any module already records is unchanged; this phase only adds a new way to read them). A CSV/formula-injection risk was investigated and verified not present in this data model (`metadata` is always JSON-wrapped, so it can never begin with a formula-triggering character). Found and fixed one real cross-origin CORS gap: `http/cors.ts` never set `Access-Control-Expose-Headers`, so a real browser silently hid the `Content-Disposition` header the export's filename depends on — caught only by real Playwright/Chromium E2E verification, the identical class of finding EAP-2's missing `Access-Control-Allow-Methods` entry for `PATCH` already was. Full detail: `ARCHITECTURE.md`'s "Enterprise Audit Center architecture (EAP-6)" section, `DECISION_LOG.md`'s EAP-6 entry, `PLATFORM_FOUNDATION.md`'s EAP-6 section.

**Explicitly not done across Phases 1–6** (deferred to later phases, not forgotten, not stubbed ahead of time): an Operations Center as a full module. Also explicitly not done: a self-service way to create a user's own identity (Auth.js's real sign-in remains the only path), an Organization Details "Members" panel, a real assignee picker wired into Lead Lifecycle's already-shipped UI, investigation notes/annotations on Audit Center events (no backing column exists on `audit_events` — a real schema addition, not built speculatively ahead of one), and a truncation signal on an Audit Export beyond 10,000 rows (the cap itself is the real control). The shell continues to be architected so the remaining module plugs in without rework: `adminNavItems`/`AdminLayout`'s `<Outlet/>`/`SectionState` are all reused exactly as before, not extended or reworked for this phase.

### Phase 7 — Enterprise Operations Center (done)

The seventh complete business module, and the first that surfaces operational rather than business data: a real per-service reachability check (`leads`/`assessments`/`organizations`/`users`/`userProfiles`/`audit`, each a genuine `search()`/`list()` call, not a fabricated ping), real request/repository-operation metrics (`observability/metrics.ts`'s own counters, instrumented since an earlier phase but never read back by anything until now), and a static system overview (version/environment/registered modules). One new endpoint, `GET /api/operations/summary`, reusing the existing Platform Administrator role — no new authorization concept, no new table, no new writer anywhere (every repository/metric this phase reads was already being written to by earlier phases). `Metrics` gains `getCounts()`/`getDurations()` on the interface itself, not just the one in-memory implementation that already had them. Background Operations is an honest, verified "no queue/Durable Object/cron infrastructure exists in this deployment" note, not a fabricated queue view (`wrangler.toml` has none of the three). One deliberate, evaluated exception to this codebase's general error-handling rule: `ServiceStatus.error` returns a caught exception's real message to the (already Platform-Administrator-only) caller, rather than the generic message every other endpoint's unhandled error returns — because showing the real failure cause is this endpoint's actual purpose. Full detail: `ARCHITECTURE.md`'s "Enterprise Operations Center architecture (EAP-7)" section, `DECISION_LOG.md`'s EAP-7 entry, `PLATFORM_FOUNDATION.md`'s EAP-7 section.

**Explicitly not done across Phases 1–7** (deferred to later phases, not forgotten, not stubbed ahead of time): a Reporting & Analytics module. Also explicitly not done: a self-service way to create a user's own identity, an Organization Details "Members" panel, a real assignee picker wired into Lead Lifecycle's already-shipped UI, investigation notes/annotations on Audit Center events, a truncation signal on an Audit Export beyond 10,000 rows, a dynamically-read build hash/timestamp (no CI step injects one anywhere in this repository), and a cross-isolate/durable metrics backend (needs Workers Analytics Engine, needs a deployed Cloudflare account that doesn't exist). The shell continues to be architected so the next module plugs in without rework: `adminNavItems`/`AdminLayout`'s `<Outlet/>`/`SectionState` are all reused exactly as before, not extended or reworked for this phase.

### Phase 8 — Enterprise Reporting & Analytics (done)

The eighth complete business module, and the first that is pure composition over every prior module's own data rather than a new entity of its own: an Executive Dashboard (real organization/lead/assessment/identity/audit KPIs), Business Reports (real per-status/priority/risk-level/framework/action breakdown tables), and an Analytics panel (an entity/date-range selector rendering a real per-day trend — `TrendSparkline` plus an accessible `DataTable` of the identical counts — for one of leads/assessments/organizations/audit/identity), plus CSV/JSON Report Export. Three new endpoints (`GET /api/reports/summary`, `GET /api/reports/trends`, `GET /api/reports/export`), reusing the existing Platform Administrator role — no new authorization concept, no new table, no new repository method beyond the `list()`/`search()` every prior module's own pages already call. One new design-system primitive, `TrendSparkline` (a dependency-free inline SVG polyline, not a charting library). **Not what this document's own prior note (below, as it read at the end of Phase 7) said this would be:** that note described reading from the pre-existing `reports` table (migrations/0006) — this phase instead composes live repository reads, because the `reports` table tracks a different concept (generated PDF report artifacts, Module H, still blocked on an undecided email/storage pipeline) — see `DECISION_LOG.md`'s EAP-8 entry for the full reasoning, recorded honestly rather than silently reinterpreted to match the old phrasing. Report Export is deliberately more conservative than Audit Export: only aggregate counts, no individual-record fields, so no row cap is needed. Full detail: `ARCHITECTURE.md`'s "Enterprise Reporting & Analytics architecture (EAP-8)" section, `DECISION_LOG.md`'s EAP-8 entry, `PLATFORM_FOUNDATION.md`'s EAP-8 section.

**Explicitly not done across Phases 1–8** (deferred, not forgotten, not stubbed ahead of time): a Customer Portal or any commercial/billing/licensing surface. A self-service way to create a user's own identity, an Organization Details "Members" panel, a real assignee picker wired into Lead Lifecycle's already-shipped UI, investigation notes/annotations on Audit Center events, a truncation signal on an Audit Export beyond 10,000 rows, a dynamically-read build hash/timestamp, and a cross-isolate/durable metrics backend all remain exactly as named above — none of them was in EAP-8's own scope, and none regressed. Server-side generated PDF reports (Module H, the `reports` table) remain unbuilt, unrelated to and unaffected by this phase's own, differently-scoped reporting layer. The shell continues to be architected so the next module plugs in without rework: `adminNavItems`/`AdminLayout`'s `<Outlet/>`/`SectionState` are all reused exactly as before, not extended or reworked for this phase.

### Recommended next EAP phase

With EAP-8, every module the original EAP-1 brief named is real, plus one more this roadmap itself recommended. Per this program's own standing instruction, no Customer Portal, Commercial Platform, Cloudflare Production, Release Candidate, or General Availability work should begin before EAP-8 is complete — it now is. Two reasonable, real candidates for what comes next, not a decision already made:

1. **A release-classification re-evaluation** (an ERP-2-style independent audit), the same kind of check ERP-1 performed after RC1 — to confirm with fresh evidence whether an admin shell with eight real modules is sufficient to advance past Internal Alpha, or whether Gates C (Operations, nothing deployed anywhere) and E (Commercial, no payments/billing) still control the classification regardless of how complete the modules themselves are.
2. **Phase 5 — Customer Portal & APIs** (this document's own Module 13/14, below): the next real business surface named in the original 20-module list, now that every internal/admin-facing module is done. Needs its own real UX and authorization decisions (a customer-facing role distinct from Platform Administrator/organization member) before implementation, not a continuation of the EAP program's existing shell.

Three smaller, real, still-unblocked follow-ups remain worth sequencing alongside whichever of the above comes first, unchanged by EAP-8: wiring a real assignee picker into Lead Lifecycle's already-shipped `LeadLifecyclePanel.tsx` (the user directory it needed didn't exist before EAP-5; it does now), an Organization Details "Members" panel (the reciprocal view of User Details' own Role Assignment), and a truncation signal on Audit Export beyond its current 10,000-row cap. This is a recommendation for sequencing, not a decision already made — the same way `ARCHITECTURE.md`'s open decisions are recommendations, not commitments.

**Which candidate was chosen:** candidate 2 above — a customer-facing portal — scoped narrowly as **CPP-1**, a deliberately smaller slice than this document's own Phase 5/Module 13 (below): organization history, reports, and support, but explicitly not bookings or enterprise APIs (Module 14), and explicitly not a continuation of the EAP program's own admin-facing modules. See the "CPP-1" section immediately below for what was actually built.

## CPP-1: the Enterprise Customer Portal, built on the verified EAP foundation

The "CPP-1 — Enterprise Customer Portal" master prompt asked for a customer-facing application layered on top of the already-verified EAP-1–EAP-8 platform: reusing its existing services, repositories, and security model rather than redesigning any of them, and explicitly excluding any Administration Console, Billing, Subscriptions, Licensing, Payments, or other Commercial Platform surface. Given the user's own stated sequencing rationale, this preserves a clean architectural boundary — EAP-1 through EAP-8 established the internal administrative platform; CPP-1 exposes an appropriately-scoped, customer-facing subset of the same data; a later Commercial Platform phase can layer subscriptions/licensing/billing onto a mature customer experience; production deployment and release activities follow once the customer-facing workflows are in place.

**What was built:** a real, separate `/portal` application — a sibling route tree to `/admin`, not nested under it, and not a new authorization role: a real Organization Member/Admin/Owner using their own existing session and `UserProfileRecord`. Five modules: **Dashboard** (Organization Overview, Compliance Summary via the new, deliberately smaller `PortalComplianceSummary` type, Recent Activity reusing the existing `audit_events` table), **Assessments** (a read-only, organization-scoped table plus Assessment Detail, which reuses the *existing* `GET /api/assessments/:id` and its pre-existing `requireAssessmentAccess` authorization check directly rather than adding a new backend route), **Reports** (the same compliance summary as a real CSV/JSON download), **Support** (submit a request, view its own history — the one genuinely new entity this phase needed, `SupportRequestRepository`, since nothing like it existed anywhere in this codebase), and **Account** (profile and session/sign-out only — explicitly not password change, notification preferences, or multi-session management, none of which this platform has a real backing service for yet).

**The core architectural device is `resolvePortalOrganizationId`**: every portal route derives its organization scope server-side, from the caller's own real `UserProfileRecord[]`, never from a client-supplied id, query parameter, or path segment. This closes, for this module specifically, the exact class of gap `SECURITY_GUIDE.md`'s "Known, accepted gaps" has flagged as missing for every admin list/search endpoint since EAP-1 (organization-scoped filtering) — not by changing any existing admin route's authorization (none were touched, verified via `git diff`: every change across `router.ts`/`router.test.ts`/`worker.ts`/`index.ts`/`repositories/types.ts`/`App.tsx` is a pure addition, zero deletions), but by adding new, dedicated, narrowly-scoped routes alongside them. Seven new endpoints: `GET /api/portal/organization`, `GET /api/portal/assessments`, `GET /api/portal/reports/summary`, `GET /api/portal/reports/export`, `GET /api/portal/activity`, `GET`/`POST /api/portal/support`. One new repository (`SupportRequestRepository`, migration `0010_support_requests.sql`) — every other module reuses the existing `AssessmentRepository`/`OrganizationRepository`/`AuditRepository` reads unchanged, just with `organizationId` forced to the caller's own rather than accepted from a query string the way the Platform-Administrator-only admin routes correctly still do.

**Verification depth matches every prior EAP phase**: 22 new backend router tests (cross-organization-isolation assertions on assessments/reports/activity — two real organizations seeded, asserting a caller only ever sees their own — a CSRF-mismatched-Origin 403 on the one write route, and a "no individual assessment fields" PII check on the CSV export), 24 new frontend component tests, and a tenth Playwright E2E spec (`customer-portal.spec.ts`, 4 tests) that proves the same property against a real browser and real local D1: a real organization member navigating directly to another organization's assessment by id — not through any UI affordance, straight to the URL — receives an honest "This assessment is not available to your organization." message, never the data. Clean across 2 consecutive full-suite runs (all 10 specs, 35 tests total), zero flakes. Full workspace verification (`npm run ci`: typecheck, lint, format, build, test) passes clean at 881/881 tests. Full detail: `ARCHITECTURE.md`'s "Enterprise Customer Portal architecture (CPP-1)" section, `DECISION_LOG.md`'s CPP-1 entry, `PLATFORM_FOUNDATION.md`'s CPP-1 section, `SECURITY_GUIDE.md`'s CPP-1 paragraph.

**Explicitly not done in CPP-1** (deferred, not forgotten, not stubbed ahead of time): an organization switcher for a caller with more than one real organization membership (`resolvePortalOrganizationId` resolves to the first membership found — a named scope limit, `DECISION_LOG.md`'s CPP-1 entry); a support-request ticketing/resolution workflow (no admin-side endpoint reads or updates `support_requests` yet — `status` starts and stays `"open"`); password change, notification preferences, and multi-session management on the Account page (no credentials provider, settings/notification service, or `SessionRepository` beyond Auth.js's own single-session-token model exists to back any of them); enterprise APIs (Module 14 — REST/webhooks for programmatic access); and bookings (no scheduling concept exists anywhere in this data model). No Commercial Platform, Billing, Subscriptions, Licensing, Payments, Cloudflare Production, Release Candidate, or General Availability work was started, per this program's own standing instruction to complete CPP-1 first.

## COM-1: the Enterprise Commercial Platform, built on the verified EAP + CPP foundation

The "COM-1 — Enterprise Commercial Platform" master prompt asked for the commercial layer — subscriptions, licensing, entitlements, plan catalog — on top of the already-verified EAP-1–EAP-8 and CPP-1 platform, explicitly excluding Payment Gateway Processing, PCI card storage, Accounting, Tax Engines, Marketplace, Affiliate Programs, CRM, ERP, and any production/deployment work. This continues the same clean architectural boundary CPP-1's own sequencing established: EAP built the internal administrative platform; CPP-1 exposed a customer-facing subset of it; COM-1 adds the monetization/entitlement layer on top of both, provider-agnostic — it models the commercial lifecycle a real billing provider would eventually plug into, without calling one.

**What was built:** two genuinely new repositories, `SubscriptionRepository` and `LicenseRepository` (`migrations/0011_subscriptions.sql`/`0012_licenses.sql`), and a code-defined Plan catalog (`commercial/planCatalog.ts` — Starter/Professional/Enterprise, the same "ships with the app" pattern `@titan/assessment-core`'s DPDP question bank already established, not a database table: nothing in this brief asked for dynamic plan authoring, and `priceDisplay` is display text, never a real chargeable amount). Eight new endpoints: `GET /api/commercial/plans` (open to any authenticated caller — non-sensitive catalog data, the same deliberate exception `GET /api/me` already is), `GET`/`POST`/`PATCH /api/portal/commercial/subscription` (customer-scoped, gated by the identical `resolvePortalOrganizationId` CPP-1 already established), and `GET /api/commercial/subscriptions/search`, `GET`/`PATCH /api/commercial/subscriptions/:id`, `GET /api/commercial/licenses/search` (Platform-Administrator-only, the same policy every EAP admin route already uses). A real UI on both sides: the Customer Portal gained `/portal/subscription` (Commercial Dashboard content — Subscription Overview, Current Plan, License Summary, Usage Summary, Entitlements, Renewal Information, Trial Status — plus self-service Plan Selection/Upgrade/Downgrade/Cancellation/Renewal); the admin app gained `/admin/commercial` (Subscription Administration workspace + detail, License Inventory surfaced as columns/sections rather than a redundant separate page, since a License is 1:1 with its Subscription in this data model).

**License is deliberately not a per-user assignment table.** It's the seat *grant* itself (`seatLimit`, `status`, `activatedAt`/`expiresAt`) — seat *usage* is the live count of an organization's own real `UserProfileRecord`s (`UserProfileRepository.findByOrganizationId`, one new, narrowly justified method added to that repository this phase), so it can never drift from real membership the way a separately maintained counter could. The same "compose from the existing repository, don't duplicate its data" discipline `PortalComplianceSummary` (CPP-1) already established for assessment counts.

**Feature Entitlements are computed and displayed, deliberately not enforced anywhere this phase.** `resolveEntitlements(plan, subscription)` is a pure function — full plan entitlements while `trialing`/`active`, collapsed to nothing once `canceled`/`expired` — real and correct, and shown on both the Customer Portal's own Subscription page and (via the admin override) the Commercial Detail page. But no route anywhere in this codebase checks it before acting: wiring enforcement into the Assessment Engine, the Reporting Engine, or the pre-existing Customer Portal routes would mean modifying systems this program's own instructions name as do-not-redesign. This is real, named future work, not a silent gap — see `DECISION_LOG.md`'s COM-1 entry.

**A deliberate, tested asymmetry between the customer-facing and admin-facing write paths.** `isSelfServicePlan(plan)` (`plan.trialDays > 0`) gates `POST`/`PATCH /api/portal/commercial/subscription`: a customer can self-serve into Starter or Professional, never Enterprise (`trialDays: 0`, sales-assisted by design) — the customer-facing UI doesn't even render a start button for it. `PATCH /api/commercial/subscriptions/:id` carries no such restriction: a Platform Administrator can assign any known plan, including Enterprise, on an organization's behalf — the real, intended path a "contact sales" plan actually gets provisioned through. Both PATCH routes share one internal state machine, `applySubscriptionPatch`, which derives real `subscription.upgraded`/`.downgraded`/`.canceled`/`.renewed` and `license.activated`/`.expired` audit events from whatever the patch actually changed — the same diff-before-recording discipline `updateOrganization`/`updateLead` already established.

**Verification depth matches every prior phase**: `git diff` against the pre-COM-1 baseline shows every touched pre-existing file (`router.ts`, `router.test.ts`, `worker.ts`, `index.ts`, `repositories/types.ts`, three `userProfileRepository.*` files, `App.tsx`, `navItems.ts`, `portalNavItems.ts`) as pure addition — 1751 insertions, 0 deletions. 35 new backend router tests (cross-organization isolation on the portal summary, a CSRF-mismatched-Origin 403 on every write route, the sales-assisted-plan-blocked-for-customers-but-not-admins property, explicit 503s for each unconfigured dependency), 60 new repository contract tests (Subscription/License × memory+D1) plus 6 more for `UserProfileRepository.findByOrganizationId`, 11 pure-logic tests for the plan catalog and entitlement resolution, 18 new frontend component tests, and an eleventh Playwright E2E spec (`commercial-platform.spec.ts`, 4 tests) proving a real customer's self-service subscribe/cancel/renew round trip against a real browser and real local D1, and a real Platform Administrator assigning Enterprise directly. Clean across 2 consecutive full-suite runs (all 11 specs, 39 tests total), zero flakes. Full workspace verification (`npm run ci`) passes clean at 1011/1011 tests. Full detail: `ARCHITECTURE.md`'s "Enterprise Commercial Platform architecture (COM-1)" section, `DECISION_LOG.md`'s COM-1 entry, `PLATFORM_FOUNDATION.md`'s COM-1 section, `SECURITY_GUIDE.md`'s COM-1 paragraph.

**Explicitly not done in COM-1** (deferred, not forgotten, not stubbed ahead of time): a real payment gateway integration (no card/invoice data or chargeable transaction exists anywhere in this system — provider-agnostic by design, per this phase's own brief); entitlement enforcement on any existing route (computed and displayed, not yet a gate — see above); a multi-subscription-per-organization concept (one subscription per organization, matching the real 1:1 tenant model every other entity in this system already uses); dynamic plan authoring/CRUD (the catalog is code-defined, the same reasoning the DPDP question bank already established); a support-request-style resolution workflow for Commercial Administration beyond the real admin override PATCH already built; Accounting, Tax Engines, Marketplace, Affiliate Programs, CRM, and ERP (all explicitly out of COM-1's own brief). No Cloudflare Production, Release Candidate, or General Availability work was started, per this program's own standing instruction to complete COM-1 first.

## PRD-1: Enterprise Production Infrastructure, built on the verified EAP + CPP + COM foundation

The "PRD-1 — Enterprise Production Infrastructure" master prompt asked for production deployment infrastructure only, explicitly excluding new application features, redesigning any verified system, Enterprise Operations & SRE (OPS-1), Enterprise Security & Compliance (SEC-1), and an actual production deployment/Release Candidate/General Availability. Ran in one continuous session from the already-clean, already-pushed COM-1 baseline. This is the first pass that is pure infrastructure — no new page, endpoint business logic, or repository capability.

**What was built:** `wrangler.toml` gains `[env.staging]`/`[env.production]` named environments (structurally verified via `wrangler deploy --dry-run --env <name>`, zero Cloudflare credentials required — real bindings resolved and printed for both). `config/validateEnv.ts`'s `validateProductionConfig` — a real, tested production-configuration check (`AUTH_SECRET`/`ALLOWED_ORIGIN`, only in a `staging`/`production` `ENVIRONMENT`), surfaced via `GET /api/operations/summary`'s new `configuration` field (EAP-7's own endpoint, extended). A second, manual-only (`workflow_dispatch`) CI/CD workflow, `titan-deploy.yml`, independent of the pre-existing `titan-ci.yml`. Six release-automation scripts (`packages/platform/scripts/`) — config-placeholder check, secrets-name validation, local D1 backup, a smoke test, and a release orchestrator — every one actually run against real local state this phase, not just written.

**A real, verified disaster-recovery drill, not a documented procedure alone**: captured real baseline row counts across five tables (this session's own accumulated data — 65/43/62/373/22 across leads/assessments/organizations/audit_events/subscriptions), exported via `wrangler d1 export`, wiped local D1 entirely, and restored. A first restore attempt failed with a real finding (re-applying migrations before restoring a full export double-inserts Wrangler's own internal bookkeeping table); a second attempt, informed by that finding, restored cleanly with an exact row-count match across all five tables, then passed a full application-level smoke test against the recovered data.

**Verification depth matches every prior phase**: `npm run ci` passes clean across all four packages — 1025 tests (up from 1011: +14 in `@titan/platform` — 9 new `validateEnv.test.ts` unit tests, 3 new `router.test.ts` tests, 2 new `worker.test.ts` end-to-end tests proving real `env.ENVIRONMENT` wiring through the actual Worker). The full 39-test Playwright E2E suite was re-run as a regression check after all backend changes — 39/39, unaffected, since PRD-1 touched no frontend code. One real, expected lint gap was found and fixed along the way (the new `.mjs` scripts needed a Node-globals ESLint block, since they run outside any browser/Workers context — `packages/config/eslint.base.mjs`). Full detail: `ARCHITECTURE.md`'s "Enterprise Production Infrastructure architecture (PRD-1)" section, `DECISION_LOG.md`'s PRD-1 entry, `PLATFORM_FOUNDATION.md`'s PRD-1 section, `DEPLOYMENT_GUIDE.md`, `DISASTER_RECOVERY.md`, `ENVIRONMENT_GUIDE.md`.

**Explicitly not done in PRD-1** (deferred, not forgotten, not stubbed ahead of time): any actual Cloudflare deployment — no account has ever existed to deploy to, and every credential-requiring command in this phase's own tooling was run and confirmed to fail with exactly the expected authentication error. R2/Queues/Durable Objects/Turnstile bindings (nothing in this codebase generates a file to store, dispatches an async job, or gates a public form yet — real, evidence-based "not yet justified," not an oversight). Custom domain/DNS/TLS. A scheduled/automated remote backup job (needs a real remote database to schedule backups of). Route-based code-splitting for `@titan/web`'s main bundle (a real, new finding this phase's own performance review surfaced — see below — genuinely out of PRD-1's own infrastructure-only scope). Enterprise Operations & SRE (OPS-1) and Enterprise Security & Compliance (SEC-1) — both explicitly named as separate future phases in PRD-1's own brief. No Release Candidate or General Availability work was started, per this program's own standing instruction to complete PRD-1 first.

**A real, new performance finding**: a fresh production build (not carried over from Stage 4's stale ~488 KB figure) shows `@titan/web`'s main bundle has grown to 826.13 kB / 185.49 kB gzip — Vite's own build output now warns "Some chunks are larger than 500 kB after minification." A real, concrete, named recommendation follows directly: route-based code-splitting (`React.lazy`) for `/admin`, `/portal`, and `/assessment/dpdp` — three genuinely separate user journeys sharing one bundle unnecessarily. Not attempted this phase (application code restructuring is out of PRD-1's own scope), named here as real, prioritized near-term follow-up rather than left undiscovered.

## OPS-1: Enterprise Operations & Site Reliability Engineering, built on the verified EAP + CPP + COM + PRD foundation

The "OPS-1 — Enterprise Operations & Site Reliability Engineering" master prompt asked for operations, observability, reliability, monitoring, alerting, incident response, operational automation, and SRE practices only — explicitly excluding security hardening, compliance programs, a WAF, penetration testing, production release, global launch, payment processing, and any business/customer/commercial feature or redesign of Authentication/Authorization/RBAC/the Assessment or Reporting Engines/the Administration or Customer Platforms/production infrastructure/the database architecture/the Repository Pattern/shared components/business logic. Ran from the already-clean, already-pushed PRD-1 baseline. Like PRD-1, this is a pure-capability pass extending the existing Enterprise Operations Center (EAP-7) rather than a new application feature.

**What was built:** two new, pure, zero-I/O leaf modules — `observability/aggregate.ts` (real p50/p95/p99 latency and 4xx/5xx error-rate computation from the exact `RecordedCount[]`/`RecordedDuration[]` shapes EAP-7's `Metrics` already produces; nothing computed either before this phase, confirmed by repository-wide search) and `observability/alerts.ts` (a pure `evaluateAlerts` function evaluating real readiness/service/configuration/error-rate/latency data against named thresholds, every returned alert carrying the real evidence that triggered it). `router.ts`'s `computeReadiness` is a new function shared by `GET /health/ready` and `GET /api/operations/summary`, so a `staging`/`production` deployment with an invalid configuration now fails readiness on its own terms, not only when the database is unreachable. `GET /api/operations/summary` gains two new, always-present fields (`requestSummary`, `alerts`) alongside the pre-existing ones — every field it already returned is unchanged. The Operations Center dashboard (`/admin/operations`) gains an Alerts panel, a "Request health" section in Runtime Metrics, and an Operational Summary banner — all composed from data the endpoint already gathers, no new signal invented at the UI layer. One new automation script, `check-operational-thresholds.mjs`, deliberately independent of the pre-existing `smoke-test.mjs`. Four new documents (`OPERATIONS_GUIDE.md`, `MONITORING_GUIDE.md`, `INCIDENT_RESPONSE_GUIDE.md`, `SRE_GUIDE.md`) covering operational architecture, observability/alert thresholds, incident response, and SLOs/capacity planning — every one honest about what's a real, tested capability versus a forward-looking framework built ahead of the real traffic that would exercise it.

**Verification depth matches every prior phase**: `npm run ci` passes clean across all four packages — 1068 tests (up from 1025: +43). `@titan/platform` gains 35 (622, up from 587 — 12 new `aggregate.test.ts` unit tests, 17 new `alerts.test.ts` unit tests, 6 new `router.test.ts` tests for readiness+configuration and the new `operationsSummary` fields); `@titan/web` gains 8 (359, up from 351 — 4 new `AlertsPanel.test.tsx` tests, 3 new `RuntimeMetricsPanel.test.tsx` tests, 1 new `OperationsWorkspacePage.test.tsx` test); `@titan/assessment-core`/`@titan/design-system` unchanged (25/62), since OPS-1 touched neither. The Playwright `operations-center.spec.ts` suite was extended (not replaced) with real assertions for the new Alerts/Request-health/Operational-summary UI — but a full-suite E2E run in this specific remote sandbox produced widespread timeouts on a controlled sample of the *unmodified pre-OPS-1 baseline* too (`DECISION_LOG.md`'s OPS-1 entry has the full investigation), so this is a real, investigated sandbox limitation, not a claim of a clean 39/39 E2E run. Full detail: `ARCHITECTURE.md`'s "Enterprise Operations & Site Reliability Engineering architecture (OPS-1)" section, `DECISION_LOG.md`'s OPS-1 entry, `PLATFORM_FOUNDATION.md`'s OPS-1 section.

**Explicitly not done in OPS-1** (deferred, not forgotten, not stubbed ahead of time): real delivery of an alert to a human (PagerDuty/Slack/email/SMS) — no deployed environment produces real traffic to evaluate and no paging provider has ever been chosen. A durable, cross-isolate metrics/alert history (needs Workers Analytics Engine, needs a deployed Cloudflare account — the same limitation `SECURITY_GUIDE.md` already names for EAP-7's own runtime metrics). Tuning alert thresholds or SLO targets against real production traffic (none has ever existed). A real on-call rotation, paging tool, or public status page (`INCIDENT_RESPONSE_GUIDE.md`'s own honest "one maintainer, nothing deployed" framing). Route-based code-splitting for `@titan/web`'s main bundle (PRD-1's own finding, re-confirmed this phase — still frontend-routing restructuring, out of an operations-only phase's scope). Enterprise Security & Compliance (SEC-1), Release Candidate, and General Availability work — per this program's own standing instruction to complete OPS-1 first.

## SEC-1: Enterprise Security & Compliance, built on the verified EAP + CPP + COM + PRD + OPS foundation

The "SEC-1 — Enterprise Security & Compliance" master prompt asked for security architecture review, threat modeling, application security, edge security documentation, rate limiting, security headers, dependency security, secrets governance, vulnerability management, penetration-test preparation, and compliance preparation only — explicitly excluding feature development, operations engineering, and any redesign of Authentication/Authorization/RBAC/the Assessment or Reporting Engines/the Administration or Customer Platforms/Production or Operations Infrastructure/the database architecture/the Repository Pattern/shared components/business logic. Ran from the already-clean, already-pushed OPS-1 baseline. Two research passes (existing security documentation, then every security-implementation file read in full) preceded any change, so this phase closes only genuine, evidenced gaps rather than re-describing or duplicating `SECURITY_GUIDE.md`'s already-mature threat model.

**What was found and fixed:** a real CSP gap (`frame-ancestors` missing from every route's policy — clickjacking protection had rested entirely on the legacy `X-Frame-Options` header) closed in `http/finalizeResponse.ts`. A real rate-limiting inconsistency (`POST /api/organizations` rate-limited while structurally-identical customer-facing writes weren't, contradicting the codebase's own stated rationale) partially closed — `POST /api/portal/support` and `POST`/`PATCH /api/portal/commercial/subscription` now share the same limiter. Explicit least-privilege `permissions: contents: read` added to both `titan-ci.yml` and `titan-deploy.yml`, matching `deploy.yml`'s own existing convention.

**What was found and honestly deferred, not blindly fixed:** a fresh `npm audit` — the first run against this exact workspace state — found 3 high-severity CVEs in `sharp`, required transitively by `miniflare`→`wrangler` (a devDependency, confirmed absent from all application code). `npm audit fix --force`'s own proposed remediation would downgrade `wrangler` from `4.112.0` to `4.15.2`, a ~97-minor-version regression of this project's entire local dev/CI/deploy tooling, since every wrangler release since `4.16.0` carries the identical transitive vulnerability. Applying it without individual verification would be reckless, not a fix — deferred with full reasoning in `THREAT_MODEL.md`'s findings register.

**Verification depth matches every prior phase**: `npm run ci` passes clean across all four packages — 1072 tests (up from 1068: +4, all in `@titan/platform` — 1 new CSP `frame-ancestors` regression test, 3 new rate-limit tests for the newly-protected routes). `@titan/web`/`@titan/assessment-core`/`@titan/design-system` unchanged (359/25/62), since SEC-1 touched no frontend code. Full detail: `ARCHITECTURE.md`'s "Enterprise Security & Compliance architecture (SEC-1)" section, `DECISION_LOG.md`'s SEC-1 entry, `PLATFORM_FOUNDATION.md`'s SEC-1 section.

**Explicitly not done in SEC-1** (deferred, not forgotten, not stubbed ahead of time): any change to the role model, `auth/rbac.ts`, `auth/authorize.ts`, or `auth/config.ts` — every one read in full and verified correct, not touched. Rate limiting for the remaining seven Platform-Administrator-only writes (a deliberate scope decision, `SECURITY_ARCHITECTURE.md`). A forced dependency downgrade to resolve the sharp/miniflare/wrangler finding. Any real penetration test or compliance certification — `THREAT_MODEL.md`/`COMPLIANCE_GUIDE.md` are real preparation material, not a claim either has occurred. No GA or Release Candidate work was started, per this program's own standing instruction to complete SEC-1 first.

## What Stage 5 / the next pass needs

1. ~~EAP Phase 8 (Reporting & Analytics)~~ — done. ~~CPP-1 (Enterprise Customer Portal)~~ — done. ~~COM-1 (Enterprise Commercial Platform)~~ — done. ~~PRD-1 (Enterprise Production Infrastructure)~~ — done. ~~OPS-1 (Enterprise Operations & SRE)~~ — done. ~~SEC-1 (Enterprise Security & Compliance)~~ — done, see above. Next: a release-classification re-evaluation, completing `DEPLOYMENT_GUIDE.md`'s "First real deploy checklist" (a real Cloudflare account is the one remaining external dependency every other item in this list is downstream of), a real payments-provider decision, or the smaller COM-1/CPP-1/PRD-1/OPS-1/SEC-1 follow-ups named in each section's own "Explicitly not done" paragraph.
2. **Organization-scoped filtering for `GET /api/leads`/`GET /api/leads/search`/`GET /api/organizations`/`GET /api/organizations/search`/`GET /api/assessments`/`GET /api/assessments/search`/`GET /api/users/search`/`GET /api/audit`/`GET /api/audit/search`/`GET /api/audit/export`/`GET /api/operations/summary`/`GET /api/reports/summary`/`GET /api/reports/trends`/`GET /api/reports/export`** — these admin-facing routes remain exactly as gapped as before; neither CPP-1 nor COM-1 touched, extended, or loosened any of them. CPP-1/COM-1 solved the *customer-facing* version of this same need instead, via dedicated `/api/portal/*` routes with `organizationId` forced server-side — a different caller and a different, narrower policy than "an organization's own admin sees their own organization's data through the existing admin routes," which remains unbuilt and would still need each repository's query layer extended to support it (`SECURITY_GUIDE.md`'s "Known, accepted gaps").
3. **A real assignee picker in Lead Lifecycle**, now genuinely unblocked by EAP-5's real user directory (`GET /api/users/search`) — the *reason* this was deferred since EAP-2 is closed; wiring the picker into `LeadLifecyclePanel.tsx` itself is real, recommended near-term work, not yet done.
4. **A real email provider decision**, to make Auth.js's Email sign-in actually send mail, and to unblock server-side reporting/delivery (Module H — the `reports` table, still distinct from EAP-8's own reporting layer).
5. **A real payments provider decision**, to move COM-1's already-real, provider-agnostic subscription/license/entitlement model toward an actual chargeable transaction — the one piece COM-1's own brief deliberately left out.
6. **The Vitest 3→4 decision**, if real Workers-runtime testing (`@cloudflare/vitest-pool-workers`, real `workerd`) becomes a priority. Still deferred (`DECISION_LOG.md`) — sql.js closed most of the practical gap for repository-level SQL correctness without requiring this upgrade.
7. **Deploying somewhere real** — PRD-1 built and structurally verified the automation that would do this (`DEPLOYMENT_GUIDE.md`'s "First real deploy checklist"), but a Cloudflare account/credentials still don't exist in any environment this project has run in, so nothing has actually deployed. Needed before Turnstile, a real global (not per-isolate) rate limiter, a real cross-isolate metrics backend (Workers Analytics Engine, EAP-7), a real paging integration (OPS-1), or a real (non-SQL-insert) way to provision the *first* Platform Administrator can exist (every grant after the first is now a real endpoint call, EAP-5).
8. **Route-based code-splitting for `@titan/web`'s main bundle** — a real finding from PRD-1's own performance review, re-confirmed by OPS-1's own review (834.46 kB / 186.42 kB gzip, `MONITORING_GUIDE.md`). `React.lazy` per top-level route (`/admin`, `/portal`, `/assessment/dpdp`) is the concrete, named approach; not attempted in PRD-1 or OPS-1 since it's application code restructuring, out of either phase's own scope.
9. **A real retention/archival policy for `audit_events`** — a new, real finding from OPS-1's own capacity-planning review (`SRE_GUIDE.md`): every write-path in this system appends to this table, forever, with nothing that ever deletes a row. Not an active problem at today's real volume (373 rows across this project's entire development history), but the one table worth planning ahead of real production traffic making it one.
10. **Tuning OPS-1's alert thresholds and SLO targets against real traffic**, once real traffic exists — `DEFAULT_ALERT_THRESHOLDS` and `SRE_GUIDE.md`'s SLO targets are reasonable starting defaults, not derived from production data that has never existed.
11. **A real paging-provider decision**, to move OPS-1's already-real, tested alert-evaluation logic toward actually notifying a human — the one piece OPS-1's own brief deliberately left blocked, the same standing reason a payments/email provider decision remains open.
12. **Monitor for a `wrangler`/`miniflare` release that resolves the `sharp` transitive vulnerability without a downgrade** — SEC-1's own real, deferred finding (`THREAT_MODEL.md`'s SEC-1-01); re-run `npm audit` periodically rather than assuming today's "no safe fix" conclusion holds indefinitely.
13. **Configure GitHub Environment protection rules for `titan-deploy.yml`'s `staging`/`production` environments** — a real, actionable repository setting named since PRD-1, re-confirmed still open by SEC-1's own threat model (`THREAT_MODEL.md`'s SEC-1-10), independent of the "no Cloudflare account" blocker every other deployment item sits behind.

## Phase 0 — Foundation

**Goal:** a deployable skeleton with nothing user-facing yet, but every cross-cutting concern (Module 15/16/17/18/19/20's *infrastructure*, not their full feature set) wired in from day one.

- Resolve the five open decisions in `ARCHITECTURE.md` (hosting target, database, auth approach, email provider, payments provider)
- `titan/` directory scaffolded: app skeleton, CI pipeline (build/lint/typecheck/test, mirroring the discipline already proven in this repo's existing `npm run build`/`verify-dist` pipeline)
- Database schema for the core entities (`ARCHITECTURE.md` data model), migrations tooling
- Auth skeleton (login, session/token handling) — no real user-facing screens yet, just the mechanism, tested
- `/health` endpoint, structured logging, error-tracking wired in
- `FEATURE_MATRIX.md` created — empty, ready for Phase 1

**Explicitly not in Phase 0:** any of the 20 feature modules. This phase produces zero user-visible functionality by design — it's the ground everything else stands on.

## Phase 1 — MVP: the free assessment, end to end

**Goal:** a working, tested, demoable core loop. This is the smallest slice that delivers the actual product value proposition.

- Module 1 (thin): landing page, hero, "start assessment" CTA — not the full marketing site (FAQ/pricing/blog/testimonials deferred)
- Module 2: questionnaire engine — question bank (from `DPDP_ASSESSMENT_FRAMEWORK.md`'s draft content, or reviewed content if the review-before-Phase-1 option was chosen), multi-step UX, validation, autosave/resume
- Module 3: risk engine — weighted scoring against the draft framework's risk categories
- Module 4 (partial): a results page with findings and priority ranking — not yet the full recommendation detail (effort estimates, reference material) that Module 4 specifies in full
- Module 17, 18 baseline: WCAG AA and SEO requirements met for every page shipped this phase, tested in CI, not assumed
- Module 20: unit tests for the risk engine (highest priority per `ARCHITECTURE.md`'s security/testing section), integration tests for the questionnaire API, one E2E test covering landing → complete assessment → see results

**Explicitly not in Phase 1:** AI Insights, PDF generation, lead capture beyond what's needed to show a result, CRM, admin portal, customer portal, booking, payments. The free tier does not require any of these.

**Definition of done:** a real person can land on the page, take the assessment, and see a risk result — and every quality gate for everything shipped this phase has passing evidence, not a claim.

## Phase 2 — Lead capture, reporting, first paid tier

- Module 7: lead qualification — capture contact info, compute a lead score from assessment answers + org size/industry
- Module 6: PDF generator — the paid "Gap Analysis" deliverable
- Module 4 (complete): full recommendation engine — business/operational impact, effort estimates, reference material
- Payments integration (per the decision made in Phase 0) — this is where "Paid Gap Analysis" actually becomes a real, chargeable tier
- Module 11 (partial): automated report-delivery email

**Gate:** a real transaction can occur — someone pays, gets a real PDF report, and that report's content traces to the same risk engine validated in Phase 1, not a separate, unaudited code path.

## Phase 3 — AI Insights, Admin Portal basics

- Module 5: AI Insights — personalized executive summary and risk narrative via Gemini, layered on top of (not replacing) the deterministic risk engine's output. **AI-generated text is additive framing, not the source of the risk score itself** — the score must remain traceable to the deterministic scoring logic from Phase 1, so the platform can always explain *why* a result was reached without depending on an LLM's explanation being consistent run-to-run.
- Module 9 (partial): admin can view assessments and leads — no full dashboard/analytics/template management yet

**Gate:** AI-generated content is reviewed for the specific failure mode of overclaiming compliance (it must never tell a user their organization "is compliant" — only frame identified gaps and priorities, consistent with `PRODUCT_VISION.md`'s scope boundary). This is a testable requirement (prompt + output review against a checklist), not a hope.

## Phase 4 — Growth & sales enablement

- Module 8: CRM (internal data model, per `ARCHITECTURE.md`)
- Module 12: booking (embedded scheduler)
- Module 11 (complete): automation — reminders, follow-ups, calendar integration
- Module 10 (partial): funnel analytics — landing conversion, drop-off, completion rate

## Phase 5 — Customer Portal & APIs

- Module 13 (partial): customer portal — 🟡 **assessment history and support are done (CPP-1)**; report downloads are done in the sense of a real compliance-summary CSV/JSON export, though CPP-1's own export is a live aggregate, not the server-side generated PDF Module 6/H still names; bookings remain unbuilt — no scheduling concept exists anywhere in this data model, and CPP-1's own brief explicitly excluded it
- Module 14: enterprise APIs — REST + webhooks, for the "Enterprise Consulting" and "Partner Programs" tiers that need programmatic access — explicitly excluded from CPP-1's own scope, unbuilt
- Module 9 (complete): full admin portal — templates, question-bank management, email templates

## Phase 6 — Platform generalization

- Extract the questionnaire/scoring/reporting engines from DPDP-specific naming into a generic `AssessmentFramework` abstraction (flagged as a low-cost-if-done-early architectural choice in `ARCHITECTURE.md` — this phase is where it pays off)
- Second assessment type (ISO 27001 or SOC 2 readiness, per the original "Enterprise recommendation") as a new question bank + scoring rubric on the existing engine — proof that the generalization actually works, not just a design intention
- Module 10 (complete): full analytics — industry trends, revenue funnel, consultation conversion
- Partner program tooling (white-label, referral tracking)

## Cross-cutting, every phase

Modules 15 (Security), 16 (Performance), 19 (Observability), 20 (Testing) are not phases — they're the bar every module in every phase above has to clear before that phase is called done. A "Phase 3 complete" claim that skipped a security review or shipped without tests isn't Phase 3 complete; it's an unvalidated claim, which the original brief's own stop conditions explicitly prohibit.

## What happens next

Nothing, until Phase 0's open decisions are answered and there's an explicit go-ahead to start implementation. This roadmap is a plan to review and revise, not a queue already running.
