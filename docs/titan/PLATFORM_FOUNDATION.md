# Platform Foundation — Status

Describes what's actually implemented under `titan/` as of this pass. Nothing below is aspirational — every claim has a passing check behind it, verified fresh, not carried over from a plan. See `ROADMAP.md` for what comes next and why it's sequenced this way.

## What exists

| Workstream (from the Phase 1 brief) | Status | Evidence |
|---|---|---|
| 1. Repository foundation | ✅ Done | `titan/` monorepo, npm workspaces (`apps/*`, `packages/*`), fully isolated from the existing marketing site's build — confirmed by rebuilding the site after adding `titan/` and re-checking `verify-dist` (still 7/7) |
| 2. Design system | ⚠️ **Started, not complete** | Tokens (color/spacing/typography) + 2 components (`Button`, `Alert`), real and tested — not the full 15-category list the brief specifies (Forms, Cards, Tables, Badges, Modals, Dialogs, Navigation, Loading/Empty/Error states are not built yet). Scoped down deliberately rather than build 15 shallow, undertested components — see `DEVELOPER_GUIDE.md` |
| 3. Application shell | ⚠️ **Started, not complete** | Layout, Header, Sidebar (data-driven, not hardcoded), Footer, routing, 404, global error boundary, skip link — all real, tested. **Breadcrumbs now exist** (`features/admin/layout/Breadcrumbs.tsx`, EAP-1) — scoped to the admin app shell specifically, derived from the real route path, not yet a general-purpose `@titan/design-system` component. Toast/notification system and loading skeletons are still not built (nothing yet needs them outside the admin Dashboard's own per-section loading states, which `useDashboardData`'s `SectionState` already covers without a generic skeleton component) |
| 4. Authentication foundation | ✅ **Done for this stage's scope** | `titan/packages/platform/src/auth/`: `createAuthConfig()` wires `@auth/d1-adapter` against the real Auth.js schema (`migrations/0001_authjs_core.sql`, verified against the real `@auth/d1-adapter` package's own migration SQL, not guessed), database session strategy, `/api/auth/*` mounted in the router. Email provider works in a dev-mode logging configuration (no real email provider decided — `DECISION_LOG.md`); Google/GitHub providers are wired but inactive without real credentials (never had any in this environment). Verified against real local D1 via `wrangler dev`, not just unit tests. Enterprise SSO explicitly out of scope for this stage |
| 5. Authorization (RBAC) | ✅ **Wired into real routes (Security Release Blocker Sprint; extended EAP-1; extended EAP-2)** | `auth/rbac.ts`: three-role organization model (member/admin/owner) plus Platform Administrator (an `organizationId: null`/`role: "owner"` profile — no schema change), organization-membership resolution (`user_profiles`). `auth/authorize.ts`'s gate helpers protect `GET /api/leads`/`GET /api/organizations`/`GET /api/assessments` (list)/`GET /api/audit` and (EAP-2) `GET /api/leads/:id`/`PATCH /api/leads/:id`/`GET /api/leads/search` (Platform Administrator, via the generalized `requirePlatformAdministrator`/`requireLeadsAccess`) and `GET /api/assessments/:id` (Platform Administrator or the assessment's own organization) — verified against a real running Worker, not just tests. Still a foundation, not full enterprise RBAC, matching the same scope line drawn around enterprise SSO |
| 6. Database foundation | ✅ **Done for this stage's scope** | `titan/packages/platform/migrations/`: 8 real migration files (Auth.js core, organizations, user_profiles, assessments, leads, reports, audit_events, EAP-2's lead lifecycle columns), applied and round-tripped against a **real local D1 SQLite instance** via `wrangler d1 migrations apply --local` — not a fake, not simulated. Five repositories (Lead, Organization, Assessment, UserProfile, Audit), each with in-memory + D1 implementations proven interchangeable by a shared contract test suite, itself run against real SQLite (sql.js) rather than a hand-rolled fake (`DECISION_LOG.md`) — `LeadRepository`/`AuditRepository` extended additively for EAP-2 (`findById`/`update`/`search` added, `list()` untouched; `list()` gained an optional filter) |
| 7. API foundation | ✅ **Done for this stage's scope** | `titan/packages/platform`'s Worker: `GET /health`, `POST`/`GET /api/leads`, `POST /api/assessments`, `GET /api/assessments/:id`, `/api/auth/*` — all with real validation, structured error responses, security headers, CORS, request-id correlation, a rate-limiting hook. **`@titan/web` now calls this API for real** — `leadStore.ts` POSTs to it, no longer `localStorage`-backed. Verified against real local D1 via `wrangler dev` with real HTTP requests, not just fakes in tests |
| 8. Observability | ✅ **Done for this stage's scope** | Structured JSON logging (`observability/logger.ts`), request correlation (`X-Request-Id`, generated or reused per request), error ids (every error response carries its request id), a basic metrics hook (`observability/metrics.ts` — request counts + durations). All scoped as real-but-per-isolate: this environment has never had Cloudflare Analytics Engine credentials, same constraint as everything else here |
| 9. Security | ✅ **Done for this stage's scope, HIGH finding fixed** | Input/schema validation, centralized error envelopes, security headers (CSP — route-scoped: strict for the JSON API, relaxed-but-locked-down for Auth.js's own HTML pages — X-Frame-Options, X-Content-Type-Options, Referrer-Policy, HSTS, Permissions-Policy, Cross-Origin-Opener-Policy, Cross-Origin-Resource-Policy) applied to every response, CORS, CSRF (Origin validation on `POST /api/leads`/`POST /api/assessments`), a documented per-isolate rate-limiting hook (separate, stricter limiter for `/api/auth/*` POST actions), cookie hardening and secret rotation. **`GET /api/leads`/`GET /api/assessments/:id` are now authorization-gated, and client-submitted risk scores are now recomputed server-side** (Security Release Blocker Sprint — both were this document's and `SECURITY_GUIDE.md`'s top two open findings; see that section below). `npm audit`: 0 known vulnerabilities. A full threat model + OWASP ASVS review exists (`SECURITY_GUIDE.md`), updated this pass |
| 10. Testing | ✅ **Substantially expanded** | 432 tests passing across 4 packages (up from 119 at Stage 4's start, 244 as of the Security Release Blocker Sprint, 300 as of EAP-1) — unit, component, contract (5 repositories × memory + D1, run against real SQLite), Worker/router integration tests (incl. authorization and server-side-recomputation cases), Auth.js config/session/RBAC/authorization tests, a contract test for `@auth/d1-adapter`'s own CRUD behavior. **Three real, committed Playwright E2E specs now exist** (`apps/web/e2e/`, RC1 + EAP-1 + EAP-2) — start the real backend and frontend, drive the full assessment flow to a real, asserted `POST /api/leads` response (RC1), the full admin sign-in → Dashboard → sign-out flow including a real cross-origin session (EAP-1), and real Lead Workspace search/filter/detail-navigation plus a lifecycle update persisting across a reload (EAP-2); 8 tests total, verified passing, strictly serial (`workers: 1` — a real cross-file data race on the suite's shared local D1 was found and fixed this pass, `DECISION_LOG.md`). Deliberately not yet wired into `titan-ci.yml` — a separate, explicit decision (`DECISION_LOG.md`) |
| 11. Developer experience | ✅ Done | ESLint (flat config, type-aware, `jsx-a11y` + `react-hooks` rulesets), Prettier, strict TypeScript, `.nvmrc`, workspace-wide scripts (`lint`, `format`, `typecheck`, `test`, `build`) |
| 12. Documentation | ✅ Kept current every pass | This document + `DEVELOPER_GUIDE.md`, updated again this pass (EAP-2) |

## Fresh verification evidence (this pass)

| Check | Result |
|---|---|
| `npm run typecheck` (all four packages) | ✅ Clean |
| `npm run lint` (`--max-warnings=0`) | ✅ Clean |
| `npm run format` (Prettier `--check`) | ✅ Clean |
| `npm run build` (all four packages) | ✅ Clean — `@titan/web` 488KB main JS / 141KB gzip (jsPDF, 391KB/129KB gzip, is a separate lazy-loaded chunk, confirmed not in the main bundle), `@titan/assessment-core` 6.4KB / 2.2KB gzip, `@titan/design-system` 13.6KB JS / 4.4KB gzip, `@titan/platform` 2.6KB / 1.0KB gzip |
| `npm run test` | ✅ **432/432 passing** (131 in `@titan/web`, 25 in `@titan/assessment-core`, 58 in `@titan/design-system`, 218 in `@titan/platform`) — up from 119 at the start of Stage 4, 222 as of ERP-1, 244 as of the Security Release Blocker Sprint, 300 as of EAP-1 Phase 1, +132 this pass (EAP-2: +45 web, +32 design-system, +55 platform, +0 assessment-core — untouched by this phase) |
| `npm run test:e2e` (`@titan/web`, RC1 + EAP-1 + EAP-2) | ✅ **8/8 passing** — `dpdp-assessment.spec.ts` (RC1, the public assessment flow) + `admin-dashboard.spec.ts` (EAP-1, 3 tests: unauthenticated redirect, full Platform Administrator flow incl. real cross-origin sign-out, non-admin honest-forbidden messaging) + `lead-workspace.spec.ts` (EAP-2, 4 tests: unauthenticated redirect, non-admin honest-forbidden messaging, real search→filter→detail-navigation, a real lifecycle update persisting across a reload with its own audit-trail entry), all via a real Chromium browser against a real `wrangler dev` Worker backed by real local D1, now strictly serial (`workers: 1`, EAP-2 — see below) |
| `npm run test:coverage` | ✅ Runs; `@titan/web` 94% statement coverage on components, `@titan/assessment-core` 100% on the question bank and risk engine, `@titan/design-system` 94% on components (token modules 0%, plain data), `@titan/platform`'s repository/router/auth logic covered via its contract + router/worker/auth tests |
| Golden-path browser check (Playwright + the pre-installed Chromium, not committed as a permanent test) | ✅ Full flow (landing → 15 questions → results → lead submit → real PDF download) works in a real browser; confirmed jsPDF isn't requested until the lead form is actually submitted; the only console errors are Google Fonts failing to load, specific to this sandbox's restricted network egress, not a code defect |
| Existing marketing site build, re-run after adding `titan/` | ✅ Unaffected — `verify-dist` still 7/7, zero files outside `titan/` and one new CI workflow touched (last confirmed when `titan/` was first added; unaffected again this pass since nothing outside `titan/` changed) |
| **Stage 4: real local D1 migrations + seed** | ✅ All 7 migration files applied cleanly against a real local D1 SQLite instance (`wrangler d1 migrations apply titan-platform-db --local`), `seed.sql` applied, a real join query across leads/organizations/assessments returned correct data |
| **Stage 4: real `wrangler dev` HTTP verification** | ✅ Real HTTP requests against a running `wrangler dev` instance: `GET /health` (200), `POST`/`GET /api/leads` (201/200), `POST /api/assessments` + `GET /api/assessments/:id` (201/200), `GET /api/auth/session` (200, real Auth.js + real D1 adapter, returns `null` with no session — matches unit test behavior), `OPTIONS` CORS preflight (204 with correct headers). Every persisted record independently re-verified via direct `wrangler d1 execute` queries, not just trusting the HTTP response |
| **Stage 4: real browser end-to-end submission** | ✅ Real Chromium (Playwright, one-off, not a committed dependency — `DECISION_LOG.md`) drove the actual `@titan/web` dev server through the full DPDP flow (15 questions → results → lead form) and submitted the lead form for real; the `POST /api/leads` fetch call returned 201 from the real running Worker; both browser-submitted leads (two runs) confirmed present in D1 via direct query, not assumed from the UI's "Report sent" state alone |
| **Security Release Blocker Sprint: real `wrangler dev` + real local D1 authorization verification** | ✅ Anonymous `GET /api/leads` → 401; the same caller authenticated (a real Auth.js CSRF token → email sign-in → magic-link callback flow, not a test shortcut) but with no `user_profiles` row → 403; the same caller after a direct D1 `INSERT` granting Platform Administrator → 200 with real data. A cross-organization `GET /api/assessments/:id` read (real FK-constrained `organizations`/`assessments` rows) → 403 for the wrong organization, 200 for a Platform Administrator. `audit_events` confirmed recording both `lead.created` and `assessment.created` via direct query |
| **Security Release Blocker Sprint: real server-side recomputation verification** | ✅ `POST /api/leads` submitted with `answers` indicating two failed critical DPDP controls alongside a client-claimed `result` of `score: 0`/`riskLevel: "low"` persisted the server's own real computation (`score: 17`, two real gaps with their actual penalties) — confirmed via the HTTP response, not just a test assertion. `POST /api/assessments` with an unrecognized `frameworkVersion` returned 400 `unsupported_framework`, not a 500 or a silently-wrong score |
| **EAP-1: real cross-origin sign-in, Dashboard data, and sign-out, in a real browser** | ✅ With `wrangler dev` (:8787) and Vite (:5173) both running, a real Chromium browser (hot-reloaded against live code, not a one-off script) drove: an unauthenticated visit to `/admin` redirecting to the real `/api/auth/signin` page; a real Email-provider sign-in (dev-mode magic link from `wrangler dev`'s own stdout, followed for real); `/admin` landing on the Dashboard with `GET /api/me` reflecting the real session; the four Platform-Administrator-gated sections showing honest "forbidden" messaging pre-grant and real seeded D1 data post-grant (a direct `INSERT` into `user_profiles`, same mechanism as the Security Release Blocker Sprint); and real sign-out ending the session (`GET /api/me` → 401 afterward) |
| **EAP-1: the CSP `form-action`/redirect bug — found, isolated, fixed, and re-verified live** | ✅ Real sign-out initially failed silently in the browser verification above. Isolated via scratch repro scripts (not committed) comparing the identical Auth.js sign-out form with and without a cross-origin `callbackUrl` — confirmed the form submits successfully with none present, and fails only once one pointing off-origin is added, pinning the cause to CSP's `form-action` restricting the redirect, not just the POST target. Fixed (`http/finalizeResponse.ts`'s `authPagesCsp`) and confirmed live against the already-running `wrangler dev` instance (Wrangler hot-reloads on file save) with zero console errors across a full repeat of the sign-in → dashboard → sign-out flow |
| **EAP-1: Playwright E2E, `admin-dashboard.spec.ts`** | ✅ 3/3 passing, run both standalone and combined with the existing `dpdp-assessment.spec.ts` (4/4 total) — confirms the real-browser findings above are captured as a permanent regression test, not just a one-time manual check |
| **EAP-2: real Lead Workspace/Lead Details browser verification** | ✅ With `wrangler dev` (:8787) and Vite (:5173) both running, a real Chromium browser drove: `/admin/leads` rendering real seeded leads with working search/status/priority/assigned-to filters and column sort; navigation into `/admin/leads/:id`; real lifecycle edits (status/priority/assignment/tag/note) each persisting via a real `PATCH` and appearing immediately in the same page's audit timeline; a saved filter surviving a reload (`localStorage`) |
| **EAP-2: the CORS `Access-Control-Allow-Methods` bug — found, isolated, fixed, and re-verified live** | ✅ A real Chromium `PATCH` from the Lead Lifecycle panel initially failed silently (UI showed no error, value never changed). Isolated via a standalone debug Playwright script (not committed) that captured the real browser console: `Method PATCH is not allowed by Access-Control-Allow-Methods in preflight response`. Fixed (`http/cors.ts`'s `ALLOWED_METHODS`) and confirmed live against the already-running `wrangler dev` instance with zero console errors across a full repeat of the lifecycle-update flow |
| **EAP-2: the `useLeadDetail` StrictMode race — found, fixed** | ✅ The same debug script that found the CORS bug first surfaced this one: a stale first-mount fetch (React 19 `<StrictMode>`'s intentional double-invocation) could resolve after a fresher `updateLifecycle`-triggered refetch, silently reverting the UI. Fixed by adding the same `cancelled`-flag guard `useDashboardData`/`useLeadSearch` already use; confirmed via a repeated real-browser lifecycle-update-then-reload flow showing the updated value staying put |
| **EAP-2: the Playwright cross-file D1 race — found, fixed** | ✅ A full-suite run (2 workers, Playwright's un-pinned default) failed `admin-dashboard.spec.ts`'s Dashboard-metrics test on a lead count off by one, traced to a concurrent `lead-workspace.spec.ts` test inserting a lead into the same shared local D1 in the same window. Fixed: `workers: 1` in `playwright.config.ts`. Re-verified with 2 consecutive clean 8/8 runs immediately after the fix. Full reasoning: `DECISION_LOG.md`'s EAP-2 entry |
| **EAP-2: Playwright E2E, `lead-workspace.spec.ts`** | ✅ 4/4 passing, run combined with the existing two specs (8/8 total), strictly serial — confirms the real-browser findings above are captured as a permanent regression test, not just a one-time manual check |

## Two real bugs this pass's own tests caught (worth recording, not hiding)

1. **`Button`'s loading state changed its accessible name.** The spinner's `aria-label="Loading"` merged into the button's computed accessible name ("Loading Submit" instead of "Submit"), caught by the component's own test suite before this was ever used anywhere. Fixed: the spinner is now `aria-hidden`, and `aria-busy` on the button itself is the correct signal to assistive tech.
2. **`NotFound` nested a `<button>` inside an `<a>`** (`Link` wrapping `Button`) — invalid HTML, ambiguous for screen readers. Fixed: replaced with `useNavigate()` + a real `Button` click handler.

## What "accessibility passes" and "security review completed" mean at this stage — and what they don't

- **Accessibility**: `jsx-a11y` lint rules run in CI; `axe-core` runs against every design-system component's rendered output in tests, with the `color-contrast` rule explicitly disabled and documented as to why (jsdom doesn't render real pixels — `DEVELOPER_GUIDE.md` explains this limitation and what would close the gap). This means: structural accessibility (ARIA correctness, keyboard operability, focus management) is genuinely checked. Color contrast, real screen-reader behavior, and full-page accessibility are **not** verified by anything in this repository yet — that needs either a real browser test run or manual audit, and should not be assumed from a green CI run.
- **Security review**: nothing in this phase handles user input, auth, or secrets, so there is no security surface to review yet beyond "the dependency tree has 0 known vulnerabilities" (`npm audit`, confirmed this pass) and "ARIA/DOM patterns don't introduce injection vectors" (no `dangerouslySetInnerHTML` anywhere in this codebase — confirmed by direct search). A real security review starts being meaningful once Workstreams 4, 6, and 7 exist.

## Phase 2 progress (through Stage 3)

Separate from the Phase 1 workstreams above: the Phase 2 master prompt ("Enterprise DPDP Platform Integration & Production Evolution") and its Stage 3 follow-up ("Production Integration & Operational Readiness") are working from the uploaded scanner asset toward Titan Module 1. See `ROADMAP.md`'s sub-phase table for the full picture; summary of what's real as of this pass:

| What | Evidence |
|---|---|
| Phase A (Discovery) | `ARCHITECTURE.md`'s Module 1 discovery section — including a confirmed `node --check` syntax error in the uploaded scanner that blocks all of its JS from running, and a confirmed off-by-one bug in its score denominator (13 hardcoded vs. 12 actual) |
| Phase B/C (Modularization, Question Engine) | `titan/packages/assessment-core` (data/logic, 5 tests) + `titan/apps/web/src/features/dpdp-assessment` (the real UI consuming it — see below) |
| Phase D (Risk Engine) | `titan/packages/assessment-core`'s `risk-engine/score.ts` — pure scoring functions, the source asset's denominator bug fixed and regression-tested, 20 tests, 100% statement/branch coverage |
| Stage 3: Application integration (Workstream 1-3) | The scanner is rebuilt as a real, tested React flow (`/assessment/dpdp` in `@titan/web`) that imports `dpdpV1` and `scoreAssessment` directly from `@titan/assessment-core` — zero duplicated question data or scoring logic. 48 tests, verified once in a real browser. Discovery's bugs are fixed as part of the rebuild: real radio/fieldset/progressbar semantics, real email validation, no `innerHTML`/XSS surface, the corrected "No Data Stored" claim, an on-screen compliance disclaimer |
| Stage 3: Cloudflare backend / repositories (Workstream 4-5) | `titan/packages/platform` — `LeadRepository` (Repository Pattern, in-memory + D1 implementations, contract-tested) and a Worker (`/health`, `POST /api/leads`), tested against a fake D1 double. Not deployed; not yet called by `@titan/web` |
| Hosting/database/auth decisions | Resolved — `DECISION_LOG.md` |

**Not done (as of Stage 3):** the original `dpdpriskscan.html` is still untouched (kept alongside the rebuild, not deleted). `@titan/web`'s lead form still writes to `localStorage`, not to the real `POST /api/leads` endpoint. No Auth.js code exists (Phase F). No admin or customer portal (Phase I/J). No deployed Worker or real D1 instance — everything backend-shaped in this pass is tested against fakes, not real Cloudflare infrastructure (no account/credentials in this environment). This is real, tested progress on two fronts, not a claim that Module 1 is fully integrated end to end.

## Phase 2 progress (through Stage 4)

Stage 4 ("Production Services & Cloudflare Operationalization") closed both of Stage 3's named gaps — the frontend/API wiring and a real local Cloudflare environment (not the Vitest 4 upgrade, which stays deferred, see `DECISION_LOG.md`) — plus Auth.js, audit logging, and security/observability hardening. Everything below is backed by evidence in `docs/titan/DECISION_LOG.md` and this pass's own commands, not carried over from a plan.

| What | Evidence |
|---|---|
| Cloudflare local dev environment | `wrangler.toml`, `migrations/` (7 files), `seed.sql`, `.dev.vars.example` — all 7 migrations applied and round-tripped against a **real local D1 SQLite instance**, not simulated |
| Repositories (Lead, Organization, Assessment, UserProfile, Audit) | Each with in-memory + D1 implementations, contract-tested against real SQLite (sql.js), not a hand-rolled fake (`DECISION_LOG.md`) |
| Worker API | `GET /health`, `POST`/`GET /api/leads`, `POST /api/assessments`, `GET /api/assessments/:id`, `/api/auth/*` — validated, structured errors, security headers, CORS, request correlation, rate-limit hook, audit events on write |
| Frontend migration off `localStorage` | `leadStore.ts` now POSTs to the real Worker API through a shared `apiClient.ts`; `LeadCaptureForm` surfaces the API's real error message and supports retry-by-resubmission |
| Auth.js foundation | `auth/config.ts`/`session.ts`/`rbac.ts` — D1-adapter-backed, database sessions, RBAC + org-membership foundation, Google/GitHub wired-but-inactive without real credentials, Email in dev-log mode. Enterprise SSO explicitly deferred |
| Audit logging | Lead and assessment creation both persist an `audit_events` row; a failed audit write is logged but never fails the request it describes |
| Security/observability | Centralized response finalization (security headers + CORS + request id on every response, including Auth.js's own), structured JSON logging, basic metrics hooks |
| Local operational verification | Real `wrangler dev` HTTP requests against every endpoint, independently re-verified via direct D1 queries; a real Chromium browser drove the full assessment flow to a real, persisted lead submission (Workstream 10 — see the verification evidence table above) |

**Not done (as of Stage 4):** the Vitest 3→4 upgrade for real `workerd`-backed automated tests remains deferred (`DECISION_LOG.md`). No admin or customer portal (Phase I/J — needs somewhere real to read leads/assessments from, which now exists, but the portals themselves are unbuilt). No deployed Cloudflare infrastructure anywhere — everything here is verified locally, never in production (no Cloudflare account/credentials in any environment this project has run in). CSRF protection for the custom JSON API endpoints (leads/assessments) is not implemented — Auth.js's own `/api/auth/*` actions have their own built-in CSRF handling, but `POST /api/leads`/`POST /api/assessments` do not yet, and should before either takes traffic that isn't same-origin-trusted. No email provider is decided, so Auth.js's Email sign-in cannot send real mail yet.

## RC1 pass: architecture audit, security hardening, observability, testing, documentation

Scoped from the "Titan RC1 — Ultra Enterprise Master Implementation Program" prompt, triaged rather than fully attempted (`DECISION_LOG.md` has the full reasoning). This pass covers architecture/security/observability/testing/documentation only — Admin Portal, Customer Portal, a server-side reporting platform, a Cloudflare deployment pipeline, commercial/billing readiness, and formal compliance work (SOC 2/ISO 27001/GDPR) are explicitly deferred, each for a concrete, named reason, not silently dropped.

| What | Evidence |
|---|---|
| Architecture audit | `ARCHITECTURE.md`'s new audit section: clean dependency DAG (no cycles), Repository Pattern boundary holds, shared models have one definition each, API contract is consistent, `npm audit` clean. One real tooling gap found and fixed: `.wrangler/` (wrangler dev's local build cache) wasn't excluded from ESLint's scan |
| CSRF | Origin-header validation on `POST /api/leads`/`POST /api/assessments` (`security/csrf.ts`) — closes the exact gap Stage 4's own report flagged as remaining |
| CSP correctness | Discovered (by actually calling `Auth()`) that Auth.js's default sign-in page renders real HTML with inline styles; the previous blanket strict CSP would have silently broken it. Routes now get a policy scoped to what they actually serve |
| Additional security headers | HSTS, Permissions-Policy, Cross-Origin-Opener-Policy, Cross-Origin-Resource-Policy — the last deliberately `cross-origin`, matching this API's real CORS design, verified by re-running the real browser flow afterward to confirm nothing broke |
| Cookie hardening, secret rotation | Reviewed against `@auth/core`'s own defaults (already sound); `AUTH_SECRET` now accepts an array for rotation |
| Rate limiting | `/api/auth/*` POST actions now go through a separate, stricter limiter than the general API |
| Threat model + ASVS review | `SECURITY_GUIDE.md` — a real STRIDE model per system surface and an OWASP ASVS control review, naming one real, unfixed finding directly (client-submitted risk scores aren't recomputed server-side) |
| Readiness probe | `GET /health/ready` — a real dependency check (`SELECT 1` against D1), distinct from `GET /health`'s pure liveness check |
| Operation timing | Repository calls record their own duration (`repository.duration_ms`), separate from total request duration |
| Playwright E2E | A real, committed suite (`apps/web/e2e/`) replacing Stage 4's throwaway script — verified passing against the real stack, not yet wired into CI (a separate decision, `DECISION_LOG.md`) |
| D1 adapter contract test | Exercises `@auth/d1-adapter`'s own CRUD directly (account linking, session lifecycle, cascading user deletion) against the real migration schema |
| Authorization-gate helper | `auth/authorize.ts`'s `requireOrganizationAccess` — not wired into any route yet (none are protected yet), but ready for the first one that is |

**Not done this pass, explicitly:** Admin Portal, Customer Portal, server-side reporting/email delivery, Cloudflare deployment pipeline (Pages/Workers/D1/Queues/R2/Turnstile — needs a real account), commercial/billing readiness (needs the still-undecided payments provider), and formal compliance work (SOC 2/ISO 27001/GDPR — organizational/legal undertakings, not code). See `DECISION_LOG.md`'s RC1 entry for why each was deferred.

## ERP-1 pass: independent release audit — engineering slice

Scoped from the "ERP-1 — Enterprise Release Program" prompt, which asked an *independent* audit to determine whether Titan had earned a release stage beyond Internal Alpha — explicitly not an instruction to make it look complete. Same engineering-only slice as RC1 (architecture/security/observability/testing/documentation); Admin/Customer Portal, reporting, deployment pipeline, commercial readiness, and formal compliance remain out of scope for the same reasons recorded in `DECISION_LOG.md`.

| What | Evidence |
|---|---|
| **`GET /api/leads` unauthenticated PII exposure — HIGH, found, not fixed** | Tracing the router's actual dispatch (not just reading past test coverage) found `GET /api/leads` returns every captured lead — name, email, company, DPDP answers, risk result — with no session or role check anywhere on that code path. Full detail, why it wasn't rushed-fixed, and the recommended fix: `SECURITY_GUIDE.md`'s Surface 2 table and "Known, accepted gaps." This is the single most important finding of this pass and the reason release classification does not advance this pass — see the ERP-1 Final Report |
| Two real WCAG AA failures found via a real Lighthouse audit (not jsdom+axe) | `npx lighthouse` against a `vite preview` production build (Chromium at `/opt/pw-browsers/chromium`) found (1) `color-contrast`: `--dpdp-text-muted` at `#6b7280` measured 3.99:1 against its background, failing AA's 4.5:1 body-text threshold; (2) `landmark-one-main`: the assessment page had no `<main>` landmark. Both were real defects jsdom+axe's `color-contrast`-disabled test suite (`PLATFORM_FOUNDATION.md`'s Phase 1 section) structurally cannot catch |
| Both fixed and **re-verified by a second Lighthouse run**, not assumed | `--dpdp-text-muted` changed to `#828b99` (5.61:1, passes AA, computed via the actual relative-luminance/contrast-ratio formulas, chosen to stay visually distinct from `--dpdp-text-secondary` at 7.6:1); `DpdpAssessmentPage.tsx`'s phase-conditional content wrapped in a `<main>` landmark between the existing `<header>`/`<footer>`. Lighthouse accessibility score: 92 → 100/100. Full typecheck + all 51 relevant tests re-run and passing after the change, not just the Lighthouse re-run alone |
| Same Lighthouse run also measured Performance (0.88) and SEO (0.45) — recorded honestly, not fixed this pass | Performance: LCP 2.0s, FCP 2.0s, TBT 50ms, CLS 0, TTI 2.2s — all real numbers but from a single headless-Chromium run inside this sandbox, not a production CDN edge, so treat as directional, not a production SLA claim. Speed Index measured 19.1s (scored 0) — driven by the `Particles` component's continuous canvas/DOM animation keeping the page "still changing" well past interactivity; a genuine finding, not a measurement artifact, but not investigated further this pass (out of the engineering-only scope). SEO scored 0.45: `is-crawlable` fails (page blocked from indexing), no `<meta name="description">`, and `robots.txt` is invalid — all three are real gaps on the public lead-gen page, appropriate for a product/marketing decision (this may be intentional for a pre-launch page) rather than an engineering fix, so left open and undecided rather than silently "fixed" by guessing intent |
| Supply-chain review | `package-lock.json` lockfile v3, `npm audit` clean (fresh this pass), exactly 3 postinstall scripts across the whole tree (`core-js`/`esbuild`/`workerd`, all expected), no dependency-confusion risk (`@titan/*` packages are private/workspace-local). Full detail: `SECURITY_GUIDE.md`'s new "Supply-chain review" section |
| ASVS V4 (Deny by default) re-evaluated honestly | RC1 recorded this as "N/A yet" reasoning no protected routes existed to fail against. ERP-1 actually traced dispatch instead of inferring from absence, and found a route that *should* have been access-controlled and wasn't. Downgraded to **Fail**, not left at the more comfortable "N/A" — `SECURITY_GUIDE.md` |
| Executive Review Board format — declined | ERP-1's prompt asked for a 9-persona "Executive Review Board" each returning individual quotes/approvals. Fabricating individual named executives and their opinions would itself be fabricated evidence, which every version of this program's rules explicitly prohibits. Findings are organized by domain in the Final Report instead, with the same substance (approval status, concerns, blocking issues, required actions, risk level per domain) minus invented personas |

**Not done this pass, explicitly:** same list as RC1's — Admin Portal, Customer Portal, server-side reporting/email delivery, Cloudflare deployment pipeline, commercial/billing readiness, formal compliance work. No new evidence exists for any of these; re-stating RC1's status here rather than re-deferring it under a new name.

## Security Release Blocker Sprint: the two ERP-1 findings, fixed and verified

Scoped from a resume prompt that described a prior session's work on this exact sprint in detail, ending mid-edit when a usage limit was hit. **That prior work was never committed** — verified via `git log`/`git reflog`/every remote branch/direct file inspection before writing any code, not assumed either way. See `DECISION_LOG.md`'s entry for the full evidence and reasoning. This section records what this pass actually verified in the repository, independent of what the prior session's transcript described.

| What | Evidence |
|---|---|
| **`GET /api/leads` unauthenticated PII exposure — HIGH, fixed** | `router.ts`'s `resolveCaller` (real Auth.js session lookup + `UserProfileRepository.findByUserId`) gates the route via `auth/authorize.ts`'s new `requireLeadsAccess`, requiring a **Platform Administrator** (`auth/rbac.ts`'s `isPlatformAdministrator` — a `user_profiles` row with `organizationId: null`, `role: "owner"`; no schema change). `GET /api/assessments/:id` is gated too, delegated to the assessment's own organization rather than Platform-Administrator-only, since a single assessment has one real organization to check membership against. Full design reasoning, including why leads and assessments are gated differently: `SECURITY_GUIDE.md`'s "Authorization model" |
| **Client-submitted risk scores not recomputed server-side — fixed** | `router.ts`'s `createLead`/`createAssessment` now call `@titan/assessment-core`'s `scoreAssessment` against real `answers` and discard the client-submitted `result` entirely. `createAssessment` resolves `framework`/`frameworkVersion` against a small registry (`resolveQuestionBank`, one entry today — `dpdpV1`) and returns 400 `unsupported_framework` for an unrecognized pair rather than guessing |
| **A real bug caught before it shipped, not after** | The first version of the Auth.js session-callback fix (needed so `resolveCaller` can read `session.user.id` — `@auth/core`'s default callback strips it) mutated and returned the callback's raw `session` argument. Probing it directly against a real adapter session — before wiring it into any route — showed that argument is `{...rawSessionRow, user}` for the database strategy, so returning it as-is would have leaked the session's own `sessionToken`/`userId` columns into `GET /api/auth/session`'s JSON body. Fixed to construct a fresh, minimal object instead of mutating the argument; re-verified by the same probe |
| Real operational verification (not just unit tests) | See the "Fresh verification evidence" table above — a real Auth.js CSRF-token → email-sign-in → magic-link-callback flow, a real Platform Administrator grant via direct D1 `INSERT`, and a real 401 → 403 → 200 progression for the same caller against the real running Worker |
| Test suite | 244 tests passing (up from 222) — `auth/rbac.test.ts` (+5), `auth/authorize.test.ts` (+8), `router.test.ts` (+8: anonymous/forbidden/authorized cases for both gated routes, tampering and unsupported-framework cases), `worker.test.ts` (+1: a real end-to-end 401 through the actual `worker.ts` wiring) |

**Not done this pass:** everything RC1/ERP-1 already deferred (Admin Portal, Customer Portal, reporting, deployment pipeline, commercial readiness, compliance) — no new evidence exists for any of these, and fixing exactly the two named release blockers was this sprint's explicit, narrow scope. Two gaps remain, named directly rather than hidden: `GET /api/leads` still can't be delegated to organization-scoped roles (the repository query itself isn't organization-filtered — `SECURITY_GUIDE.md`), and there is no self-service way to become a Platform Administrator (provisioning is a direct SQL insert, `OPERATIONAL_RUNBOOK.md`).

## EAP-1 Phase 1: Admin Application Foundation

Scoped from the "EAP-1 — Enterprise Administration Platform" prompt, which requested a full admin application (shell, dashboard, Lead/Assessment/Organization/User Management, an Audit Center, an Operations Center) in one pass. The user directed this to be built as an explicitly phased program instead, and scoped this session to Phase 1 only — full reasoning in `DECISION_LOG.md`'s EAP-1 entry. Phase 1: the authenticated application shell and exactly one complete module, Dashboard.

| What | Evidence |
|---|---|
| Authenticated application shell | `features/admin/auth/` (`SessionContext`/`useSession`, `RequireAuth`), `features/admin/layout/` (`AdminLayout`, `Breadcrumbs`, `adminNavItems`) — session lifecycle via `GET /api/me`, protected-route redirect to Auth.js's real hosted sign-in page (not a custom form), role-aware navigation built for pluggability (`ARCHITECTURE.md`'s "Admin Application architecture" section has the full design) |
| Reusable admin UI framework | `MetricCard`, `Panel` — added to `@titan/design-system` (not the feature folder) so later modules reuse them |
| Dashboard — the one complete module | `features/admin/dashboard/` — executive overview, organization/lead/assessment metrics, risk-level breakdown, recent activity, audit summary, platform health/system status. Each privileged section loads independently (`useDashboardData`'s `SectionState`) and shows an honest "Platform Administrator role required" state for a non-admin, never fabricated data |
| Four new read endpoints | `GET /api/me` (any authenticated caller), `GET /api/organizations`, `GET /api/assessments` (list), `GET /api/audit` (all three Platform-Administrator-only, via `auth/authorize.ts`'s generalized `requirePlatformAdministrator`) — reusing the existing role from the Security Release Blocker Sprint, no new authorization concept |
| Cross-origin session support | CORS `Access-Control-Allow-Credentials` (`http/cors.ts`) + `credentials: "include"` fetches (`apiClient.ts`), an Auth.js `redirect` callback origin allowlist (`auth/config.ts`) — all three needed because the admin SPA and the Worker are different origins in local dev, and a database-session cookie has to survive that round trip |
| **CSP `form-action`/redirect bug — found only by real-browser verification, fixed** | Auth.js's real sign-out page POSTs to its own origin then 302s to the cross-origin SPA's `callbackUrl`; the CSP spec's `form-action` directive restricts that redirect too, not just the POST target, and Chromium correctly refused to complete it under the previous, narrower policy — silently breaking cross-origin sign-out. Fixed: `http/finalizeResponse.ts`'s `authPagesCsp(allowedOrigin)`. The single most significant finding of this phase — structurally invisible to jsdom-based component tests or curl, caught only because this phase's own verification requirements insisted on a real sign-in through a real browser. Full reasoning: `DECISION_LOG.md`'s EAP-1 entry, `SECURITY_GUIDE.md`'s Surface 4 |
| Real operational verification | See the "Fresh verification evidence" table above — a real cross-origin sign-in/Dashboard/sign-out cycle in a real Chromium browser, the CSP bug's isolated repro and live re-verification, and a new committed Playwright suite (`apps/web/e2e/admin-dashboard.spec.ts`, 3 tests) |
| Test suite | 300 tests passing (up from 244) — `+9` `@titan/design-system` (`MetricCard`, `Panel`), `+23` `@titan/web` (`SessionContext`, `RequireAuth`, `AdminLayout`, `Breadcrumbs`, `DashboardPage`, routing, plus one real pre-existing type bug fixed in `leadStore.ts` — `fetchLeads()` was typed as `NewLead[]` instead of its real `LeadRecord[]` return shape), `+24` `@titan/platform` (redirect callback, `requirePlatformAdministrator`, the four new endpoints, one real end-to-end worker test) |

**Explicitly not built this phase, deferred to later EAP phases, not forgotten:** Lead Management, Assessment Management, Organization Management, User Management, an Audit Center, and an Operations Center as full modules (no create/edit/delete, no per-record detail views, no search/filter beyond the Dashboard's own summary needs). The shell is architected for each to plug in — see `ROADMAP.md`'s EAP-1 section for the recommended sequencing (Lead Management first: its API and authorization already exist and are already exercised by the Dashboard).

## EAP-2 Phase 2: Lead Intelligence Platform

Scoped from the "EAP-2 — Enterprise Lead Intelligence Platform" prompt, which explicitly built on EAP-1's shell and restricted scope to exactly one module: "Stay focused on Lead Intelligence." Full reasoning in `DECISION_LOG.md`'s EAP-2 entry. This phase: the first complete enterprise business module — a real Lead Workspace, Lead Details, Lead Lifecycle management, a Risk Intelligence panel, and audit integration — built entirely on EAP-1's shell/auth/RBAC/repository pattern, none of which were touched or replaced.

| What | Evidence |
|---|---|
| Lead lifecycle data model | `migrations/0008_lead_lifecycle.sql` — `status`/`priority`/`assignedTo`/`tags` columns added to the existing `leads` table (with indexes), not a new table. Internal notes and activity history reuse `audit_events` rather than a new table — a deliberate decision, `DECISION_LOG.md` |
| `LeadRepository` extended additively | `findById`, `update`, `search` added; `list()` — the EAP-1 Dashboard's own dependency — left completely unchanged, verified both by reading the diff and by the Dashboard's own tests still passing unmodified |
| Three new endpoints | `GET /api/leads/:id`, `PATCH /api/leads/:id` (Origin/CSRF-checked, same policy as the anonymous `POST` routes), `GET /api/leads/search` (server-side filter/sort/paginate) — all Platform-Administrator-only via the same `requireLeadsAccess`/`requirePlatformAdministrator` gate every other lead/organization/assessment-list/audit route already uses. `GET /api/audit` gained optional `entityType`/`entityId` filters, backward compatible with every existing no-args caller |
| Lead Workspace — the search/filter/table module | `features/admin/leads/LeadWorkspacePage.tsx` — server-backed search, status/priority/assigned-to filters, sortable columns, pagination, `localStorage`-backed saved filters and column selection (named, per-browser limitations, not silently dropped requirements) |
| Lead Details — the record module | `features/admin/leads/LeadDetailPage.tsx` — identity/organization, real submitted-assessment risk breakdown (findings grouped by severity, never a fabricated recommendations list), lifecycle controls, and a real activity/audit timeline sourced server-side from `GET /api/audit?entityType=lead&entityId=...` |
| Eight new shared design-system primitives | `Badge`, `SearchBar`, `Pagination`, `EmptyState`, `LoadingSkeleton`, `FilterPanel`, `Timeline`, `DataTable` — each justified by a real, immediate consumer in this phase, none speculative. Domain-aware wrappers (`StatusBadge`/`RiskBadge`/`PriorityBadge`) live in the feature layer, not the design system, to preserve its zero-`@titan/*`-dependency leaf-package invariant (`ARCHITECTURE.md`'s audit) |
| **CORS `Access-Control-Allow-Methods` bug — found only by real-browser verification, fixed** | `PATCH /api/leads/:id` is this codebase's first non-POST write; the Worker's CORS preflight response never listed `PATCH`, so every real cross-origin browser call to it failed client-side before reaching the Worker at all — invisible to any test calling `handleRequest`/`worker.fetch` directly. Fixed: `http/cors.ts`'s `ALLOWED_METHODS`. The single most significant finding of this phase, structurally the same class as EAP-1's CSP `form-action` bug. Full reasoning: `DECISION_LOG.md`'s EAP-2 entry, `SECURITY_GUIDE.md`'s Surface 2 |
| **`useLeadDetail` StrictMode race — found, fixed** | No `cancelled`-flag guard on its data-fetching effect, unlike every sibling hook — a stale first-mount fetch could resolve after a fresher refetch and silently revert the UI. Fixed to match the established pattern. Caught by this phase's own E2E test, not any unit test |
| **Playwright cross-file D1 race — found, fixed** | `fullyParallel: false` alone doesn't stop different spec files running in separate concurrent workers; a third lead-inserting spec file made a latent cross-file count race real. Fixed: `workers: 1`. Full reasoning: `DECISION_LOG.md`'s EAP-2 entry |
| Real operational verification | See the "Fresh verification evidence" table above — real Lead Workspace/Details browser verification, both bugs' isolated repro and live re-verification, and a new committed Playwright suite (`apps/web/e2e/lead-workspace.spec.ts`, 4 tests) |
| Test suite | 432 tests passing (up from 300) — `+32` `@titan/design-system` (8 new components), `+45` `@titan/web` (leads feature components/hooks/pages, nav/breadcrumb regression fixes), `+55` `@titan/platform` (repository/audit-filter/endpoint/router/worker tests) |

**Explicitly not built this phase, deferred to later EAP phases, not forgotten:** Assessment Management, Organization Management, User Management, an Audit Center, an Operations Center, a Customer Portal, or any commercial/billing/licensing surface. No deployment or infrastructure changes — this phase remains entirely local-only, same constraint as everything before it. See `ROADMAP.md`'s recommended EAP-3 scope (Assessment Management).

## What's next

**EAP-3 (Assessment Management)** is the recommended next module — `ROADMAP.md` has the reasoning (its API and Platform-Administrator authorization already exist, exercised by both the Dashboard and, indirectly, Lead Details' linked-assessment lookup). Separately, both of ERP-1's named release blockers (`GET /api/leads` authentication, server-side risk-score recomputation) remain fixed and verified — see the Security Release Blocker Sprint section above. A release-classification re-evaluation (the next ERP-style independent audit) is still the natural way to confirm whether the admin shell plus two real modules (Dashboard, Lead Intelligence) now built is sufficient to advance past Internal Alpha, or whether Gates C (Operations, no deployment) and E (Commercial, no payments/billing) still control the classification regardless — no new evidence exists on either gate this pass.
