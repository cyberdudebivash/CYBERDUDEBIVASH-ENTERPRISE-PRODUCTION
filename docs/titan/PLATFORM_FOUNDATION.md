# Platform Foundation — Status

Describes what's actually implemented under `titan/` as of this pass. Nothing below is aspirational — every claim has a passing check behind it, verified fresh, not carried over from a plan. See `ROADMAP.md` for what comes next and why it's sequenced this way.

## What exists

**This table is a snapshot as of EAP-5 and was not re-verified line-by-line for EAP-6 through EAP-8 or CPP-1** (each of those phases added its own dedicated section further down instead of rewriting every row here) — treat the test counts/endpoint lists below as historical, and see `FEATURE_MATRIX.md` plus this document's own EAP-6/EAP-7/EAP-8/CPP-1 sections for the current, fully current state. Recorded honestly rather than silently left implying currency it doesn't have.

| Workstream (from the Phase 1 brief) | Status | Evidence |
|---|---|---|
| 1. Repository foundation | ✅ Done | `titan/` monorepo, npm workspaces (`apps/*`, `packages/*`), fully isolated from the existing marketing site's build — confirmed by rebuilding the site after adding `titan/` and re-checking `verify-dist` (still 7/7) |
| 2. Design system | ⚠️ **Started, not complete** | Tokens (color/spacing/typography) + 2 components (`Button`, `Alert`), real and tested — not the full 15-category list the brief specifies (Forms, Cards, Tables, Badges, Modals, Dialogs, Navigation, Loading/Empty/Error states are not built yet). Scoped down deliberately rather than build 15 shallow, undertested components — see `DEVELOPER_GUIDE.md` |
| 3. Application shell | ⚠️ **Started, not complete** | Layout, Header, Sidebar (data-driven, not hardcoded), Footer, routing, 404, global error boundary, skip link — all real, tested. **Breadcrumbs now exist** (`features/admin/layout/Breadcrumbs.tsx`, EAP-1) — scoped to the admin app shell specifically, derived from the real route path, not yet a general-purpose `@titan/design-system` component. Toast/notification system and loading skeletons are still not built (nothing yet needs them outside the admin Dashboard's own per-section loading states, which `useDashboardData`'s `SectionState` already covers without a generic skeleton component) |
| 4. Authentication foundation | ✅ **Done for this stage's scope** | `titan/packages/platform/src/auth/`: `createAuthConfig()` wires `@auth/d1-adapter` against the real Auth.js schema (`migrations/0001_authjs_core.sql`, verified against the real `@auth/d1-adapter` package's own migration SQL, not guessed), database session strategy, `/api/auth/*` mounted in the router. Email provider works in a dev-mode logging configuration (no real email provider decided — `DECISION_LOG.md`); Google/GitHub providers are wired but inactive without real credentials (never had any in this environment). Verified against real local D1 via `wrangler dev`, not just unit tests. Enterprise SSO explicitly out of scope for this stage |
| 5. Authorization (RBAC) | ✅ **Wired into real routes (Security Release Blocker Sprint; extended EAP-1; extended EAP-2; extended EAP-3; extended EAP-4; extended EAP-5)** | `auth/rbac.ts`: three-role organization model (member/admin/owner) plus Platform Administrator (an `organizationId: null`/`role: "owner"` profile — no schema change), organization-membership resolution (`user_profiles`). `auth/authorize.ts`'s gate helpers protect `GET /api/leads`/`GET /api/organizations`/`GET /api/assessments` (list)/`GET /api/audit` and (EAP-2) `GET /api/leads/:id`/`PATCH /api/leads/:id`/`GET /api/leads/search` and (EAP-3) `GET /api/assessments/search` and (EAP-4) `POST /api/organizations`/`GET /api/organizations/search`/`GET /api/organizations/:id`/`PATCH /api/organizations/:id` and (EAP-5) `GET /api/users/search`/`GET /api/users/:id`/`POST /api/users/:id/profiles`/`PATCH`/`DELETE /api/users/:id/profiles/:profileId` (Platform Administrator, via the generalized `requirePlatformAdministrator`/`requireLeadsAccess`) and `GET /api/assessments/:id` (Platform Administrator or the assessment's own organization) — verified against a real running Worker, not just tests. EAP-5 additionally introduces `wouldRemoveLastPlatformAdministrator` (router.ts), a new guard preventing any role change/revoke from leaving the system with zero Platform Administrators. Still a foundation, not full enterprise RBAC, matching the same scope line drawn around enterprise SSO |
| 6. Database foundation | ✅ **Done for this stage's scope** | `titan/packages/platform/migrations/`: 9 real migration files (Auth.js core, organizations, user_profiles, assessments, leads, reports, audit_events, EAP-2's lead lifecycle columns, EAP-4's organization lifecycle columns), applied and round-tripped against a **real local D1 SQLite instance** via `wrangler d1 migrations apply --local` — not a fake, not simulated. **EAP-5 adds no new migration** — it reads Auth.js's own pre-existing `users` table (read-only) and extends the pre-existing `user_profiles` table's repository interface, not its schema. Six repositories now (Lead, Organization, Assessment, UserProfile, User, Audit), each with in-memory + D1 implementations proven interchangeable by a shared contract test suite, itself run against real SQLite (sql.js) rather than a hand-rolled fake (`DECISION_LOG.md`) — `LeadRepository`/`AuditRepository` extended additively for EAP-2 (`findById`/`update`/`search` added, `list()` untouched; `list()` gained an optional filter), `AssessmentRepository` extended additively for EAP-3 (`search()` added, `list()`/`save()`/`findById()` untouched; no migration needed), `OrganizationRepository`/`LeadRepository`/`AssessmentRepository` extended additively again for EAP-4 (`OrganizationRepository` gains `findById`/`update`/`search` plus real `status`/`industry`/`region`/`tags`/`updatedAt` fields; `Lead`/`AssessmentSearchOptions` each gain an `organizationId` filter), and `UserProfileRepository`/`AssessmentRepository` extended additively again for EAP-5 (`UserProfileRepository` gains `findById`/`list`/`update`/`remove` — this system's first real deletion; `AssessmentSearchOptions` gains a `createdBy` filter) alongside the brand-new, read-only `UserRepository` |
| 7. API foundation | ✅ **Done for this stage's scope** | `titan/packages/platform`'s Worker: `GET /health`, `POST`/`GET /api/leads`, `POST /api/assessments`, `GET /api/assessments/:id`, `GET /api/assessments/search` (EAP-3), `POST /api/organizations`/`GET /api/organizations/search`/`GET /api/organizations/:id`/`PATCH /api/organizations/:id` (EAP-4), `GET /api/users/search`/`GET /api/users/:id`/`POST /api/users/:id/profiles`/`PATCH`/`DELETE /api/users/:id/profiles/:profileId` (EAP-5), `/api/auth/*` — all with real validation, structured error responses, security headers, CORS, request-id correlation, a rate-limiting hook. `DELETE` is EAP-5's own addition to CORS's `Access-Control-Allow-Methods`, proactively (not found broken after shipping, unlike EAP-2's own `PATCH` addition). **`@titan/web` now calls this API for real** — `leadStore.ts` POSTs to it, no longer `localStorage`-backed. Verified against real local D1 via `wrangler dev` with real HTTP requests, not just fakes in tests |
| 8. Observability | ✅ **Done for this stage's scope** | Structured JSON logging (`observability/logger.ts`), request correlation (`X-Request-Id`, generated or reused per request), error ids (every error response carries its request id), a basic metrics hook (`observability/metrics.ts` — request counts + durations). All scoped as real-but-per-isolate: this environment has never had Cloudflare Analytics Engine credentials, same constraint as everything else here |
| 9. Security | ✅ **Done for this stage's scope, HIGH finding fixed** | Input/schema validation, centralized error envelopes, security headers (CSP — route-scoped: strict for the JSON API, relaxed-but-locked-down for Auth.js's own HTML pages — X-Frame-Options, X-Content-Type-Options, Referrer-Policy, HSTS, Permissions-Policy, Cross-Origin-Opener-Policy, Cross-Origin-Resource-Policy) applied to every response, CORS, CSRF (Origin validation on `POST /api/leads`/`POST /api/assessments`), a documented per-isolate rate-limiting hook (separate, stricter limiter for `/api/auth/*` POST actions), cookie hardening and secret rotation. **`GET /api/leads`/`GET /api/assessments/:id` are now authorization-gated, and client-submitted risk scores are now recomputed server-side** (Security Release Blocker Sprint — both were this document's and `SECURITY_GUIDE.md`'s top two open findings; see that section below). `npm audit`: 0 known vulnerabilities. A full threat model + OWASP ASVS review exists (`SECURITY_GUIDE.md`), updated this pass |
| 10. Testing | ✅ **Substantially expanded** | 713 tests passing across 4 packages (up from 119 at Stage 4's start, 244 as of the Security Release Blocker Sprint, 300 as of EAP-1, 432 as of EAP-2, 497 as of EAP-3, 609 as of EAP-4) — unit, component, contract (6 repositories × memory + D1, run against real SQLite), Worker/router integration tests (incl. authorization and server-side-recomputation cases), Auth.js config/session/RBAC/authorization tests, a contract test for `@auth/d1-adapter`'s own CRUD behavior. **Six real, committed Playwright E2E specs now exist** (`apps/web/e2e/`, RC1 + EAP-1 + EAP-2 + EAP-3 + EAP-4 + EAP-5) — start the real backend and frontend, drive the full assessment flow to a real, asserted `POST /api/leads` response (RC1), the full admin sign-in → Dashboard → sign-out flow including a real cross-origin session (EAP-1), real Lead Workspace search/filter/detail-navigation plus a lifecycle update persisting across a reload (EAP-2), real Assessment Workspace search/filter/detail-navigation plus Findings/Category coverage/Question responses/audit history/lead linkage (EAP-3), real Organization Workspace create/search/detail-navigation plus Health/Relationships/Administration(archive)/audit history (EAP-4), and real User Workspace search/detail-navigation plus a real grant→change→revoke Role Assignment round trip including this codebase's first real `DELETE` request (EAP-5); 20 tests total, verified passing, strictly serial (`workers: 1`), stable across 3 of 4 consecutive full-suite runs this pass (the one exception a reproduced-clean-on-retry `workerd` CLI-contention flake). Deliberately not yet wired into `titan-ci.yml` — a separate, explicit decision (`DECISION_LOG.md`) |
| 11. Developer experience | ✅ Done | ESLint (flat config, type-aware, `jsx-a11y` + `react-hooks` rulesets), Prettier, strict TypeScript, `.nvmrc`, workspace-wide scripts (`lint`, `format`, `typecheck`, `test`, `build`) |
| 12. Documentation | ✅ Kept current every pass | This document + `DEVELOPER_GUIDE.md`, updated again this pass (EAP-5) |

## Fresh verification evidence (this pass)

| Check | Result |
|---|---|
| `npm run typecheck` (all four packages) | ✅ Clean |
| `npm run lint` (`--max-warnings=0`) | ✅ Clean |
| `npm run format` (Prettier `--check`) | ✅ Clean |
| `npm run build` (all four packages) | ✅ Clean — `@titan/web` 488KB main JS / 141KB gzip (jsPDF, 391KB/129KB gzip, is a separate lazy-loaded chunk, confirmed not in the main bundle), `@titan/assessment-core` 6.4KB / 2.2KB gzip, `@titan/design-system` 13.6KB JS / 4.4KB gzip, `@titan/platform` 2.6KB / 1.0KB gzip |
| `npm run test` | ✅ **713/713 passing** (258 in `@titan/web`, 25 in `@titan/assessment-core`, 58 in `@titan/design-system`, 372 in `@titan/platform`) — up from 119 at the start of Stage 4, 222 as of ERP-1, 244 as of the Security Release Blocker Sprint, 300 as of EAP-1 Phase 1, 432 as of EAP-2, 497 as of EAP-3, 609 as of EAP-4, +104 this pass (EAP-5: +39 web, +65 platform, +0 design-system/assessment-core — untouched by this phase) |
| `npm run test:e2e` (`@titan/web`, RC1 + EAP-1 + EAP-2 + EAP-3 + EAP-4 + EAP-5) | ✅ **20/20 passing** — `dpdp-assessment.spec.ts` (RC1, the public assessment flow) + `admin-dashboard.spec.ts` (EAP-1, 3 tests: unauthenticated redirect, full Platform Administrator flow incl. real cross-origin sign-out, non-admin honest-forbidden messaging) + `lead-workspace.spec.ts` (EAP-2, 4 tests: unauthenticated redirect, non-admin honest-forbidden messaging, real search→filter→detail-navigation, a real lifecycle update persisting across a reload with its own audit-trail entry) + `assessment-center.spec.ts` (EAP-3, 4 tests: unauthenticated redirect, non-admin honest-forbidden messaging, real search→filter→detail-navigation, real Findings/Category coverage/Question responses/audit history/lead linkage) + `organization-workspace.spec.ts` (EAP-4, 4 tests: unauthenticated redirect, non-admin honest-forbidden messaging, real create-via-form→search→detail-navigation, real Health/Relationships/Administration(archive)/audit history) + `user-management.spec.ts` (EAP-5, 4 tests: unauthenticated redirect, non-admin honest-forbidden messaging, real search→detail-navigation, a real grant→change→revoke Role Assignment round trip including this codebase's first real `DELETE` request, with real audit history), all via a real Chromium browser against a real `wrangler dev` Worker backed by real local D1, strictly serial (`workers: 1`), stable across 3 of 4 consecutive full-suite runs this pass (the one exception a `workerd` CLI-contention flake, reproduced-clean on immediate retry) |
| `npm run test:coverage` | ✅ Runs; `@titan/web` 94% statement coverage on components, `@titan/assessment-core` 100% on the question bank and risk engine, `@titan/design-system` 94% on components (token modules 0%, plain data), `@titan/platform`'s repository/router/auth logic covered via its contract + router/worker/auth tests |
| Golden-path browser check (Playwright + the pre-installed Chromium, not committed as a permanent test) | ✅ Full flow (landing → 15 questions → results → lead submit → real PDF download) works in a real browser; confirmed jsPDF isn't requested until the lead form is actually submitted; the only console errors are Google Fonts failing to load, specific to this sandbox's restricted network egress, not a code defect |
| Existing marketing site build, re-run after adding `titan/` | ✅ Unaffected — `verify-dist` still 7/7, zero files outside `titan/` and one new CI workflow touched (last confirmed when `titan/` was first added; unaffected again this pass since nothing outside `titan/` changed) |
| **Stage 4: real local D1 migrations + seed** | ✅ All 7 migration files applied cleanly against a real local D1 SQLite instance (`wrangler d1 migrations apply titan-platform-db --local`), `seed.sql` applied, a real join query across leads/organizations/assessments returned correct data |
| **Stage 4: real `wrangler dev` HTTP verification** | ✅ Real HTTP requests against a running `wrangler dev` instance: `GET /health` (200), `POST`/`GET /api/leads` (201/200), `POST /api/assessments` + `GET /api/assessments/:id` (201/200), `GET /api/auth/session` (200, real Auth.js + real D1 adapter, returns `null` with no session — matches unit test behavior), `OPTIONS` CORS preflight (204 with correct headers). Every persisted record independently re-verified via direct `wrangler d1 execute` queries, not just trusting the HTTP response |
| **Stage 4: real browser end-to-end submission** | ✅ Real Chromium (Playwright, one-off, not a committed dependency — `DECISION_LOG.md`) drove the actual `@titan/web` dev server through the full DPDP flow (15 questions → results → lead form) and submitted the lead form for real; the `POST /api/leads` fetch call returned 201 from the real running Worker; both browser-submitted leads (two runs) confirmed present in D1 via direct query, not assumed from the UI's "Report sent" state alone |
| **Security Release Blocker Sprint: real `wrangler dev` + real local D1 authorization verification** | ✅ Anonymous `GET /api/leads` → 401; the same caller authenticated (a real Auth.js CSRF token → email sign-in → magic-link callback flow, not a test shortcut) but with no `user_profiles` row → 403; the same caller after a direct D1 `INSERT` granting Platform Administrator → 200 with real data. A cross-organization `GET /api/assessments/:id` read (real FK-constrained `organizations`/`assessments` rows) → 403 for the wrong organization, 200 for a Platform Administrator. `audit_events` confirmed recording both `lead.created` and `assessment.created` via direct query |
| **Security Release Blocker Sprint: real server-side recomputation verification** | ✅ `POST /api/leads` submitted with `answers` indicating two failed critical DPDP controls alongside a client-claimed `result` of `score: 0`/`riskLevel: "low"` persisted the server's own real computation (`score: 17`, two real gaps with their actual penalties) — confirmed via the HTTP response, not just a test assertion. `POST /api/assessments` with an unrecognized `frameworkVersion` returned 400 `unsupported_framework`, not a 500 or a silently-wrong score |
| **EAP-1: real cross-origin sign-in, Dashboard data, and sign-out, in a real browser** | ✅ With `wrangler dev` (:8787) and Vite (:5173) both running, a real Chromium browser (hot-reloaded against live code, not a one-off script) drove: an unauthenticated visit to `/admin` redirecting to the real `/api/auth/signin` page; a real Email-provider sign-in (dev-mode magic link from `wrangler dev`'s own stdout, followed for real); `/admin` landing on the Dashboard with `GET /api/me` reflecting the real session; the four Platform-Administrator-gated sections showing honest "forbidden" messaging pre-grant and real seeded D1 data post-grant (a direct `INSERT` into `user_profiles`, same mechanism as the Security Release Blocker Sprint); and real sign-out ending the session (`GET /api/me` → 401 afterward) |
| **EAP-1: the CSP `form-action`/redirect bug — found, isolated, fixed, and re-verified live** | ✅ Real sign-out initially failed silently in the browser verification above. Isolated via scratch repro scripts (not committed) comparing the identical Auth.js sign-out form with and without a cross-origin `callbackUrl` — confirmed the form submits successfully with none present, and fails only once one pointing off-origin is added, pinning the cause to CSP's `form-action` restricting the redirect, not just the POST target. Fixed (`http/finalizeResponse.ts`'s `authPagesCsp`) and confirmed live against the already-running `wrangler dev` instance (Wrangler hot-reloads on file save) with zero console errors across a full repeat of the sign-in → dashboard → sign-out flow |
| **EAP-1: Playwright E2E, `admin-dashboard.spec.ts`** | ✅ 3/3 passing, run both standalone and combined with the existing `dpdp-assessment.spec.ts` (4/4 total) — confirms the real-browser findings above are captured as a permanent regression test, not just a one-time manual check |
| **EAP-2: real Lead Workspace/Lead Details browser verification** | ✅ With `wrangler dev` (:8787) and Vite (:5173) both running, a real Chromium browser drove: `/admin/leads` rendering real seeded leads with working search/status/priority/assigned-to filters and column sort; navigation into `/admin/leads/:id`; real lifecycle edits (status/priority/assignment/tag/note) each persisting via a real `PATCH` and appearing immediately in the same page's audit timeline; a saved filter surviving a reload (`localStorage`) |
| **EAP-2: the CORS `Access-Control-Allow-Methods` bug — found, isolated, fixed, and re-verified live** | ✅ A real Chromium `PATCH` from the Lead Lifecycle panel initially failed silently (UI showed no error, value never changed). Isolated via a standalone debug Playwright script (not committed) that captured the real browser console: `Method PATCH is not allowed by Access-Control-Allow-Methods in preflight response`. Fixed (`http/cors.ts`'s `ALLOWED_METHODS`) and confirmed live against the already-running `wrangler dev` instance with zero console errors across a full repeat of the lifecycle-update flow |
| **EAP-2: the `useLeadDetail` StrictMode race — found, fixed** | ✅ The same debug script that found the CORS bug first surfaced this one: a stale first-mount fetch (React 19 `<StrictMode>`'s intentional double-invocation) could resolve after a fresher `updateLifecycle`-triggered refetch, silently reverting the UI. Fixed by adding the same `cancelled`-flag guard `useDashboardData`/`useLeadSearch` already use; confirmed via a repeated real-browser lifecycle-update-then-reload flow showing the updated value staying put |
| **EAP-2: the Playwright cross-file D1 race — found, fixed** | ✅ A full-suite run (2 workers, Playwright's un-pinned default) failed `admin-dashboard.spec.ts`'s Dashboard-metrics test on a lead count off by one, traced to a concurrent `lead-workspace.spec.ts` test inserting a lead into the same shared local D1 in the same window. Fixed: `workers: 1` in `playwright.config.ts`. Re-verified with 2 consecutive clean 8/8 runs immediately after the fix. Full reasoning: `DECISION_LOG.md`'s EAP-2 entry |
| **EAP-2: Playwright E2E, `lead-workspace.spec.ts`** | ✅ 4/4 passing, run combined with the existing two specs (8/8 total), strictly serial — confirms the real-browser findings above are captured as a permanent regression test, not just a one-time manual check |
| **EAP-3: real Assessment Workspace/Assessment Details browser verification** | ✅ With `wrangler dev` (:8787) and Vite (:5173) both running, a real Chromium browser drove: `/admin/assessments` rendering real seeded assessments (critical and low risk) with a working risk-level/framework filter and the Compliance Intelligence panel showing real aggregate numbers; navigation into `/admin/assessments/:id`; Findings/Category coverage/Question responses/Lead linkage/Activity & audit history all rendering real data with zero console errors; the pre-existing Lead Details page (`/admin/leads/:id`) re-verified unaffected by the shared hook's sequencing fix |
| **EAP-3: a real, pre-existing bug in `LeadWorkspacePage.tsx` — found while building its analogue, fixed** | The "Risk" column's `id: "risk"` was passed straight through to `onSortChange` by `DataTable`, but only `"riskScore"` is a `sortBy` value `router.ts` accepts — clicking it sent an invalid `sortBy=risk`, which the backend correctly 400'd, breaking the table. No existing test exercised that specific header click. Fixed (`id: "riskScore"`), covered by a new regression test, and built correctly from the start in the new `AssessmentWorkspacePage.tsx` |
| **EAP-3: a real, pre-existing race in `useLeadDetail.ts` — found, fixed; built correctly from the start in `useAssessmentDetail.ts`** | The record-fetch and audit-trail-fetch fired in parallel, with no ordering guarantee against the view-triggered audit write (`getLead`/`getAssessment` each await their own write before returning, but that says nothing about a second, independently-dispatched request's timing). Fixed by sequencing the audit-trail fetch to start only after the record fetch resolves, in both hooks. Verified via a new deterministic ordering test (a manually-resolved fetch Promise confirming the audit-trail request only fires after the record request resolves) |
| **EAP-3: a real, understood, dev-mode-only React `<StrictMode>` characteristic — investigated, recorded, not "fixed"** | Real-browser E2E verification showed two `assessment.viewed` audit events per single page view. Root-caused to `main.tsx`'s `<StrictMode>` double-invoking effects in development builds only — two real `GET /api/assessments/:id` requests genuinely reach the Worker per dev-mode page load, and it correctly records a real event for each. Confirmed via a direct D1 query showing both events with the real caller as actor. Not fixed: this does not occur in a production build, and event deduplication would risk masking a legitimately fast real second view. Full reasoning: `DECISION_LOG.md`'s EAP-3 entry |
| **EAP-3: Playwright E2E, `assessment-center.spec.ts`** | ✅ 4/4 passing standalone, 12/12 passing combined with the existing three specs, strictly serial (`workers: 1`), stable across 3 consecutive full-suite runs — confirms the real-browser findings above are captured as a permanent regression test |
| **EAP-4: real Organization Workspace/Details browser verification** | ✅ With `wrangler dev` (:8787) and Vite (:5173) both running, a real Chromium browser drove: creating a real organization through the Workspace's own inline form (name → auto-slugged identifier, still editable), the new organization appearing in a real server-side search, navigation into `/admin/organizations/:id`; Health/Relationships/Administration/Activity & audit history all rendering real data with zero console errors; archiving via the Administration panel's real `PATCH`, reflected in the page's own status badge after the server round-trip and in the audit timeline as a real `organization.archived` event |
| **EAP-4: a real, pre-existing bug in EAP-3's own shipped `leadApi.ts` — found while wiring this phase's equivalent call, fixed** | `searchLeads` accepted `LeadSearchOptions.assessmentId` (added EAP-3) but never forwarded it into the request's query string — every component test mocking `fetch` matched on the URL containing `/api/leads/search` alone, which passed regardless, so nothing caught it. Assessment Details' "Lead linkage" panel had been silently issuing an unfiltered `GET /api/leads/search?pageSize=100` since EAP-3 shipped. Fixed (both `assessmentId` and the new `organizationId` now forwarded), with new regression tests (`leadApi.test.ts`) asserting the real URL string, not just that a request occurred |
| **EAP-4: two real ambiguous-query fixes, in this phase's own new tests, not app bugs** | A component test asserting the Organization Health panel's "Critical" risk badge, and an E2E test asserting the same fact in a real browser, each initially matched a second, legitimate occurrence of similar text (a component test: the risk-distribution list's own lowercase "critical" label, matched by `getByText`'s default substring behavior; an E2E test: Playwright's `getByText`'s default case-insensitive matching hitting the identical lowercase label). Both fixed by scoping the query more precisely (a CSS selector; `{exact: true}`), the same "two legitimate real occurrences" pattern this project has hit and resolved identically several times before (`DECISION_LOG.md`) |
| **EAP-4: Playwright E2E, `organization-workspace.spec.ts`** | ✅ 4/4 passing standalone (twice), 16/16 passing combined with the existing four specs (twice), strictly serial (`workers: 1`) — confirms the real-browser findings above are captured as a permanent regression test |
| **EAP-5: real environmental finding — this container had never had `AUTH_SECRET` provisioned, so every authenticated real-browser scenario silently resolved as anonymous** | ✅ First symptom looked like a browser/cookie/proxy problem (this sandbox routes outbound traffic through a configured proxy). Reproduced against the already-shipped `organization-workspace.spec.ts` before assuming anything about new code — same failure, proving it wasn't EAP-5-specific. Root-caused with a direct `curl -H "Cookie: ..." -H "Origin: ..."` request bypassing the browser entirely: a real seeded session still returned 401, pointing at server-side session resolution. `packages/platform/.dev.vars` didn't exist in this fresh container (only `.dev.vars.example` did) — fixed by following `OPERATIONAL_RUNBOOK.md`'s own already-documented setup step. Full account: `DECISION_LOG.md`'s EAP-5 entry |
| **EAP-5: real User Workspace/User Details browser verification, after the `.dev.vars` fix above** | ✅ With `wrangler dev` (:8787) and Vite (:5173) both running, a real Chromium browser drove: `/admin/users` rendering real seeded users with a working name/email search; navigation into `/admin/users/:id`; Role Assignment granting a real organization role, changing it, and revoking it, each a real server round trip reflected after reload; Relationships and Activity & audit history rendering real data with zero console errors |
| **EAP-5: real deletion + real audit persistence, confirmed via direct D1 query** | ✅ `SELECT action, entity_type, actor_id FROM audit_events WHERE action LIKE 'user.%'` against the real local D1 instance confirmed `user.viewed`/`user.role_granted`/`user.role_changed`/`user.role_revoked` all persisted with the real Platform Administrator caller as `actor_id` — not just trusted from the UI's own rendering |
| **EAP-5: Playwright E2E, `user-management.spec.ts`** | ✅ 4/4 passing standalone, 20/20 passing combined with the existing five specs, strictly serial (`workers: 1`), stable across 3 of 4 consecutive full-suite runs (the one exception a `workerd` CLI-contention flake during D1 seeding, reproduced-clean on immediate retry, not a deterministic failure) — confirms the real-browser findings above, including this codebase's first real `DELETE` request, are captured as a permanent regression test |

## Two real bugs this pass's own tests caught (worth recording, not hiding)

1. **`Button`'s loading state changed its accessible name.** The spinner's `aria-label="Loading"` merged into the button's computed accessible name ("Loading Submit" instead of "Submit"), caught by the component's own test suite before this was ever used anywhere. Fixed: the spinner is now `aria-hidden`, and `aria-busy` on the button itself is the correct signal to assistive tech.
2. **`NotFound` nested a `<button>` inside an `<a>`** (`Link` wrapping `Button`) — invalid HTML, ambiguous for screen readers. Fixed: replaced with `useNavigate()` + a real `Button` click handler.

## What "accessibility passes" and "security review completed" mean at this stage — and what they don't

- **Accessibility**: `jsx-a11y` lint rules run in CI; `axe-core` runs against every design-system component's rendered output in tests, with the `color-contrast` rule explicitly disabled and documented as to why (jsdom doesn't render real pixels — `DEVELOPER_GUIDE.md` explains this limitation and what would close the gap). This means: structural accessibility (ARIA correctness, keyboard operability, focus management) is genuinely checked. Color contrast, real screen-reader behavior, and full-page accessibility are **not** verified by anything in this repository yet — that needs either a real browser test run or manual audit, and should not be assumed from a green CI run.
- **Security review**: nothing in this phase handles user input, auth, or secrets, so there is no security surface to review yet beyond "the dependency tree has 0 known vulnerabilities" (`npm audit`, confirmed this pass) and "ARIA/DOM patterns don't introduce injection vectors" (no `dangerouslySetInnerHTML` anywhere in this codebase — confirmed by direct search). A real security review starts being meaningful once Workstreams 4, 6, and 7 exist.

## Phase 2 progress (through Stage 3)

Separate from the Phase 1 workstreams above: the Phase 2 master prompt ("Enterprise DPDP Platform Integration & Production Evolution") and its Stage 3 follow-up ("Production Integration & Operational Readiness") are working from the uploaded scanner asset toward Titan Module 1. See `ROADMAP.md`'s sub-phase table for the full picture; summary of what's real as of this pass:

| What | Evidence |
|---|---|
| Phase A (Discovery) | `ARCHITECTURE.md`'s Module 1 discovery section — including a confirmed `node --check` syntax error in the uploaded scanner that blocks all of its JS from running, and a confirmed off-by-one bug in its score denominator (13 hardcoded vs. 12 actual) |
| Phase B/C (Modularization, Question Engine) | `titan/packages/assessment-core` (data/logic, 5 tests) + `titan/apps/web/src/features/dpdp-assessment` (the real UI consuming it — see below) |
| Phase D (Risk Engine) | `titan/packages/assessment-core`'s `risk-engine/score.ts` — pure scoring functions, the source asset's denominator bug fixed and regression-tested, 20 tests, 100% statement/branch coverage |
| Stage 3: Application integration (Workstream 1-3) | The scanner is rebuilt as a real, tested React flow (`/assessment/dpdp` in `@titan/web`) that imports `dpdpV1` and `scoreAssessment` directly from `@titan/assessment-core` — zero duplicated question data or scoring logic. 48 tests, verified once in a real browser. Discovery's bugs are fixed as part of the rebuild: real radio/fieldset/progressbar semantics, real email validation, no `innerHTML`/XSS surface, the corrected "No Data Stored" claim, an on-screen compliance disclaimer |
| Stage 3: Cloudflare backend / repositories (Workstream 4-5) | `titan/packages/platform` — `LeadRepository` (Repository Pattern, in-memory + D1 implementations, contract-tested) and a Worker (`/health`, `POST /api/leads`), tested against a fake D1 double. Not deployed; not yet called by `@titan/web` |
| Hosting/database/auth decisions | Resolved — `DECISION_LOG.md` |

**Not done (as of Stage 3):** the original `dpdpriskscan.html` is still untouched (kept alongside the rebuild, not deleted). `@titan/web`'s lead form still writes to `localStorage`, not to the real `POST /api/leads` endpoint. No Auth.js code exists (Phase F). No admin or customer portal (Phase I/J). No deployed Worker or real D1 instance — everything backend-shaped in this pass is tested against fakes, not real Cloudflare infrastructure (no account/credentials in this environment). This is real, tested progress on two fronts, not a claim that Module 1 is fully integrated end to end.

## Phase 2 progress (through Stage 4)

Stage 4 ("Production Services & Cloudflare Operationalization") closed both of Stage 3's named gaps — the frontend/API wiring and a real local Cloudflare environment (not the Vitest 4 upgrade, which stays deferred, see `DECISION_LOG.md`) — plus Auth.js, audit logging, and security/observability hardening. Everything below is backed by evidence in `docs/titan/DECISION_LOG.md` and this pass's own commands, not carried over from a plan.

| What | Evidence |
|---|---|
| Cloudflare local dev environment | `wrangler.toml`, `migrations/` (7 files), `seed.sql`, `.dev.vars.example` — all 7 migrations applied and round-tripped against a **real local D1 SQLite instance**, not simulated |
| Repositories (Lead, Organization, Assessment, UserProfile, Audit) | Each with in-memory + D1 implementations, contract-tested against real SQLite (sql.js), not a hand-rolled fake (`DECISION_LOG.md`) |
| Worker API | `GET /health`, `POST`/`GET /api/leads`, `POST /api/assessments`, `GET /api/assessments/:id`, `/api/auth/*` — validated, structured errors, security headers, CORS, request correlation, rate-limit hook, audit events on write |
| Frontend migration off `localStorage` | `leadStore.ts` now POSTs to the real Worker API through a shared `apiClient.ts`; `LeadCaptureForm` surfaces the API's real error message and supports retry-by-resubmission |
| Auth.js foundation | `auth/config.ts`/`session.ts`/`rbac.ts` — D1-adapter-backed, database sessions, RBAC + org-membership foundation, Google/GitHub wired-but-inactive without real credentials, Email in dev-log mode. Enterprise SSO explicitly deferred |
| Audit logging | Lead and assessment creation both persist an `audit_events` row; a failed audit write is logged but never fails the request it describes |
| Security/observability | Centralized response finalization (security headers + CORS + request id on every response, including Auth.js's own), structured JSON logging, basic metrics hooks |
| Local operational verification | Real `wrangler dev` HTTP requests against every endpoint, independently re-verified via direct D1 queries; a real Chromium browser drove the full assessment flow to a real, persisted lead submission (Workstream 10 — see the verification evidence table above) |

**Not done (as of Stage 4):** the Vitest 3→4 upgrade for real `workerd`-backed automated tests remains deferred (`DECISION_LOG.md`). No admin or customer portal (Phase I/J — needs somewhere real to read leads/assessments from, which now exists, but the portals themselves are unbuilt). No deployed Cloudflare infrastructure anywhere — everything here is verified locally, never in production (no Cloudflare account/credentials in any environment this project has run in). CSRF protection for the custom JSON API endpoints (leads/assessments) is not implemented — Auth.js's own `/api/auth/*` actions have their own built-in CSRF handling, but `POST /api/leads`/`POST /api/assessments` do not yet, and should before either takes traffic that isn't same-origin-trusted. No email provider is decided, so Auth.js's Email sign-in cannot send real mail yet.

## RC1 pass: architecture audit, security hardening, observability, testing, documentation

Scoped from the "Titan RC1 — Ultra Enterprise Master Implementation Program" prompt, triaged rather than fully attempted (`DECISION_LOG.md` has the full reasoning). This pass covers architecture/security/observability/testing/documentation only — Admin Portal, Customer Portal, a server-side reporting platform, a Cloudflare deployment pipeline, commercial/billing readiness, and formal compliance work (SOC 2/ISO 27001/GDPR) are explicitly deferred, each for a concrete, named reason, not silently dropped.

| What | Evidence |
|---|---|
| Architecture audit | `ARCHITECTURE.md`'s new audit section: clean dependency DAG (no cycles), Repository Pattern boundary holds, shared models have one definition each, API contract is consistent, `npm audit` clean. One real tooling gap found and fixed: `.wrangler/` (wrangler dev's local build cache) wasn't excluded from ESLint's scan |
| CSRF | Origin-header validation on `POST /api/leads`/`POST /api/assessments` (`security/csrf.ts`) — closes the exact gap Stage 4's own report flagged as remaining |
| CSP correctness | Discovered (by actually calling `Auth()`) that Auth.js's default sign-in page renders real HTML with inline styles; the previous blanket strict CSP would have silently broken it. Routes now get a policy scoped to what they actually serve |
| Additional security headers | HSTS, Permissions-Policy, Cross-Origin-Opener-Policy, Cross-Origin-Resource-Policy — the last deliberately `cross-origin`, matching this API's real CORS design, verified by re-running the real browser flow afterward to confirm nothing broke |
| Cookie hardening, secret rotation | Reviewed against `@auth/core`'s own defaults (already sound); `AUTH_SECRET` now accepts an array for rotation |
| Rate limiting | `/api/auth/*` POST actions now go through a separate, stricter limiter than the general API |
| Threat model + ASVS review | `SECURITY_GUIDE.md` — a real STRIDE model per system surface and an OWASP ASVS control review, naming one real, unfixed finding directly (client-submitted risk scores aren't recomputed server-side) |
| Readiness probe | `GET /health/ready` — a real dependency check (`SELECT 1` against D1), distinct from `GET /health`'s pure liveness check |
| Operation timing | Repository calls record their own duration (`repository.duration_ms`), separate from total request duration |
| Playwright E2E | A real, committed suite (`apps/web/e2e/`) replacing Stage 4's throwaway script — verified passing against the real stack, not yet wired into CI (a separate decision, `DECISION_LOG.md`) |
| D1 adapter contract test | Exercises `@auth/d1-adapter`'s own CRUD directly (account linking, session lifecycle, cascading user deletion) against the real migration schema |
| Authorization-gate helper | `auth/authorize.ts`'s `requireOrganizationAccess` — not wired into any route yet (none are protected yet), but ready for the first one that is |

**Not done this pass, explicitly:** Admin Portal, Customer Portal, server-side reporting/email delivery, Cloudflare deployment pipeline (Pages/Workers/D1/Queues/R2/Turnstile — needs a real account), commercial/billing readiness (needs the still-undecided payments provider), and formal compliance work (SOC 2/ISO 27001/GDPR — organizational/legal undertakings, not code). See `DECISION_LOG.md`'s RC1 entry for why each was deferred.

## ERP-1 pass: independent release audit — engineering slice

Scoped from the "ERP-1 — Enterprise Release Program" prompt, which asked an *independent* audit to determine whether Titan had earned a release stage beyond Internal Alpha — explicitly not an instruction to make it look complete. Same engineering-only slice as RC1 (architecture/security/observability/testing/documentation); Admin/Customer Portal, reporting, deployment pipeline, commercial readiness, and formal compliance remain out of scope for the same reasons recorded in `DECISION_LOG.md`.

| What | Evidence |
|---|---|
| **`GET /api/leads` unauthenticated PII exposure — HIGH, found, not fixed** | Tracing the router's actual dispatch (not just reading past test coverage) found `GET /api/leads` returns every captured lead — name, email, company, DPDP answers, risk result — with no session or role check anywhere on that code path. Full detail, why it wasn't rushed-fixed, and the recommended fix: `SECURITY_GUIDE.md`'s Surface 2 table and "Known, accepted gaps." This is the single most important finding of this pass and the reason release classification does not advance this pass — see the ERP-1 Final Report |
| Two real WCAG AA failures found via a real Lighthouse audit (not jsdom+axe) | `npx lighthouse` against a `vite preview` production build (Chromium at `/opt/pw-browsers/chromium`) found (1) `color-contrast`: `--dpdp-text-muted` at `#6b7280` measured 3.99:1 against its background, failing AA's 4.5:1 body-text threshold; (2) `landmark-one-main`: the assessment page had no `<main>` landmark. Both were real defects jsdom+axe's `color-contrast`-disabled test suite (`PLATFORM_FOUNDATION.md`'s Phase 1 section) structurally cannot catch |
| Both fixed and **re-verified by a second Lighthouse run**, not assumed | `--dpdp-text-muted` changed to `#828b99` (5.61:1, passes AA, computed via the actual relative-luminance/contrast-ratio formulas, chosen to stay visually distinct from `--dpdp-text-secondary` at 7.6:1); `DpdpAssessmentPage.tsx`'s phase-conditional content wrapped in a `<main>` landmark between the existing `<header>`/`<footer>`. Lighthouse accessibility score: 92 → 100/100. Full typecheck + all 51 relevant tests re-run and passing after the change, not just the Lighthouse re-run alone |
| Same Lighthouse run also measured Performance (0.88) and SEO (0.45) — recorded honestly, not fixed this pass | Performance: LCP 2.0s, FCP 2.0s, TBT 50ms, CLS 0, TTI 2.2s — all real numbers but from a single headless-Chromium run inside this sandbox, not a production CDN edge, so treat as directional, not a production SLA claim. Speed Index measured 19.1s (scored 0) — driven by the `Particles` component's continuous canvas/DOM animation keeping the page "still changing" well past interactivity; a genuine finding, not a measurement artifact, but not investigated further this pass (out of the engineering-only scope). SEO scored 0.45: `is-crawlable` fails (page blocked from indexing), no `<meta name="description">`, and `robots.txt` is invalid — all three are real gaps on the public lead-gen page, appropriate for a product/marketing decision (this may be intentional for a pre-launch page) rather than an engineering fix, so left open and undecided rather than silently "fixed" by guessing intent |
| Supply-chain review | `package-lock.json` lockfile v3, `npm audit` clean (fresh this pass), exactly 3 postinstall scripts across the whole tree (`core-js`/`esbuild`/`workerd`, all expected), no dependency-confusion risk (`@titan/*` packages are private/workspace-local). Full detail: `SECURITY_GUIDE.md`'s new "Supply-chain review" section |
| ASVS V4 (Deny by default) re-evaluated honestly | RC1 recorded this as "N/A yet" reasoning no protected routes existed to fail against. ERP-1 actually traced dispatch instead of inferring from absence, and found a route that *should* have been access-controlled and wasn't. Downgraded to **Fail**, not left at the more comfortable "N/A" — `SECURITY_GUIDE.md` |
| Executive Review Board format — declined | ERP-1's prompt asked for a 9-persona "Executive Review Board" each returning individual quotes/approvals. Fabricating individual named executives and their opinions would itself be fabricated evidence, which every version of this program's rules explicitly prohibits. Findings are organized by domain in the Final Report instead, with the same substance (approval status, concerns, blocking issues, required actions, risk level per domain) minus invented personas |

**Not done this pass, explicitly:** same list as RC1's — Admin Portal, Customer Portal, server-side reporting/email delivery, Cloudflare deployment pipeline, commercial/billing readiness, formal compliance work. No new evidence exists for any of these; re-stating RC1's status here rather than re-deferring it under a new name.

## Security Release Blocker Sprint: the two ERP-1 findings, fixed and verified

Scoped from a resume prompt that described a prior session's work on this exact sprint in detail, ending mid-edit when a usage limit was hit. **That prior work was never committed** — verified via `git log`/`git reflog`/every remote branch/direct file inspection before writing any code, not assumed either way. See `DECISION_LOG.md`'s entry for the full evidence and reasoning. This section records what this pass actually verified in the repository, independent of what the prior session's transcript described.

| What | Evidence |
|---|---|
| **`GET /api/leads` unauthenticated PII exposure — HIGH, fixed** | `router.ts`'s `resolveCaller` (real Auth.js session lookup + `UserProfileRepository.findByUserId`) gates the route via `auth/authorize.ts`'s new `requireLeadsAccess`, requiring a **Platform Administrator** (`auth/rbac.ts`'s `isPlatformAdministrator` — a `user_profiles` row with `organizationId: null`, `role: "owner"`; no schema change). `GET /api/assessments/:id` is gated too, delegated to the assessment's own organization rather than Platform-Administrator-only, since a single assessment has one real organization to check membership against. Full design reasoning, including why leads and assessments are gated differently: `SECURITY_GUIDE.md`'s "Authorization model" |
| **Client-submitted risk scores not recomputed server-side — fixed** | `router.ts`'s `createLead`/`createAssessment` now call `@titan/assessment-core`'s `scoreAssessment` against real `answers` and discard the client-submitted `result` entirely. `createAssessment` resolves `framework`/`frameworkVersion` against a small registry (`resolveQuestionBank`, one entry today — `dpdpV1`) and returns 400 `unsupported_framework` for an unrecognized pair rather than guessing |
| **A real bug caught before it shipped, not after** | The first version of the Auth.js session-callback fix (needed so `resolveCaller` can read `session.user.id` — `@auth/core`'s default callback strips it) mutated and returned the callback's raw `session` argument. Probing it directly against a real adapter session — before wiring it into any route — showed that argument is `{...rawSessionRow, user}` for the database strategy, so returning it as-is would have leaked the session's own `sessionToken`/`userId` columns into `GET /api/auth/session`'s JSON body. Fixed to construct a fresh, minimal object instead of mutating the argument; re-verified by the same probe |
| Real operational verification (not just unit tests) | See the "Fresh verification evidence" table above — a real Auth.js CSRF-token → email-sign-in → magic-link-callback flow, a real Platform Administrator grant via direct D1 `INSERT`, and a real 401 → 403 → 200 progression for the same caller against the real running Worker |
| Test suite | 244 tests passing (up from 222) — `auth/rbac.test.ts` (+5), `auth/authorize.test.ts` (+8), `router.test.ts` (+8: anonymous/forbidden/authorized cases for both gated routes, tampering and unsupported-framework cases), `worker.test.ts` (+1: a real end-to-end 401 through the actual `worker.ts` wiring) |

**Not done this pass:** everything RC1/ERP-1 already deferred (Admin Portal, Customer Portal, reporting, deployment pipeline, commercial readiness, compliance) — no new evidence exists for any of these, and fixing exactly the two named release blockers was this sprint's explicit, narrow scope. Two gaps remain, named directly rather than hidden: `GET /api/leads` still can't be delegated to organization-scoped roles (the repository query itself isn't organization-filtered — `SECURITY_GUIDE.md`), and there is no self-service way to become a Platform Administrator (provisioning is a direct SQL insert, `OPERATIONAL_RUNBOOK.md`).

## EAP-1 Phase 1: Admin Application Foundation

Scoped from the "EAP-1 — Enterprise Administration Platform" prompt, which requested a full admin application (shell, dashboard, Lead/Assessment/Organization/User Management, an Audit Center, an Operations Center) in one pass. The user directed this to be built as an explicitly phased program instead, and scoped this session to Phase 1 only — full reasoning in `DECISION_LOG.md`'s EAP-1 entry. Phase 1: the authenticated application shell and exactly one complete module, Dashboard.

| What | Evidence |
|---|---|
| Authenticated application shell | `features/admin/auth/` (`SessionContext`/`useSession`, `RequireAuth`), `features/admin/layout/` (`AdminLayout`, `Breadcrumbs`, `adminNavItems`) — session lifecycle via `GET /api/me`, protected-route redirect to Auth.js's real hosted sign-in page (not a custom form), role-aware navigation built for pluggability (`ARCHITECTURE.md`'s "Admin Application architecture" section has the full design) |
| Reusable admin UI framework | `MetricCard`, `Panel` — added to `@titan/design-system` (not the feature folder) so later modules reuse them |
| Dashboard — the one complete module | `features/admin/dashboard/` — executive overview, organization/lead/assessment metrics, risk-level breakdown, recent activity, audit summary, platform health/system status. Each privileged section loads independently (`useDashboardData`'s `SectionState`) and shows an honest "Platform Administrator role required" state for a non-admin, never fabricated data |
| Four new read endpoints | `GET /api/me` (any authenticated caller), `GET /api/organizations`, `GET /api/assessments` (list), `GET /api/audit` (all three Platform-Administrator-only, via `auth/authorize.ts`'s generalized `requirePlatformAdministrator`) — reusing the existing role from the Security Release Blocker Sprint, no new authorization concept |
| Cross-origin session support | CORS `Access-Control-Allow-Credentials` (`http/cors.ts`) + `credentials: "include"` fetches (`apiClient.ts`), an Auth.js `redirect` callback origin allowlist (`auth/config.ts`) — all three needed because the admin SPA and the Worker are different origins in local dev, and a database-session cookie has to survive that round trip |
| **CSP `form-action`/redirect bug — found only by real-browser verification, fixed** | Auth.js's real sign-out page POSTs to its own origin then 302s to the cross-origin SPA's `callbackUrl`; the CSP spec's `form-action` directive restricts that redirect too, not just the POST target, and Chromium correctly refused to complete it under the previous, narrower policy — silently breaking cross-origin sign-out. Fixed: `http/finalizeResponse.ts`'s `authPagesCsp(allowedOrigin)`. The single most significant finding of this phase — structurally invisible to jsdom-based component tests or curl, caught only because this phase's own verification requirements insisted on a real sign-in through a real browser. Full reasoning: `DECISION_LOG.md`'s EAP-1 entry, `SECURITY_GUIDE.md`'s Surface 4 |
| Real operational verification | See the "Fresh verification evidence" table above — a real cross-origin sign-in/Dashboard/sign-out cycle in a real Chromium browser, the CSP bug's isolated repro and live re-verification, and a new committed Playwright suite (`apps/web/e2e/admin-dashboard.spec.ts`, 3 tests) |
| Test suite | 300 tests passing (up from 244) — `+9` `@titan/design-system` (`MetricCard`, `Panel`), `+23` `@titan/web` (`SessionContext`, `RequireAuth`, `AdminLayout`, `Breadcrumbs`, `DashboardPage`, routing, plus one real pre-existing type bug fixed in `leadStore.ts` — `fetchLeads()` was typed as `NewLead[]` instead of its real `LeadRecord[]` return shape), `+24` `@titan/platform` (redirect callback, `requirePlatformAdministrator`, the four new endpoints, one real end-to-end worker test) |

**Explicitly not built this phase, deferred to later EAP phases, not forgotten:** Lead Management, Assessment Management, Organization Management, User Management, an Audit Center, and an Operations Center as full modules (no create/edit/delete, no per-record detail views, no search/filter beyond the Dashboard's own summary needs). The shell is architected for each to plug in — see `ROADMAP.md`'s EAP-1 section for the recommended sequencing (Lead Management first: its API and authorization already exist and are already exercised by the Dashboard).

## EAP-2 Phase 2: Lead Intelligence Platform

Scoped from the "EAP-2 — Enterprise Lead Intelligence Platform" prompt, which explicitly built on EAP-1's shell and restricted scope to exactly one module: "Stay focused on Lead Intelligence." Full reasoning in `DECISION_LOG.md`'s EAP-2 entry. This phase: the first complete enterprise business module — a real Lead Workspace, Lead Details, Lead Lifecycle management, a Risk Intelligence panel, and audit integration — built entirely on EAP-1's shell/auth/RBAC/repository pattern, none of which were touched or replaced.

| What | Evidence |
|---|---|
| Lead lifecycle data model | `migrations/0008_lead_lifecycle.sql` — `status`/`priority`/`assignedTo`/`tags` columns added to the existing `leads` table (with indexes), not a new table. Internal notes and activity history reuse `audit_events` rather than a new table — a deliberate decision, `DECISION_LOG.md` |
| `LeadRepository` extended additively | `findById`, `update`, `search` added; `list()` — the EAP-1 Dashboard's own dependency — left completely unchanged, verified both by reading the diff and by the Dashboard's own tests still passing unmodified |
| Three new endpoints | `GET /api/leads/:id`, `PATCH /api/leads/:id` (Origin/CSRF-checked, same policy as the anonymous `POST` routes), `GET /api/leads/search` (server-side filter/sort/paginate) — all Platform-Administrator-only via the same `requireLeadsAccess`/`requirePlatformAdministrator` gate every other lead/organization/assessment-list/audit route already uses. `GET /api/audit` gained optional `entityType`/`entityId` filters, backward compatible with every existing no-args caller |
| Lead Workspace — the search/filter/table module | `features/admin/leads/LeadWorkspacePage.tsx` — server-backed search, status/priority/assigned-to filters, sortable columns, pagination, `localStorage`-backed saved filters and column selection (named, per-browser limitations, not silently dropped requirements) |
| Lead Details — the record module | `features/admin/leads/LeadDetailPage.tsx` — identity/organization, real submitted-assessment risk breakdown (findings grouped by severity, never a fabricated recommendations list), lifecycle controls, and a real activity/audit timeline sourced server-side from `GET /api/audit?entityType=lead&entityId=...` |
| Eight new shared design-system primitives | `Badge`, `SearchBar`, `Pagination`, `EmptyState`, `LoadingSkeleton`, `FilterPanel`, `Timeline`, `DataTable` — each justified by a real, immediate consumer in this phase, none speculative. Domain-aware wrappers (`StatusBadge`/`RiskBadge`/`PriorityBadge`) live in the feature layer, not the design system, to preserve its zero-`@titan/*`-dependency leaf-package invariant (`ARCHITECTURE.md`'s audit) |
| **CORS `Access-Control-Allow-Methods` bug — found only by real-browser verification, fixed** | `PATCH /api/leads/:id` is this codebase's first non-POST write; the Worker's CORS preflight response never listed `PATCH`, so every real cross-origin browser call to it failed client-side before reaching the Worker at all — invisible to any test calling `handleRequest`/`worker.fetch` directly. Fixed: `http/cors.ts`'s `ALLOWED_METHODS`. The single most significant finding of this phase, structurally the same class as EAP-1's CSP `form-action` bug. Full reasoning: `DECISION_LOG.md`'s EAP-2 entry, `SECURITY_GUIDE.md`'s Surface 2 |
| **`useLeadDetail` StrictMode race — found, fixed** | No `cancelled`-flag guard on its data-fetching effect, unlike every sibling hook — a stale first-mount fetch could resolve after a fresher refetch and silently revert the UI. Fixed to match the established pattern. Caught by this phase's own E2E test, not any unit test |
| **Playwright cross-file D1 race — found, fixed** | `fullyParallel: false` alone doesn't stop different spec files running in separate concurrent workers; a third lead-inserting spec file made a latent cross-file count race real. Fixed: `workers: 1`. Full reasoning: `DECISION_LOG.md`'s EAP-2 entry |
| Real operational verification | See the "Fresh verification evidence" table above — real Lead Workspace/Details browser verification, both bugs' isolated repro and live re-verification, and a new committed Playwright suite (`apps/web/e2e/lead-workspace.spec.ts`, 4 tests) |
| Test suite | 432 tests passing (up from 300) — `+32` `@titan/design-system` (8 new components), `+45` `@titan/web` (leads feature components/hooks/pages, nav/breadcrumb regression fixes), `+55` `@titan/platform` (repository/audit-filter/endpoint/router/worker tests) |

**Explicitly not built this phase, deferred to later EAP phases, not forgotten:** Assessment Management, Organization Management, User Management, an Audit Center, an Operations Center, a Customer Portal, or any commercial/billing/licensing surface. No deployment or infrastructure changes — this phase remains entirely local-only, same constraint as everything before it. See `ROADMAP.md`'s recommended EAP-3 scope (Assessment Management).

## EAP-3 Phase 3: Enterprise Assessment Center

Scoped from the "EAP-3 — Enterprise Assessment Center" prompt, which explicitly built on EAP-1's shell and EAP-2's patterns and restricted scope to exactly one module: "Stay focused exclusively on Assessment Center." A resume prompt for this exact task described the module as substantially complete already; repository evidence (`git log`, every remote branch, reflog, dangling commits, pull-request history) showed none of that work existed — the prior session's container was reclaimed before anything reached `git`, the second time this exact failure mode has occurred in this project (the first: the Security Release Blocker Sprint entry above). Rebuilt fresh from the verified-clean EAP-2 baseline, using the transcript as a design reference only. Full reasoning: `DECISION_LOG.md`'s EAP-3 entry. This phase: the second complete enterprise business module — a real Assessment Workspace, Assessment Details, Assessment Results, a Compliance Intelligence panel, Lead linkage, and audit integration — built entirely on EAP-1's shell/auth/RBAC/repository pattern and EAP-2's design-system primitives, none of which were touched or replaced except for two verified defect fixes (below).

| What | Evidence |
|---|---|
| Read-only by design — no lifecycle data model added | An assessment is one immutable row per completed run (`migrations/0004_assessments.sql`'s own comment) — no migration was needed for this phase at all. `AssessmentRepository` extended additively (`search()` added; `save`/`findById`/`list()` untouched) |
| One new/extended endpoint | `GET /api/assessments/search` (server-side search on id/organizationId/createdBy substring, filter by framework/riskLevel, sort, paginate) — Platform-Administrator-only via the same `requirePlatformAdministrator` gate every other cross-organization list/search route uses. `GET /api/assessments/:id` (pre-existing) now also records a real `assessment.viewed` audit event. `GET /api/leads/search` gained an `assessmentId` filter for Assessment Details' "Lead linkage" panel |
| Assessment Workspace — the search/filter/table module | `features/admin/assessments/AssessmentWorkspacePage.tsx` — server-backed search, framework/risk-level filters, sortable columns, pagination, `localStorage`-backed saved filters and column selection, with a Compliance Intelligence panel (aggregate risk distribution/framework status/outstanding findings by section/risk trend, computed client-side from the existing `GET /api/assessments` full-list endpoint) above the table |
| Assessment Details — the record module | `features/admin/assessments/AssessmentDetailPage.tsx` — metadata (framework/version, an honest "Completed" status, completion timestamp, organization, owner), `AssessmentResultsPanel` (risk badge/score, severity breakdown, findings by severity, category coverage by real DPDP section, full question-by-question responses with Pass/Gap status — mirroring `LeadRiskPanel`'s structure/labeling deliberately, not diverging), Lead linkage (real leads this assessment produced), and a real audit timeline sourced server-side from `GET /api/audit?entityType=assessment&entityId=...` |
| Shared components — reused, not duplicated | `RiskBadge` imported directly from the leads feature (no lead-specific dependency, same precedent as `SectionState`'s existing cross-feature import). `FrameworkBadge` is genuinely new, with two real consumers (Workspace table, Details metadata panel) |
| **A real, pre-existing bug in EAP-2's shipped Lead Workspace — found only while building its Assessment Workspace analogue, fixed** | `LeadWorkspacePage.tsx`'s "Risk" column had `id: "risk"`, but `DataTable` passes a column's `id` straight through to `onSortChange`, and only `"riskScore"` is a `sortBy` value `router.ts` accepts — clicking it sent an invalid `sortBy=risk`, 400ing the whole table. No existing test exercised that header. Fixed (`id: "riskScore"`), covered by a new regression test |
| **A real, pre-existing race in `useLeadDetail.ts` — found, fixed; built correctly from the start in the new `useAssessmentDetail.ts`** | The record-fetch and audit-trail-fetch fired in parallel with no ordering guarantee against the view-triggered audit write — a real race, structurally the same class of finding as EAP-1's CSP bug and EAP-2's CORS bug (a real timing/browser behavior no server-side-only test exercises). Fixed by sequencing the audit-trail fetch after the record fetch resolves, in both hooks |
| **A real, understood, dev-mode-only React `<StrictMode>` characteristic — investigated and recorded, not "fixed"** | Two `assessment.viewed` audit events per real page view, in this dev-server environment specifically — root-caused to `<StrictMode>`'s development-only double-invocation, confirmed via direct D1 query, deliberately not "fixed" via event deduplication (see `DECISION_LOG.md`'s EAP-3 entry for the full reasoning) |
| Real operational verification | See the "Fresh verification evidence" table above — real Assessment Workspace/Details browser verification, both real bugs' fixes, the StrictMode finding, and a new committed Playwright suite (`apps/web/e2e/assessment-center.spec.ts`, 4 tests) |
| Test suite | 497 tests passing (up from 432) — `+38` `@titan/web` (assessments feature components/hooks/pages, `LeadWorkspacePage`/`useLeadDetail`/`App`/`navItems` regression coverage), `+27` `@titan/platform` (repository/router/worker tests) |

**Explicitly not built this phase, deferred to later EAP phases, not forgotten:** Organization Management, User Management, an Audit Center, an Operations Center, a Customer Portal, or any commercial/billing/licensing surface. No assessment lifecycle/status workflow was added (none exists to manage — see above) and no `EvidencePanel` component was built (no file-upload/attachment concept exists anywhere in this codebase's data model). No deployment or infrastructure changes — this phase remains entirely local-only, same constraint as everything before it. See `ROADMAP.md`'s recommended next EAP phase (Organization Management).

## EAP-4 Phase 4: Enterprise Organization Management Platform

Scoped from the "EAP-4 — Enterprise Organization Management Platform" prompt, which explicitly built on the now-verified EAP-1/EAP-2/EAP-3 architecture and restricted scope to exactly one module: "Stay focused on Organizations." Verified via real git evidence at the start of this phase (not assumed) that EAP-3's commit was genuinely present on both local `HEAD` and the remote branch, working tree clean — a real, clean continuation, not a third instance of the lost-work failure mode the Security Release Blocker Sprint and EAP-3 entries above both document. Full reasoning: `DECISION_LOG.md`'s EAP-4 entry. This phase: the third complete enterprise business module — a real Organization Workspace, Organization Details (Metadata, Health, Relationships, Administration, audit history), and, unlike Assessments, a real create/update/archive/restore write surface — built entirely on EAP-1's shell/auth/RBAC/repository pattern and EAP-2/EAP-3's design-system primitives, none of which were touched or replaced except for one verified defect fix (below).

| What | Evidence |
|---|---|
| Data model — additive, real lifecycle columns | `migrations/0009_organization_lifecycle.sql` adds `status`/`industry`/`region`/`tags_json`/`updated_at` to the existing `organizations` table. `status` is closed two-value (`active`/`archived`) — archived and restorable, never deleted, matching every other repository's own no-`delete` contract. `updated_at` is repository-owned (set on `save`, refreshed on every `update`), the first timestamp field in this codebase not trusted from the caller. `OrganizationRepository` extended additively (`findById`/`update`/`search` added; `list()`, EAP-1's Dashboard's own dependency, untouched). `LeadSearchOptions`/`AssessmentSearchOptions` each gained an `organizationId` exact-match filter, mirroring EAP-3's `assessmentId` filter pattern |
| Four new/extended endpoints | `POST /api/organizations` (create, with a real slug-uniqueness conflict check), `GET /api/organizations/search` (server-side search on name/slug/industry/region substring, filter by status/industry/region/tag, sort, paginate), `GET /api/organizations/:id` (records a real `organization.viewed` audit event), `PATCH /api/organizations/:id` (metadata edits/archive/restore/notes — diffs the patch against the pre-update record so only changed fields produce an audit event, archive/restore getting their own named events rather than a generic "updated") — all Platform-Administrator-only via the same `requirePlatformAdministrator` gate every other cross-organization route uses, CSRF-checked the same way `PATCH /api/leads/:id` already is |
| A deliberate scope cut, evaluated not assumed | No `riskLevel`/`assessmentStatus` filter on organization search — `assessmentStatus` has no backing field anywhere in this system (assessments have no lifecycle), and `riskLevel` would require a cross-repository join this codebase's repository-isolation architecture doesn't support anywhere, for a query volume that doesn't justify introducing it. Risk is surfaced instead at the single-organization scope Organization Health actually needs |
| Organization Workspace — the search/filter/table/create module | `features/admin/organizations/OrganizationWorkspacePage.tsx` — server-backed search, status filter, sortable columns, pagination, `localStorage`-backed saved filters and column selection, plus a real inline create-organization form (this module's first admin-facing creation UI) that auto-slugs the identifier from the name while staying directly editable |
| Organization Details — the record module | `features/admin/organizations/OrganizationDetailPage.tsx` — Metadata (identifier/industry/region/tags/created/last-activity), `OrganizationHealthPanel` (current risk/average score/risk distribution/score trend, all derived from this organization's own real linked assessments via the new `organizationId` filter, computed client-side the same way `ComplianceIntelligencePanel` aggregates for EAP-3 — nothing fabricated), `OrganizationRelationshipsPanel` (real linked leads/assessments, deep-linked into the existing Lead Intelligence/Assessment Center modules rather than duplicating either), `OrganizationAdministrationPanel` (real name/industry/region/tag edits committed on blur/change, archive/restore, notes — every control a direct `PATCH`, never optimistic-only, mirroring `LeadLifecyclePanel`'s established pattern), and a real audit timeline sourced server-side from `GET /api/audit?entityType=organization&entityId=...` |
| Shared components — reused, not duplicated | `RiskBadge`/`StatusBadge` imported directly from the leads feature for Relationships' list items — no lead-specific dependency, same precedent as EAP-3's `RiskBadge` reuse. `OrganizationStatusBadge` is genuinely new (active/archived), same domain-wrapper pattern as `FrameworkBadge`/`StatusBadge` |
| **A real, pre-existing bug in EAP-3's own shipped frontend code — found only while wiring this phase's equivalent call, fixed** | `leadApi.ts`'s `searchLeads` accepted `LeadSearchOptions.assessmentId` (added EAP-3) but never forwarded it into the request's query string — every component test mocking `fetch` matched on the URL containing `/api/leads/search` alone, which passed regardless of the actual query string, so nothing in this codebase's test suite caught it. Assessment Details' "Lead linkage" panel had been silently calling an unfiltered `GET /api/leads/search?pageSize=100` since EAP-3 shipped. Fixed (`assessmentId` and the new `organizationId` both now forwarded), with new regression tests (`leadApi.test.ts`, a file that didn't exist before this phase) asserting the real URL string built |
| **Two real ambiguous-query fixes in this phase's own new tests — ambiguous UI text, not app bugs** | A component test and an E2E test each initially matched two legitimate real occurrences of similar text (a "Critical" risk badge and the risk-distribution list's own lowercase "critical" label) — the same class of issue this project has hit and resolved identically several times before (`DECISION_LOG.md`). Fixed via a more specific selector in each case, not by loosening either assertion |
| Real operational verification | See the "Fresh verification evidence" table above — real Organization Workspace/Details browser verification (including a real create → search → archive round trip), the `leadApi.ts` bug's fix, and a new committed Playwright suite (`apps/web/e2e/organization-workspace.spec.ts`, 4 tests) |
| Test suite | 609 tests passing (up from 497) — `+50` `@titan/web` (organizations feature components/hooks/pages, `leadApi.ts`/`assessmentApi.ts` regression coverage, nav/routing), `+62` `@titan/platform` (repository/router/worker tests) |

**Explicitly not built this phase, deferred to later EAP phases, not forgotten:** an Audit Center, an Operations Center, a Customer Portal, or any commercial/billing/licensing surface. No organization-scoped delegation of any cross-organization list/search endpoint — every gate stays Platform-Administrator-only. No deployment or infrastructure changes — this phase remains entirely local-only, same constraint as everything before it. See `ROADMAP.md`'s recommended next EAP phase (the Audit Center).

## EAP-5 Phase 5: Enterprise Identity & User Management

Scoped from an "EAP-5 — Enterprise Identity & User Management" prompt, which built on the now-verified EAP-1 through EAP-4 architecture. Verified via real git evidence at the start of this phase (not assumed) that `main` genuinely had EAP-1 through EAP-4 merged (PRs #40–#44), and that the designated branch for this phase itself had zero prior commits anywhere — a real, clean start, not a resume, and not a third instance of the lost-work failure mode the Security Release Blocker Sprint and EAP-3 entries above both document. Full reasoning: `DECISION_LOG.md`'s EAP-5 entry. This phase: the fourth complete enterprise business module — a real Enterprise User Directory, User Details (Identity, Role Assignment, Relationships, audit history), and a real grant/change/revoke write surface for organization and platform-wide roles — built entirely on EAP-1's shell/auth/RBAC/repository pattern and EAP-2/EAP-3/EAP-4's design-system primitives, none of which were touched or replaced.

| What | Evidence |
|---|---|
| Data model — no new table, a new read-only repository plus an extended existing one | A new `UserRepository` (`findById`/`search`, no `save`) reads Auth.js's own pre-existing `users` table (migrations/0001) — the real identity record, written exclusively by `@auth/d1-adapter`. `UserProfileRepository` (pre-existing since RC1) extended additively with `findById`/`list`/`update`/`remove` — `remove` is this system's first real deletion, a deliberate, reasoned exception to every other repository's archive-not-delete pattern (a role grant has no dependent data of its own to preserve). `AssessmentSearchOptions` gained a `createdBy` exact-match filter, mirroring EAP-3's `assessmentId`/EAP-4's `organizationId` filter pattern exactly |
| Five new endpoints | `GET /api/users/search` (server-side search on name/email substring, sort, paginate), `GET /api/users/:id` (identity composed with the user's own `UserProfileRecord[]`, records a real `user.viewed` audit event), `POST /api/users/:id/profiles` (grant — validates target user/organization existence, 409 on duplicate grant), `PATCH /api/users/:id/profiles/:profileId` (change role), `DELETE /api/users/:id/profiles/:profileId` (revoke — this codebase's first real `DELETE` request) — all Platform-Administrator-only via the same `requirePlatformAdministrator` gate every other cross-organization route uses, the three writes CSRF-checked the same way `PATCH /api/organizations/:id` already is, and additionally guarded by the new `wouldRemoveLastPlatformAdministrator` whenever the profile touched is itself a platform-wide grant |
| A new security control, not just documentation | `wouldRemoveLastPlatformAdministrator` (router.ts) rejects (409) any role change/revoke that would leave the system with zero Platform Administrators — verified with real scenarios in both directions (the only remaining grant → 409; the same action once a second grant exists → 200) |
| A deliberate scope cut, evaluated not assumed | No `organizationId`/`role` filter on user search — the identical repository-isolation reasoning `OrganizationSearchOptions`'s missing `riskLevel` filter already established; would require a cross-repository join no repository in this codebase supports anywhere. No Organization Details "Members" panel as a consequence — real, deferred follow-up, not built this pass |
| Enterprise User Directory — the search/table module | `features/admin/users/UserWorkspacePage.tsx` — server-backed name/email search, sortable columns, pagination. Deliberately smaller than every sibling Workspace: no `FilterPanel`/saved filters/column toggle (nothing categorical to filter by, nothing to hide) and no create-form (identity comes only from a real sign-in) |
| User Details — the record module | `features/admin/users/UserDetailPage.tsx` — Identity (email/verified/id), `RoleAssignmentPanel` (grant/change/revoke, every control a direct server round-trip, never optimistic-only, mirroring `OrganizationAdministrationPanel`'s established pattern), `UserRelationshipsPanel` (real assigned leads via `LeadSearchOptions.assignedTo` and real created assessments via the new `createdBy` filter, deep-linked into the existing Lead Intelligence/Assessment Center modules), and a real audit timeline sourced server-side from `GET /api/audit?entityType=user&entityId=...` |
| Shared components — reused, not duplicated | `RiskBadge`/`StatusBadge` imported directly from the leads feature for Relationships' list items, same precedent as EAP-3/EAP-4's own reuse. `RoleBadge` is genuinely new (owner/admin/member, plus a distinct "Platform Administrator" label) — named `userRole`, not `role`, as its prop, since a plain `role` prop is indistinguishable from the DOM's own ARIA attribute to both a static linter and a future reader |
| **A real environmental gap — found via this phase's own real-browser verification requirement, fixed** | This session's fresh container had never had `packages/platform/.dev.vars` provisioned, so `AUTH_SECRET` was unset and every caller — even one with a genuinely valid session cookie — silently resolved as anonymous. Looked at first like a browser/cookie/proxy issue; reproduced deliberately against already-shipped EAP-4 code before concluding anything about new code; root-caused with a direct `curl` request bypassing the browser entirely; fixed by following `OPERATIONAL_RUNBOOK.md`'s own already-documented setup step. Full account: `DECISION_LOG.md`'s EAP-5 entry |
| Real operational verification | See the "Fresh verification evidence" table above — real User Workspace/Details browser verification (including a real grant → change → revoke round trip), a direct D1 query confirming real audit persistence, and a new committed Playwright suite (`apps/web/e2e/user-management.spec.ts`, 4 tests, including this codebase's first real `DELETE` request) |
| Test suite | 713 tests passing (up from 609) — `+39` `@titan/web` (users feature components/hooks/pages, `assessmentApi.ts`'s first-ever test file, nav/routing), `+65` `@titan/platform` (repository/router/worker tests) |

**Explicitly not built this phase, deferred to later EAP phases or named as near-term follow-up, not forgotten:** an Audit Center, an Operations Center, a Customer Portal, or any commercial/billing/licensing surface. No self-service creation of a user's own identity. No Organization Details "Members" panel (real, cross-repository-join-limited follow-up). No real assignee picker wired into Lead Lifecycle's already-shipped UI (the user directory it needed didn't exist before this phase; it does now, but wiring the picker itself wasn't this phase's own scope). No organization-scoped delegation of any cross-organization list/search endpoint — every gate stays Platform-Administrator-only. No deployment or infrastructure changes — this phase remains entirely local-only, same constraint as everything before it. See `ROADMAP.md`'s recommended next EAP phase (the Audit Center).

## EAP-6 Phase 6: Enterprise Audit Center

Scoped from an "EAP-6 — Enterprise Audit Center" prompt describing prior work as complete through security review and real-browser verification, stopped only mid-documentation. Repository evidence contradicted this at the start of this phase (not assumed either way): `git status`/`git log`/`git reflog`/`git stash list` and a direct file search for every artifact the prior narrative named showed **none of it existed anywhere in the repository** — no branch, no dangling commit, no stash, across this branch, `main`, or any of the 20+ other remote branches. The third instance of this log's now-recurring "described work was never committed" pattern (see the Security Release Blocker Sprint and EAP-3 entries above) — proceeded as new implementation from the verified-empty baseline, using the prior narrative only as an already-reasoned design reference to evaluate independently, per this program's own standing discipline. Full reasoning: `DECISION_LOG.md`'s EAP-6 entry. This phase: the sixth complete enterprise business module — a consolidated, cross-entity Audit Workspace, Audit Details, Investigation View, and CSV/JSON export over `audit_events`, the exact same table every prior module already writes to — built entirely on EAP-1's shell/auth/RBAC/repository pattern and EAP-2 through EAP-5's design-system primitives, none of which were touched or replaced.

| What | Evidence |
|---|---|
| Data model — no new table, no new column, one new repository method | `AuditRepository` gains `search(options: AuditSearchOptions): Promise<AuditSearchResult>` alongside its pre-existing `record`/`list` (unchanged — the Dashboard and all four per-entity audit panels keep depending on `list()` exactly as before). `AuditSortField` has exactly one member (`"createdAt"`) — no other field on this table has ever had a real sort use case |
| Two new endpoints | `GET /api/audit/search` (filtered/sorted/paginated) and `GET /api/audit/export` (identical filters, unpaginated, capped at 10,000 rows, real CSV/JSON file download via `Content-Disposition`) — both Platform-Administrator-only via the same `requirePlatformAdministrator` gate every other cross-organization route uses, sharing one `parseAuditSearchOptions` filter parser |
| A risk investigated and verified not present, not assumed | CSV/formula injection (CWE-1236) on the export's `metadata` column — every exported `metadata` cell is `JSON.stringify`'d first, so it always begins with `{` (or is empty); confirmed with a test proving this real, currently-true property rather than adding a defensive escape for a scenario this codebase's own principles rule out fabricating a fix for |
| A real cross-origin CORS gap, found only by real Playwright/Chromium E2E verification | `http/cors.ts` never set `Access-Control-Expose-Headers`, so a real browser's cross-origin `fetch()` silently hid the `Content-Disposition` header from JS — every export "worked" under casual inspection (a file did download), just under a browser-guessed fallback filename instead of the server's real one. The identical class of finding EAP-2's missing `Access-Control-Allow-Methods` entry for `PATCH` already was. Fixed with a new router-level regression test asserting the header going forward |
| Shared components — consolidated, not duplicated a sixth time | `auditActionLabels.ts` replaces four near-identical private `ACTION_LABELS` maps the Lead/Assessment/Organization/User audit panels each already carried (a genuine fifth consumer justified the extraction — the four panels' own behavior/tests are otherwise unchanged). `AuditEntityBadge` (three real consumers: Workspace table, Investigation grouping, event detail panel) |
| Enterprise Audit Workspace — the search/table module | `features/admin/audit/AuditWorkspacePage.tsx` — server-backed search/filter (entity type, action, actor id, organization id, date range)/sort/pagination, saved filters and column preferences (the same `localStorage`-backed pattern every sibling Workspace uses), a Table/Investigation view toggle, and CSV/JSON export |
| Audit Details — an inline panel, not a new route | `AuditEventDetailPanel.tsx` — actor/action/timestamp/entity/organization/raw metadata over an already-loaded record; "related events" reuses the pre-existing `GET /api/audit?entityType=...&entityId=...` endpoint rather than a new one; states plainly, not silently, that this event log has no request-context or result-status columns |
| Investigation View — grouping, not a correlation engine | `AuditInvestigationView.tsx` — groups the currently-loaded page of search results by entity/actor/organization, reusing the shared `Timeline` component; deliberately doesn't fetch every matching row across every page |
| Real operational verification | Direct D1 query confirming real audit event persistence with expected `action`/`entity_type`/`entity_id`/`metadata_json` values, and a new committed Playwright suite (`apps/web/e2e/audit-center.spec.ts`, 5 tests, including the real downloaded-CSV-content assertion that caught the CORS gap above) |
| Test suite | 769 tests passing (up from 713) — `+27` `@titan/web` (audit feature components/hooks/API tests, nav/routing), `+29` `@titan/platform` (repository/router tests) |

**Explicitly not built this phase, deferred to later EAP phases or named as near-term follow-up, not forgotten:** an Operations Center, a Customer Portal, or any commercial/billing/licensing surface. Investigation notes/annotations (no backing column exists on `audit_events` — a real schema addition, not built speculatively ahead of one). A truncation signal on an Audit Export beyond its 10,000-row cap (the cap itself is the real control). No organization-scoped delegation of `GET /api/audit/search`/`GET /api/audit/export` — both stay Platform-Administrator-only, the same line every prior phase's own cross-organization endpoints already drew. No deployment or infrastructure changes — this phase remains entirely local-only, same constraint as everything before it. See `ROADMAP.md`'s recommended next EAP phase (the Operations Center).

## EAP-7 Phase 7: Enterprise Operations Center

Scoped from an "EAP-7 — Enterprise Operations Center" prompt describing every prior module through EAP-6 as verified. Repository evidence confirmed this cleanly this time — `git log`/`git fetch --prune` showed EAP-6's PR (#46) genuinely merged into `main`, and the designated branch, restarted from that fresh tip per this program's own merged-branch protocol, carried zero commits of its own before this phase began; `git reflog`/`git stash list`/a repo-wide search found no prior EAP-7 work anywhere. A genuinely clean continuation, not a repeat of the "described work was never committed" pattern this log has recorded twice before. Full reasoning: `DECISION_LOG.md`'s EAP-7 entry. This phase: the seventh complete enterprise business module — the first to surface operational rather than business state: real per-service reachability, real request/repository-operation telemetry (read back from counters an earlier phase instrumented but nothing ever consumed until now), and a static system overview — built entirely on EAP-1's shell/auth/RBAC/repository pattern and EAP-2 through EAP-6's design-system primitives, none of which were touched or replaced.

| What | Evidence |
|---|---|
| Data model — no new table, no new writer, one interface promoted | Every repository's existing `search()`/`list()` is reused as-is for a real reachability check. `Metrics` (`observability/metrics.ts`) gains `getCounts()`/`getDurations()` on the interface itself — the concrete in-memory implementation already had both, just never promised by the contract |
| One new endpoint | `GET /api/operations/summary` — Platform-Administrator-only (same `requirePlatformAdministrator` gate every cross-organization route uses), composing per-service status, real metrics readback, and a static overview in one read |
| A real reachability check that doubles as an activity count | `checkSearchableService`/`checkUserProfilesService` (`router.ts`) run a genuine `search({page:1,pageSize:1})`/`list()` against each repository, timed through the same `withOperationTiming` every other repository call already uses — one real query serving both Service Status and Runtime Metrics' activity totals, not two separate reads |
| An honest, verified "not applicable" | Background Operations panel states plainly that no background job/queue/scheduled-task infrastructure exists — confirmed by reading `wrangler.toml` directly (no `[[queues]]`/`durable_objects`/`[triggers]` block) rather than building a fabricated queue view |
| A deliberate, evaluated exception to an existing rule | `ServiceStatus.error` returns a caught exception's real message to the caller, departing from `handleRequest`'s own generic-error-to-client convention — accepted because only a Platform Administrator can reach this endpoint and showing the real cause is its actual purpose; every failure is still logged server-side too. Full reasoning: `SECURITY_GUIDE.md`'s "Authorization model" |
| Enterprise Operations Workspace | `features/admin/operations/OperationsWorkspacePage.tsx` — Platform Health, Readiness, Service Status, Runtime Metrics, Background Operations, System Overview, six independently-loading `Panel`s reusing `DataTable`/`Badge`/`EmptyState`/`LoadingSkeleton`/`Alert` |
| Real operational verification | A new committed Playwright suite (`apps/web/e2e/operations-center.spec.ts`, 3 tests) including one that sends a real request through the running Worker via `page.request.get` and then asserts the Runtime Metrics table reflects that exact real request — not a number the test fabricates and hands back to itself |
| Test suite | 789 tests passing (up from 769) — `+12` `@titan/web` (operations feature components, nav/routing), `+8` `@titan/platform` (a new `metrics.test.ts` plus `GET /api/operations/summary` router tests) |

**Explicitly not built this phase, deferred to later EAP phases or named as near-term follow-up, not forgotten:** a Reporting & Analytics module, a Customer Portal, or any commercial/billing/licensing surface. A dynamically-read build hash/timestamp (no CI step injects one anywhere in this repository — `SystemOverview.version`/`.environment` are real but manually-maintained constants instead). A cross-isolate/durable metrics backend (`Metrics` stays in-memory and per-isolate, needing Workers Analytics Engine and a deployed Cloudflare account this project has never had). No organization-scoped delegation of `GET /api/operations/summary` — stays Platform-Administrator-only, the same line every prior cross-organization endpoint already drew. No deployment or infrastructure changes — this phase remains entirely local-only, same constraint as everything before it. See `ROADMAP.md`'s recommended next EAP phase (Reporting & Analytics).

## EAP-8 Phase 8: Enterprise Reporting & Analytics

Scoped from an "EAP-8 — Enterprise Reporting & Analytics" resume prompt describing every prior module through EAP-7 as already merged and EAP-8 itself as fully implemented, verified, and committed, stopped only mid-documentation. Repository evidence confirmed the first half cleanly (EAP-1 through EAP-7 genuinely merged into `main` through PR #48) and confirmed the second half too, with one real correction: the checkpoint commit PR #48 merged (`0fc4a18`) was real and matched the resume prompt's account, but two further commits it also described (nav/routing wiring, the Playwright e2e spec) had been pushed to the still-open `claude/session-179knd` branch and never merged — recovered by cherry-picking them onto this designated branch (verified conflict-free via their real merge-base) rather than rebuilding either from scratch. Full reasoning, including why this is a materially different (and better) outcome than the three prior "described work was never committed" incidents this log already records: `DECISION_LOG.md`'s EAP-8 entry. This phase: the eighth business module — the first that is pure composition over every prior module's own data rather than a new entity of its own: a live Executive Dashboard, Business Reports, and an Analytics panel with trend visualization and CSV/JSON export — built entirely on EAP-1's shell/auth/RBAC/repository pattern and EAP-2 through EAP-7's design-system primitives, none of which were touched or replaced.

| What | Evidence |
|---|---|
| Data model — no schema change, no new repository method | Every repository (`Lead`/`Assessment`/`Organization`/`Audit`/`User`/`UserProfile`) keeps its existing `list()`/`search()` shape unchanged. One real addition: `auth/rbac.ts`'s `countPlatformAdministrators(profiles)`, a system-wide sibling to the existing per-caller `isPlatformAdministrator`, sharing its same predicate |
| Three new endpoints | `GET /api/reports/summary` (the `ExecutiveSummary` — organization/lead/assessment/identity/audit counts and breakdowns, composed via `Promise.all`-parallelized repository reads, each timed through `withOperationTiming`), `GET /api/reports/trends` (a `TrendSeries` — one time-bucketed count per UTC day, for one of leads/assessments/organizations/audit/identity, over a caller-chosen 1–90 day window), `GET /api/reports/export` (the same `ExecutiveSummary` as a real CSV/JSON file download via `Content-Disposition`) — all three Platform-Administrator-only via the same `requirePlatformAdministrator` gate every other cross-organization route uses |
| Not what `ROADMAP.md`'s own prior note said this would be — corrected, not silently reinterpreted | EAP-7's "Recommended next EAP phase" note described this as reading from the pre-existing, never-written-to `reports` table (migrations/0006). That table tracks a different concept (generated PDF report artifacts, Roadmap Module H, still blocked on an undecided email/storage pipeline) — this module instead composes live repository reads (Roadmap Module 10, analytics over already-real data). The `reports` table remains at zero rows, untouched by this phase. Full reasoning: `DECISION_LOG.md`'s EAP-8 entry, `ARCHITECTURE.md`'s EAP-8 section |
| A deliberately more conservative export than the one it's modeled on | `GET /api/reports/export` contains only aggregate counts/breakdowns — no name, email, company, or other individual-record field anywhere in `ExecutiveSummary` — verified directly against `toExecutiveSummaryCsv`'s own row list. Needs no row cap the way `GET /api/audit/export`'s 10,000-row limit does; reuses the same blanket `Access-Control-Expose-Headers: Content-Disposition` CORS policy EAP-6 already established |
| Executive Dashboard / Business Reports — the summary-rendering modules | `features/admin/reporting/ExecutiveSummaryPanel.tsx` (real KPI `MetricCard`s, an honest "—"/"Not configured" hint instead of a fabricated zero for an unconfigured `organizations`/`identity` report) and `BusinessReportsPanel.tsx` (every breakdown a real `DataTable` over a server-computed count: leads by status/priority/risk level, assessments by risk level/framework, organizations by status when configured, top audit actions) |
| Analytics — the trend-visualization module | `features/admin/reporting/AnalyticsPanel.tsx` — an entity/date-range selector (`localStorage`-backed, per-browser, the same saved-view convention every sibling Workspace uses), rendering the new `TrendSparkline` design-system component alongside a real, accessible `DataTable` of the identical per-day counts (so a screen-reader user is never left with the decorative shape alone) |
| New design-system primitive | `TrendSparkline` — a minimal, dependency-free inline SVG polyline, not a charting library dependency; a real, data-derived `aria-label` (e.g. "Lead trend: 12 total over 30 days, 3 on the most recent day"), with its own `axe-core` check |
| Real operational verification | Real `wrangler dev` + real local D1, freshly provisioned this session (`.dev.vars`/`AUTH_SECRET`, migrations, seed — the same first-time-in-a-fresh-container step EAP-5's entry already documents) — and a new committed Playwright suite (`apps/web/e2e/reporting-center.spec.ts`, 3 tests: unauthenticated redirect, non-admin honest-forbidden messaging, and a full Platform Administrator flow whose Executive Dashboard/Business Reports/Analytics all reflect a real, freshly seeded lead plus a real downloaded-CSV-content assertion), run alongside the existing eight specs (31 E2E tests total, up from 28), clean across two consecutive full-suite runs this pass (4.8 and 4.7 minutes), zero flakes |
| Test suite | 823 tests passing (up from 789) — `+18` `@titan/platform` (16 new router tests across three `describe` blocks for the three new endpoints, `+2` `countPlatformAdministrators` in `rbac.test.ts`), `+12` `@titan/web` (10 across the four new reporting feature-panel test files, `+1` `navItems.test.ts`, `+1` `App.test.tsx`), `+4` `@titan/design-system` (`TrendSparkline.test.tsx`), `@titan/assessment-core` unchanged at 25 |

**Explicitly not built this phase, deferred or named as near-term follow-up, not forgotten:** a Customer Portal or any commercial/billing/licensing surface. A materialized/persisted report record via the pre-existing `reports` table (a different roadmap item — server-side PDF generation/storage/email delivery — still blocked on an undecided email/storage pipeline, unrelated to this phase's scope). Organization-scoped delegation of any of the three new endpoints — all three stay Platform-Administrator-only, the same line every prior cross-organization endpoint already drew. A real assignee picker in Lead Lifecycle, an Organization Details "Members" panel, and a truncation signal on Audit Export beyond its 10,000-row cap all remain the same real, named near-term follow-ups `ROADMAP.md` has carried since EAP-5/EAP-6 — none of them was in this phase's own scope either, and none regressed. No deployment or infrastructure changes — this phase remains entirely local-only, same constraint as everything before it.

## CPP-1: Enterprise Customer Portal

Scoped from a "CPP-1 — Enterprise Customer Portal" master prompt asking for a customer-facing application layered on the already-verified EAP-1–EAP-8 platform: reusing its existing services/repositories/security model, never redesigning the administration platform, never duplicating admin functionality, and completing this phase before any Commercial Platform/Cloudflare Production/Release Candidate/General Availability work begins. Unlike several prior EAP phases, this pass ran in one continuous session with no mid-work container reset, so there was no "verify a resume prompt's claims against real git state" step — this section instead records what was built and how it was verified, starting from the already-clean, already-pushed EAP-8 baseline (`831bb54`). This phase: the first business module built for a customer rather than a Platform Administrator — a real, separate `/portal` shell (Dashboard, Assessments, Reports, Support, Account) for a real Organization Member/Admin/Owner, reusing EAP-1's session/RBAC layer and EAP-2/EAP-3/EAP-6/EAP-8's own repository reads, none of which were touched or replaced.

| What | Evidence |
|---|---|
| Data model — one new table, everything else reused unchanged | `SupportRequestRepository` (`save`/`listByUser`, in-memory + D1, contract-tested against real SQLite the same way every repository since Stage 4 has been) backed by `migrations/0010_support_requests.sql` — the one entity with no existing analogue. Every other portal route calls the exact same `AssessmentRepository.search()`/`OrganizationRepository.findById()`/`AuditRepository.search()` methods the admin Assessment Workspace/Organization Details/Audit Center already call, with `organizationId` forced to the caller's own rather than accepted as an optional filter |
| Seven new endpoints, one new authorization primitive | `GET /api/portal/organization`, `GET /api/portal/assessments`, `GET /api/portal/reports/summary`, `GET /api/portal/reports/export`, `GET /api/portal/activity`, `GET`/`POST /api/portal/support` — every one gated by `resolvePortalOrganizationId` (new this phase), which derives the caller's organization id from their own `UserProfileRecord[]` server-side and never from client input. `POST /api/portal/support` additionally carries the same `isTrustedOrigin` CSRF check every other authenticated write in this codebase has, and deliberately no rate limit, matching the standing convention that rate limiting exists only for anonymous POST routes |
| Zero changes to any existing admin route — verified, not assumed | `git diff` against the pre-CPP-1 baseline shows every touched pre-existing file (`router.ts`, `router.test.ts`, `worker.ts`, `index.ts`, `repositories/types.ts`, `App.tsx`) as pure addition: 948 insertions, 0 deletions |
| Assessment Detail reuses the existing route, no new one added | `GET /api/assessments/:id` (EAP-2/EAP-3) already delegates to `requireAssessmentAccess` — "a Platform Administrator, or a member of the assessment's own organization" — exactly the access a portal customer needs. `usePortalAssessmentDetail.ts` calls the admin feature folder's own `fetchAssessment` directly; a 403 renders an honest "This assessment is not available to your organization." message |
| A deliberately smaller sibling to EAP-8's `ExecutiveSummary`, not the same type reused | `PortalComplianceSummary` composes only this organization's own assessments (by risk level, by framework, latest date) — most of `ExecutiveSummary`'s other sections (leads, identity, cross-tenant audit rollups) have no honest single-organization form in this data model, full reasoning in `DECISION_LOG.md`'s CPP-1 entry |
| Customer Dashboard / Assessments / Reports / Support / Account — the five portal modules | `features/portal/dashboard/` (Organization Overview, Compliance Summary via the new `AssessmentSummaryCard`, Recent Activity reusing `audit_events`), `features/portal/assessments/` (a read-only `DataTable` plus Assessment Detail), `features/portal/reports/` (the compliance summary plus real CSV/JSON export, mirroring `AuditWorkspacePage.tsx`/`ReportingWorkspacePage.tsx`'s own export-button pattern), `features/portal/support/` (submission form plus own-history `Timeline`), `features/portal/account/` (`PortalAccountPage`/`PortalAccountContent`, the same `*Content`-prop-driven testability convention every sibling admin detail page follows) |
| New shared component | `AssessmentSummaryCard` (`features/portal/`) — two real consumers (Dashboard, Reports), composing the existing `RiskBadge`/`MetricCard`. Deliberately not added to `@titan/design-system`, preserving that package's standing zero-`@titan/*`-dependency leaf constraint |
| Real operational verification | Real `wrangler dev` + real local D1, migrations (incl. the new `0010_support_requests.sql`) applied automatically via `playwright.config.ts`'s own `webServer` step — and a new committed Playwright suite (`apps/web/e2e/customer-portal.spec.ts`, 4 tests: unauthenticated redirect, an honest "no organization membership" message with the nav hidden entirely, a real organization member's Dashboard/Assessments/Reports — including a real CSV download — reflecting only their own organization's data plus the core cross-tenant-isolation proof, and a real support-request submit-and-view-history flow), run alongside the existing nine specs (35 E2E tests total, up from 31), clean across two consecutive full-suite runs this pass (5.6 and 5.4 minutes), zero flakes |
| Test suite | 881 tests passing (up from 823) — `+22` `@titan/platform` (461, up from 439: 22 new router tests across six `describe` blocks, plus 12 new repository contract tests split across memory/D1 implementations), `+24` `@titan/web` (333, up from 309: component tests across 9 new test files), `@titan/assessment-core` (25) and `@titan/design-system` (62) unchanged |
| Full workspace verification | `npm run ci` (typecheck → lint → format → build → test) passes clean across all four packages, fresh this pass, confirmed via explicit exit-code check and a full log review of every stage (not just the tail of the output) |

**Explicitly not built this phase, deferred or named as near-term follow-up, not forgotten:** an organization switcher for a caller with more than one real organization membership (`resolvePortalOrganizationId` resolves to the first found, a named scope limit). A support-request ticketing/resolution workflow — no admin-side endpoint reads or updates `support_requests` anywhere in this codebase; `status` starts and stays `"open"`. Password change, notification preferences, and multi-session management on the Account page — no credentials provider, settings/notification service, or `SessionRepository` beyond Auth.js's own single-session-token model exists to back any of them. Enterprise APIs (Roadmap Module 14) and bookings (Roadmap Module 13's other half) — both explicitly out of CPP-1's own brief. Organization-scoped delegation of any existing admin route (`GET /api/leads`, `GET /api/organizations/search`, etc.) — none were touched; that remains the same, separate, still-open gap `SECURITY_GUIDE.md` has named since EAP-1. No Commercial Platform, Billing, Subscriptions, Licensing, Payments, Cloudflare Production, Release Candidate, or General Availability work — per this program's own explicit instruction. No deployment or infrastructure changes — this phase remains entirely local-only, same constraint as everything before it.

## COM-1: Enterprise Commercial Platform

Scoped from a "COM-1 — Enterprise Commercial Platform" master prompt asking for the monetization/entitlement layer on top of the already-verified EAP-1–EAP-8 platform and CPP-1's Customer Portal: reusing their existing services/repositories/security model, never redesigning any verified capability, never duplicating existing customer or admin functionality, implementing commercial capabilities only — explicitly not payment gateway logic — and completing this phase before any Cloudflare Production/Release Candidate/General Availability work begins. This pass ran in one continuous session with no mid-work container reset, starting from the already-clean, already-pushed CPP-1 baseline (`ab416f4`). This phase: the ninth business module and second built on CPP-1's Portal shell — a real Subscription/License/Entitlement model surfaced through both a customer-facing `/portal/subscription` self-service page and an admin-facing `/admin/commercial` Workspace + Detail pair, reusing EAP-1's session/RBAC layer, CPP-1's `resolvePortalOrganizationId`, and EAP-5's `UserProfileRepository`, none of which were touched or replaced.

| What | Evidence |
|---|---|
| Data model — two new tables, one new repository method on an existing one | `SubscriptionRepository` and `LicenseRepository` (`migrations/0011_subscriptions.sql`/`0012_licenses.sql`), each in-memory + D1, contract-tested against real SQLite the same way every repository since Stage 4 has been. `UserProfileRepository` gains `findByOrganizationId` (memory + D1 + 3 new contract cases) — the one extension to an existing repository, used to compute seat usage as a live count rather than a stored column. The Plan catalog (`commercial/planCatalog.ts`) is deliberately **not** a database table — a hardcoded, code-reviewed array of three plans (Starter/Professional/Enterprise), the same "content ships with the app" precedent `@titan/assessment-core`'s `dpdpV1` question bank already established |
| Nine new endpoints, one shared state machine | `GET /api/commercial/plans`, `GET`/`POST`/`PATCH /api/portal/commercial/subscription`, `GET /api/commercial/subscriptions/search`, `GET`/`PATCH /api/commercial/subscriptions/:id`, `GET /api/commercial/licenses/search` — the portal routes gated by CPP-1's own `resolvePortalOrganizationId` (unchanged, reused exactly as every existing portal route already calls it), the admin routes by the same `requirePlatformAdministrator` gate every cross-organization route uses. Both `PATCH` routes call one shared `applySubscriptionPatch` (router.ts) that diffs before/after state to derive the correct upgrade/downgrade/cancel/renew audit event and keeps the linked license's status in lockstep — not two independent implementations of the same state machine |
| Zero changes to any existing route — verified, not assumed | `git diff --stat` against the pre-COM-1 baseline shows every touched pre-existing file (`router.ts`, `router.test.ts`, `worker.ts`, `index.ts`, `repositories/types.ts`, the three `userProfileRepository.*` files, `App.tsx`, `navItems.ts`, `portalNavItems.ts`/`.test.ts`) as pure addition: 1073 insertions, 0 deletions |
| A deliberate authorization asymmetry, not a bug | `isSelfServicePlan(plan)` (`plan.trialDays > 0`) gates which plans the customer-facing `PATCH` accepts but not the admin-facing one — a Platform Administrator can assign any plan, including sales-assisted ones like Enterprise, to any organization; a customer can only self-serve between plans that actually offer a trial. Both routes still require the identical underlying authorization gate; only the business-rule validation after it differs. Full reasoning: `DECISION_LOG.md`'s COM-1 entry |
| Entitlements — computed for display, not enforced anywhere | `resolveEntitlements(plan, subscription)` (`commercial/entitlements.ts`) is a pure function called only inside `getPortalCommercialSummary`, to render the Dashboard's Entitlements panel. Nothing in this codebase gates a request on its output — wiring it into an existing EAP/CPP-1 route would be extending the Assessment/Reporting Engine or the Customer Portal's authorization model, both explicitly out of this phase's scope |
| Commercial Dashboard / Commercial Workspace / Subscription Detail — the three commercial modules | `features/portal/subscription/PortalSubscriptionPage.tsx` (Subscription Overview, Current Plan, License Summary via `SeatUsageMeter`, Entitlements via `EntitlementBadge`, renewal/trial status, self-service upgrade/downgrade/cancel), `features/admin/commercial/CommercialWorkspacePage.tsx` (server-backed subscription search/filter/sort/pagination), `features/admin/commercial/CommercialSubscriptionDetailPage.tsx` (full subscription detail, admin plan/status reassignment, real seat usage, linked license) |
| Six new shared components, every one with a real second consumer | `PlanCard`, `LicenseCard`, `SeatUsageMeter`, `EntitlementBadge`, `SubscriptionStatusBadge` — each rendered by both the portal Subscription page and the admin Commercial Workspace/Detail pages, the same reuse discipline `RiskBadge`/`AssessmentResultsPanel` already established across the admin/portal boundary in CPP-1 |
| **A real Playwright flake — found only by real-browser E2E verification, fixed in the test, not the app** | `commercial-platform.spec.ts`'s admin flow initially used `page.waitForURL()` with a glob pattern after clicking from the Commercial Workspace's search results into a subscription's Detail page — timed out unreliably even though the navigation had genuinely succeeded, because glob-pattern URL matching against client-side React Router navigation isn't reliable in this stack. Fixed by asserting on the resulting page's actual visible content instead, the convention every other spec in this suite already followed; no other spec had ever hit this because none had previously asserted on a URL immediately after a client-side link click |
| Real operational verification | Real `wrangler dev` + real local D1, migrations (incl. the two new COM-1 migrations) applied automatically via `playwright.config.ts`'s own `webServer` step — and a new committed Playwright suite (`apps/web/e2e/commercial-platform.spec.ts`, 4 tests: a real organization member starting a self-service trial subscription through the Portal's own Plan Selection UI and upgrading it, both persisting across a real page reload, plus a Platform Administrator's Commercial Workspace search landing on that same real subscription's Detail page with an admin-only plan reassignment reflected immediately), run alongside the existing ten specs (39 E2E tests total, up from 35), clean across two consecutive full-suite runs this pass, zero flakes |
| Test suite | 1011 tests passing (up from 881) — `@titan/platform`: 573 (up from 461, +112: new router tests across 8 new `describe` blocks for the plan/subscription/license endpoints, new `subscriptionRepository`/`licenseRepository` contract test suites run against both memory and D1 implementations, `planCatalog.test.ts`/`entitlements.test.ts` unit tests, and 3 new `userProfileRepository.findByOrganizationId` contract cases); `@titan/web`: 351 (up from 333, +18: component tests for the six new shared commercial components, the admin Commercial Workspace/Detail pages, and the portal Subscription page); `@titan/assessment-core` (25) and `@titan/design-system` (62) unchanged |
| Full workspace verification | `npm run ci` (typecheck → lint → format → build → test) passes clean across all four packages, verified twice this pass via explicit exit-code check and a full log review of every stage (not just the tail of the output) — once mid-implementation, once as a final post-E2E checkpoint |

**Explicitly not built this phase, deferred or named as near-term follow-up, not forgotten:** Payment Gateway Processing, PCI card storage, Accounting, Tax Engines, a Marketplace, Affiliate Programs, CRM, ERP integration — all explicitly out of this phase's own brief. Entitlement *enforcement* on any existing EAP/CPP-1 route (computed today, not consulted by any request-gating code). A real Billing Integration Layer adapter for any specific payment provider (the subscription model is provider-agnostic by design — no payment amount/currency/invoice/card/token field exists anywhere in `subscriptions`/`licenses` or their TypeScript types). Multiple subscriptions per organization (`SubscriptionRepository` assumes one active subscription per organization, matching `resolvePortalOrganizationId`'s own single-organization-per-caller scope limit). Dynamic Plan catalog CRUD (the three plans are a hardcoded array, not a database table — see above). A dedicated admin-facing License Inventory page (license data is fully visible through Subscription Detail's own linked-license panel; a separate page has no distinct real consumer yet). Organization-scoped delegation of any pre-existing admin route — none were touched. No Cloudflare Production, Release Candidate, or General Availability work — per this program's own explicit instruction. No deployment or infrastructure changes — this phase remains entirely local-only, same constraint as everything before it.

## What's next

Both of ERP-1's named release blockers (`GET /api/leads` authentication, server-side risk-score recomputation) remain fixed and verified — see the Security Release Blocker Sprint section above. With EAP-8, the admin shell had eight real modules (Dashboard, Lead Intelligence, Assessment Center, Organization Management, Enterprise Identity & User Management, Enterprise Audit Center, Enterprise Operations Center, Enterprise Reporting & Analytics) — every module the original EAP-1 brief named, plus one more the roadmap itself recommended once all seven were real. CPP-1 added the first customer-facing module (the Enterprise Customer Portal) on top of that foundation, reusing its services and security model rather than redesigning any of it. **COM-1 has now added the commercial/entitlement layer (Subscriptions, Licenses, Entitlements) on top of both, spanning a new admin Commercial Workspace and a new portal Subscription page — deliberately stopping short of any payment gateway integration.** Three smaller, real, still-unblocked admin-side follow-ups remain worth sequencing alongside whatever comes next, unchanged by CPP-1 or COM-1: a real assignee picker in Lead Lifecycle (EAP-5's user directory removed the reason this was deferred since EAP-2), an Organization Details "Members" panel (the reciprocal view of User Details' own Role Assignment), and a truncation signal on Audit Export beyond its current cap (EAP-6). CPP-1's own near-term follow-ups (an organization switcher, a support-request ticketing workflow, Account page password/preferences/session management) and COM-1's own (a real Billing Integration Layer adapter, entitlement enforcement, a dedicated License Inventory page) are both named above, not forgotten. Server-side PDF report generation/storage/email delivery (Roadmap Module H, the `reports` table) remains genuinely blocked on the still-undecided email and object-storage providers, unaffected by either EAP-8's or COM-1's own, different reporting/commercial layers. A release-classification re-evaluation (the next ERP-style independent audit) is still the natural way to confirm whether this admin shell plus a real customer portal and commercial layer is sufficient to advance past Internal Alpha, or whether Gate C (Operations, no deployment) and the payment-gateway half of Gate E (Commercial — entitlement modeling now exists, real billing still doesn't) still control the classification regardless — no new evidence exists on either gate this pass. Per this program's own explicit instruction, COM-1 is complete before any Cloudflare Production, Release Candidate, or General Availability work begins.
