# Project Titan — CyberDudeBivash Compliance Platform

**Status: Phase 1 (Platform Foundation) complete for its original scope. Phase 2 (DPDP Platform Integration) through Stage 4, an RC1 hardening pass, an independent ERP-1 release audit, a Security Release Blocker Sprint, EAP-1 Phase 1 (Admin Application Foundation), EAP-2 Phase 2 (Lead Intelligence Platform), EAP-3 (Enterprise Assessment Center), EAP-4 (Enterprise Organization Management Platform), EAP-5 (Enterprise Identity & User Management), and EAP-6 (Enterprise Audit Center):** the DPDP scanner is a real, tested module in Titan, backed by a real Cloudflare Workers + D1 stack — repositories, API, Auth.js foundation, audit logging, CSRF protection, route-scoped CSP, a threat model + ASVS review, and seven committed Playwright E2E suites are all built and verified against a real local D1 instance via `wrangler dev` (never deployed — no Cloudflare account/credentials in any environment this project has run in). The frontend calls the real API instead of `localStorage`. **ERP-1's independent audit found one HIGH-severity gap, `GET /api/leads` unauthenticated PII exposure, plus client-submitted risk scores not being recomputed server-side — both are now fixed and verified (Security Release Blocker Sprint: `GET /api/leads` requires a Platform Administrator; `POST /api/leads`/`POST /api/assessments` recompute `result` server-side and discard the client's value — `SECURITY_GUIDE.md`, `DECISION_LOG.md`).** Built on that authorization foundation, **EAP-1 Phase 1 adds a real authenticated admin application**: session lifecycle, protected routes, role-aware navigation, a reusable admin UI framework, and one complete module (Dashboard). **EAP-2 Phase 2 adds the first complete enterprise business module on top of that shell: Lead Intelligence** — a real Lead Workspace (server-side search/filter/sort/pagination), Lead Details (risk breakdown, lifecycle management, real audit history), all Platform-Administrator-gated and RBAC-consistent with everything before it (`ROADMAP.md`'s EAP-2 section). **EAP-3 adds the second complete enterprise business module: the Enterprise Assessment Center** — a real Assessment Workspace, Assessment Details (results, severity breakdown, category coverage, question responses), a Compliance Intelligence panel, and lead linkage, read-only end to end (an assessment has no lifecycle to manage), reusing the same shell/auth/RBAC/repository pattern. **EAP-4 adds the third complete enterprise business module: Enterprise Organization Management** — a real Organization Workspace (server-side search/filter/sort/pagination), Organization Details (metadata, Health, Relationships, Administration, audit history), real create/update/archive/restore writes (unlike Assessments, an organization has a real administrative lifecycle), and real deep links into the existing Lead Intelligence and Assessment Center modules rather than duplicating them. **EAP-5 adds the fourth complete enterprise business module: Enterprise Identity & User Management** — a real Enterprise User Directory (server-side search over Auth.js's own `users` table), User Details (Identity, Role Assignment — grant/change/revoke a user's organization or platform-wide role, guarded against ever leaving the system with zero Platform Administrators — Relationships, audit history), closing (for every grant after the first) the standing "no self-service way to grant Platform Administrator" gap. **EAP-6 adds the fifth complete enterprise business module: the Enterprise Audit Center** — a consolidated, cross-entity Audit Workspace, Audit Details, Investigation View, and CSV/JSON export over the exact same `audit_events` table every prior module already writes to, no new writer, no new schema — architected for an Operations Center to plug in as the next phase, not built yet. Customer Portal, commercial readiness, and formal compliance work remain explicitly deferred (`DECISION_LOG.md`). Read in this order:

1. [`PRODUCT_VISION.md`](./PRODUCT_VISION.md) — what this is, who it's for, business model, explicit scope boundaries
2. [`ARCHITECTURE.md`](./ARCHITECTURE.md) — technical design, including which decisions are settled and which are still open
3. [`DECISION_LOG.md`](./DECISION_LOG.md) — chronological record of decisions once actually made, with source and rationale
4. [`ROADMAP.md`](./ROADMAP.md) — phased delivery plan, what "done" means per phase
5. [`PLATFORM_FOUNDATION.md`](./PLATFORM_FOUNDATION.md) — **current implementation status against the Phase 1 brief**, with fresh verification evidence, not claims
6. [`FEATURE_MATRIX.md`](./FEATURE_MATRIX.md) — every implemented feature, one row each, added only once real
7. [`DEVELOPER_GUIDE.md`](./DEVELOPER_GUIDE.md) — how to work in `titan/`
8. [`OPERATIONAL_RUNBOOK.md`](./OPERATIONAL_RUNBOOK.md) — running, verifying, and troubleshooting the Cloudflare backend locally (local-only — nothing here is deployed anywhere)
9. [`SECURITY_GUIDE.md`](./SECURITY_GUIDE.md) — threat model, OWASP ASVS control review, and known/accepted security gaps
10. [`DPDP_ASSESSMENT_FRAMEWORK.md`](./DPDP_ASSESSMENT_FRAMEWORK.md) — draft compliance content — **not legal advice, requires expert review before any real use**

## What actually exists right now

Real code, under `titan/` — a separate npm workspace, fully isolated from the marketing site's build (confirmed: adding it doesn't change the existing site's `verify-dist` result). Four packages: `@titan/design-system` (tokens, app-shell components, `MetricCard`/`Panel`, and 8 EAP-2 enterprise UI primitives — `Badge`/`SearchBar`/`Pagination`/`EmptyState`/`LoadingSkeleton`/`FilterPanel`/`Timeline`/`DataTable`); `@titan/assessment-core` (the DPDP question bank and risk-scoring engine); `@titan/web` (the public assessment flow at `/assessment/dpdp`, and the authenticated admin application at `/admin` — EAP-1 shell + Dashboard, EAP-2 Lead Workspace + Lead Details, EAP-3 Assessment Workspace + Assessment Details, EAP-4 Organization Workspace + Organization Details, EAP-5 User Workspace + User Details, EAP-6 Audit Workspace + Audit Details/Investigation View); and `@titan/platform` — 6 repositories (Lead, Organization, Assessment, UserProfile, User, Audit — `Lead`/`Audit` extended additively for EAP-2, `Assessment`/`Lead` extended additively again for EAP-3, `Organization`/`Lead`/`Assessment` extended additively again for EAP-4 with a real lifecycle: `status`/`industry`/`region`/`tags`/`updatedAt`, `findById`/`update`/`search`, `UserProfile`/`Assessment` extended additively again for EAP-5 — `findById`/`list`/`update`/`remove` on `UserProfile`, this system's first real deletion, plus a `createdBy` filter on `Assessment` search — alongside the brand-new, read-only `User` repository, and `Audit` extended additively again for EAP-6 with a `search` method, no new table), a Worker with 25 route handlers (health/readiness, leads incl. detail/update/search, assessments incl. detail/search, organizations incl. create/detail/update/search, users incl. detail/grant/change/revoke/search, audit incl. search/export, `/api/me`) plus Auth.js's own mounted `/api/auth/*` actions, an Auth.js foundation (D1-adapter sessions, RBAC, org membership, a Platform Administrator role, cross-origin session support), audit logging, CSRF protection, route-scoped security headers, and observability hardening — all applied and verified against a **real local D1 SQLite instance** via `wrangler dev`, with real HTTP requests and direct database checks, not fakes. `@titan/web`'s `/assessment/dpdp` route submits leads through the real Worker API instead of `localStorage`; `/admin` is a real, authenticated application shell with six complete modules (Dashboard, Lead Intelligence, Assessment Center, Organization Management, Enterprise Identity & User Management, Enterprise Audit Center) reading real Worker data. All routes are verified in a real Chromium browser end to end and by seven committed Playwright E2E suites (25 tests, run strictly serially). `npm run typecheck && npm run lint && npm run format && npm run build && npm run test` all pass, fresh — 769/769 tests. Full detail and honest gaps: `PLATFORM_FOUNDATION.md`.

## What's still blocked

Nothing is deployed anywhere — no Cloudflare account/credentials have existed in any environment this project has run in, so "verified" here always means "verified locally," never "verified in production." Payments provider and email provider remain genuinely undecided (`ARCHITECTURE.md`'s "Still open" list) — Auth.js's Email sign-in logs its link instead of sending real mail as a result. An Operations Center, customer portal, and enterprise SSO remain unbuilt — the admin application has a real shell and six modules (Dashboard, Lead Intelligence, Assessment Center, Organization Management, Enterprise Identity & User Management, Enterprise Audit Center), not the full set (`ROADMAP.md`'s recommended next EAP phase, the Operations Center). Lead assignment still only supports "assign to me"/unassign in its own UI — the user directory it needed is now real (EAP-5), but a real assignee picker isn't wired into `LeadLifecyclePanel.tsx` yet, a named near-term follow-up, not a silent gap (`SECURITY_GUIDE.md`). The Assessment Center has no lifecycle/status workflow — an assessment is one immutable row per completed run, not a gap, a deliberate reflection of the real data model (`DECISION_LOG.md`'s EAP-3 entry). The Audit Center's export is capped at 10,000 rows with no truncation signal yet — the cap itself is the real control, a named near-term follow-up (`SECURITY_GUIDE.md`'s known gaps). Both of ERP-1's named findings (`GET /api/leads` authentication, server-side risk-score recomputation) are fixed — see above; the current release-classification gates are Gate C (Operations, nothing deployed anywhere) and Gate E (Commercial, no payments/billing) rather than any known, unfixed security finding.

## Why this is eight documents plus a matrix, not the nine-folder structure originally requested

Each folder in the original brief (`/docs/product`, `/docs/api`, `/docs/security`, `/docs/testing`, `/docs/operations`, `/docs/analytics`, `/docs/growth`) needs real content to justify existing. Right now there's enough real material for product vision, architecture, a decision log, roadmap, implementation status, a feature matrix, and a developer guide — not yet for API docs (no API exists), security baseline (no backend to secure), or a testing guide beyond what `DEVELOPER_GUIDE.md` already covers. New documents get added when the phase that needs them starts, not preemptively.
