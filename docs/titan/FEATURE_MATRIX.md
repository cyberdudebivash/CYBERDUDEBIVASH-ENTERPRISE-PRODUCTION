# Feature Matrix

Every implemented feature under `titan/`, as of this pass. A row only appears here once it's real — built, tested, and passing — not when it's planned. Update in place as features are added; don't let this drift from `PLATFORM_FOUNDATION.md`'s status table.

| Feature | Package | Tests | Status |
|---|---|---|---|
| Design tokens (color, spacing, typography) | `@titan/design-system` | N/A (data only, no logic to test) | ✅ Implemented |
| `Button` (variants, sizes, loading state, disabled state) | `@titan/design-system` | 9 tests, incl. axe a11y check | ✅ Implemented |
| `Alert` (4 semantic variants, dismissible, correct live-region roles) | `@titan/design-system` | 8 tests, incl. axe a11y check | ✅ Implemented |
| App shell layout (header/sidebar/content/footer) | `@titan/web` | Covered via `App.test.tsx` integration tests | ✅ Implemented |
| Skip-to-content link | `@titan/web` | Tested (`App.test.tsx`) | ✅ Implemented |
| Client-side routing | `@titan/web` | Tested (`App.test.tsx`) | ✅ Implemented |
| Sidebar navigation (data-driven) | `@titan/web` | 3 tests | ✅ Implemented |
| 404 / not-found page | `@titan/web` | Covered via `App.test.tsx` | ✅ Implemented |
| Global error boundary | `@titan/web` | 3 tests | ✅ Implemented |
| DPDP v1 question bank — 15 questions, data-driven, typed, versioned | `@titan/assessment-core` | 5 tests | ✅ Implemented |
| Risk-scoring engine — score calculation, risk-level banding, gap breakdown | `@titan/assessment-core` | 20 tests, 100% statement/branch coverage | ✅ Implemented |
| DPDP assessment flow — landing, 15-question flow (real radio/fieldset/progressbar semantics, `<main>` landmark), results, risk meter, upsell tiers, client-side PDF report (lazy-loaded), lead capture (real Worker API submission, real email validation) | `@titan/web` (`features/dpdp-assessment`) | 51 tests across 8 files, incl. axe a11y checks on every component and one full golden-path integration test; verified end-to-end in a real Chromium browser (jsdom + a committed Playwright E2E suite); WCAG AA-verified via a real Lighthouse audit (100/100 accessibility, ERP-1 — jsdom+axe alone missed two real defects this closed, `PLATFORM_FOUNDATION.md`) | ✅ Implemented |
| `LeadRepository` — Repository Pattern, in-memory + D1 implementations, contract-tested against real SQLite (sql.js) | `@titan/platform` | 14 tests | ✅ Implemented |
| `OrganizationRepository` — save/findBySlug/list, in-memory + D1 | `@titan/platform` | 10 tests | ✅ Implemented |
| `AssessmentRepository` — save/findById/list/search (search added EAP-3: filter by framework/riskLevel, substring on id/organizationId/createdBy, sort, paginate), in-memory + D1 | `@titan/platform` | 24 tests (12 shared contract tests × 2 implementations) | ✅ Implemented |
| `UserProfileRepository` — RBAC/org-membership persistence, in-memory + D1 | `@titan/platform` | 10 tests | ✅ Implemented |
| `AuditRepository` — append-only audit event persistence, in-memory + D1 | `@titan/platform` | 10 tests | ✅ Implemented |
| D1 migrations (Auth.js core, organizations, user_profiles, assessments, leads, reports, audit_events — 7 files) | `@titan/platform` | Applied and round-tripped against a **real local D1 SQLite instance** (`wrangler d1 migrations apply --local`) | ✅ Implemented & verified |
| Worker HTTP layer — `GET /health`, `GET /health/ready`, `POST`/`GET /api/leads`, `POST /api/assessments`, `GET /api/assessments/:id` — validation, structured errors, security headers (incl. route-scoped CSP), CORS, CSRF (Origin validation), request correlation, rate-limit hooks (general + auth-specific), authorization gates on `GET /api/leads`/`GET /api/assessments/:id` | `@titan/platform` | 39 tests (router) + 5 (worker, against real SQLite) | ✅ Implemented & verified against real local D1 via `wrangler dev` |
| Auth.js foundation — D1-adapter session persistence, `/api/auth/*` mounted, provider abstraction (Email dev-mode, Google/GitHub wired-but-inactive without credentials), secret rotation, session callback exposing `user.id` (without leaking the raw session row) | `@titan/platform` | 6 (config) + 2 (session) + 5 (D1 adapter contract) tests | ✅ Implemented & verified against real local D1 via `wrangler dev` |
| RBAC + organization-membership + Platform Administrator, authorization-gate helpers (`requireLeadsAccess`, `requireAssessmentAccess`, `requireOrganizationAccess`) | `@titan/platform` | 11 (rbac) + 12 (authorize) tests | ✅ Implemented & wired — gates `GET /api/leads` (Platform Administrator) and `GET /api/assessments/:id` (Platform Administrator or the assessment's own organization) |
| Server-side risk-score recomputation — `POST /api/leads`/`POST /api/assessments` recompute `result` from `answers` via `@titan/assessment-core`, discarding the client-submitted value; unrecognized `framework`/`frameworkVersion` rejected with 400 | `@titan/platform` | Covered in router tests (tampering + unsupported-framework cases) | ✅ Implemented & verified against a real Worker (tampered client result discarded, real recomputed result persisted to real D1) |
| Audit event recording on lead/assessment creation | `@titan/platform` | Covered in router tests | ✅ Implemented & verified against real D1 |
| Structured logging, request correlation, basic metrics hooks (request counts/durations + per-operation repository timing) | `@titan/platform` | Covered in router tests | ✅ Implemented |
| DPDP lead capture — real Worker API submission (no `localStorage`), server error messages surfaced, retry-by-resubmission | `@titan/web` | 7 (LeadCaptureForm) + 14 (leadStore) tests | ✅ Implemented & verified in a real browser against the real Worker |
| Playwright E2E suite — real backend + frontend, full assessment flow to a real, asserted lead submission | `@titan/web` (`e2e/`) | 1 spec, verified passing (18.3s) | ✅ Implemented & verified — not yet wired into CI (`DECISION_LOG.md`) |
| Threat model + OWASP ASVS control review | `docs/titan/SECURITY_GUIDE.md` | N/A (documentation) | ✅ Implemented |
| Cross-origin session support — CORS credentials (`Access-Control-Allow-Credentials`), Auth.js `redirect` callback origin allowlist, CSP `form-action` origin allowlist for Auth.js's own pages (EAP-1) | `@titan/platform` | 6 (redirect callback) + 1 (CSP form-action) tests | ✅ Implemented & verified — real cross-origin sign-in and sign-out confirmed in a real Chromium browser |
| `GET /api/me`, `GET /api/organizations`, `GET /api/assessments` (list), `GET /api/audit` — `/api/me` open to any authenticated caller, the other three Platform-Administrator-only (EAP-1) | `@titan/platform` | 13 (router) + 1 (worker, real D1 end-to-end) + 3 (`requirePlatformAdministrator`) tests | ✅ Implemented & verified against real local D1 via `wrangler dev` |
| Admin Application Shell — `SessionContext`/`useSession`, `RequireAuth` (protected-route redirect to Auth.js's real sign-in page), `AdminLayout` (reuses `Header`/`Sidebar`/`Footer`, adds signed-in email + real sign-out link), `Breadcrumbs`, role-aware navigation (`adminNavItems`, built for pluggability) (EAP-1) | `@titan/web` (`features/admin`) | 5 (SessionContext) + 4 (RequireAuth) + 3 (AdminLayout) + 5 (Breadcrumbs) + 2 (App routing) tests | ✅ Implemented & verified — real sign-in-required redirect and real sign-out confirmed in a real Chromium browser, not just jsdom |
| `MetricCard`, `Panel` — reusable admin UI framework primitives (EAP-1) | `@titan/design-system` | 5 (MetricCard) + 4 (Panel) tests, incl. axe a11y checks | ✅ Implemented |
| Dashboard — the first complete Admin Application module: executive overview, organization/lead/assessment metrics, risk-level breakdown, recent activity, audit summary, platform health/system status; each privileged section loads independently and shows an honest "Platform Administrator role required" state (never fabricated data) for a non-admin (EAP-1) | `@titan/web` (`features/admin/dashboard`) | 3 tests (`DashboardPage.test.tsx`) | ✅ Implemented & verified against real D1-backed data in a real browser |
| Playwright E2E — admin shell + Dashboard: unauthenticated redirect to the real sign-in page, full Platform Administrator flow (real seeded data, real sign-out ending the session), non-admin honest-forbidden messaging (EAP-1) | `@titan/web` (`e2e/admin-dashboard.spec.ts`) | 1 spec, 3 tests, verified passing | ✅ Implemented & verified — found and confirmed the fix for a real CSP `form-action`/redirect bug (`DECISION_LOG.md`); not yet wired into CI, same reason as the existing E2E spec |
| Lead lifecycle fields — status/priority/assignment/tags on every lead, defaulted for the public scan flow, `LeadRepository.findById`/`update`/`search` (EAP-2) | `@titan/platform` | 22 shared contract tests × 2 implementations (in-memory + real D1 via sql.js) = 44 | ✅ Implemented & verified against real local D1 |
| `GET /api/leads/:id`, `PATCH /api/leads/:id`, `GET /api/leads/search` — Platform-Administrator-only, real audit events on view/status/priority/assignment/tag/note changes (EAP-2) | `@titan/platform` | 20 (router) + 1 (worker, real D1 end-to-end) tests | ✅ Implemented & verified against real local D1 via `wrangler dev` — includes a real CSP-class finding (missing `PATCH` in CORS `Access-Control-Allow-Methods`), found and fixed (`DECISION_LOG.md`) |
| `GET /api/audit` entity-scoped filtering (`?entityType=&entityId=`) — a single lead's own real activity/audit trail, not the whole table fetched client-side (EAP-2) | `@titan/platform` | 2 shared contract tests × 2 implementations = 4 | ✅ Implemented & verified |
| Shared enterprise UI primitives — `Badge`, `SearchBar`, `Pagination`, `EmptyState`, `LoadingSkeleton`, `FilterPanel`, `Timeline`, `DataTable` (real `<table>`, sortable headers with `aria-sort`, no virtualization — not justified by real data volume yet) (EAP-2) | `@titan/design-system` | 32 tests, incl. axe a11y checks on every component | ✅ Implemented |
| Lead Workspace — real server-side search/filter/sort/pagination, saved filters and column selection (`localStorage`-backed, per-browser), honest per-state loading/forbidden/error/empty rendering (EAP-2) | `@titan/web` (`features/admin/leads`) | 8 tests (`LeadWorkspacePage.test.tsx`) | ✅ Implemented & verified against real D1-backed data in a real browser |
| Lead Details — identity, organization (resolved to a real name via `GET /api/organizations`), submission info, Risk Intelligence (real score/breakdown/findings-by-severity from the lead's own recomputed result), Lifecycle controls (status/priority/assign-to-me-or-unassign/tags/notes), Activity & audit history (EAP-2) | `@titan/web` (`features/admin/leads`) | 9 (`LeadDetailPage.test.tsx`) + 9 (`LeadLifecyclePanel.test.tsx`) + 5 (`LeadRiskPanel.test.tsx`) + 6 (`LeadAuditPanel.test.tsx`) tests | ✅ Implemented & verified — a real lifecycle update persists across a page reload and appears in the real audit trail, verified in a real browser |
| Playwright E2E — Lead Intelligence Platform: unauthenticated redirect, non-admin honest-forbidden messaging, real search→filter→detail-navigation, a real lifecycle update persisting across a reload with a real audit-trail entry (EAP-2) | `@titan/web` (`e2e/lead-workspace.spec.ts`) | 1 spec, 4 tests, suite run strictly serially (`workers: 1`) after a real cross-file shared-D1 race was found and fixed, stable across 5 consecutive full-suite runs total across both verification passes | ✅ Implemented & verified — found and fixed a real CORS `Access-Control-Allow-Methods` bug, a real `useLeadDetail` StrictMode race condition, and a real cross-file Playwright worker race on the suite's shared local D1 (`DECISION_LOG.md`); not yet wired into CI, same reason as the existing E2E specs |
| `GET /api/assessments/search` — Platform-Administrator-only server-side search (id/organizationId/createdBy substring)/filter (framework, riskLevel)/sort (createdAt, riskScore, framework)/pagination; `GET /api/assessments/:id` now also records a real `assessment.viewed` audit event with the real caller as actor (EAP-3) | `@titan/platform` | 9 (search router tests) + 2 (assessment.viewed audit tests) + 1 (worker, real D1 end-to-end) tests | ✅ Implemented & verified against real local D1 via `wrangler dev` |
| `LeadRepository.search()`/`GET /api/leads/search` gained an `assessmentId` exact-match filter — backs Assessment Details' "Lead linkage" panel (EAP-3) | `@titan/platform` | 1 contract test (× 2 implementations) + 1 router test + covered in the worker end-to-end test | ✅ Implemented & verified against real local D1 |
| Assessment Workspace — real server-side search/filter (framework, risk level)/sort/pagination, saved filters and column selection (`localStorage`-backed, per-browser), honest per-state loading/forbidden/error/empty rendering (EAP-3) | `@titan/web` (`features/admin/assessments`) | 9 tests (`AssessmentWorkspacePage.test.tsx`) | ✅ Implemented & verified against real D1-backed data in a real browser |
| Compliance Intelligence — aggregate risk distribution, framework status, outstanding findings by DPDP section, month-over-month risk trend, all computed client-side from the existing `GET /api/assessments` full-list endpoint (EAP-3) | `@titan/web` (`features/admin/assessments/ComplianceIntelligencePanel.tsx`) | 4 tests | ✅ Implemented & verified against real D1-backed data in a real browser |
| Assessment Details — metadata (framework/version, honest "Completed" status, completion timestamp, organization, owner), Assessment Results (risk badge/score, severity breakdown, findings by severity, category coverage by DPDP section, full question-by-question responses with Pass/Gap status), Lead linkage (real leads produced by this assessment), Activity & audit history (EAP-3) | `@titan/web` (`features/admin/assessments`) | 9 (`AssessmentDetailPage.test.tsx`) + 7 (`AssessmentResultsPanel.test.tsx`, incl. axe a11y check) + 4 (`AssessmentAuditPanel.test.tsx`) tests | ✅ Implemented & verified against real D1-backed data in a real browser |
| Playwright E2E — Enterprise Assessment Center: unauthenticated redirect, non-admin honest-forbidden messaging, real search→filter→detail-navigation, Assessment Details' Findings/Category coverage/Question responses/audit history/lead linkage all rendering real data (EAP-3) | `@titan/web` (`e2e/assessment-center.spec.ts`) | 1 spec, 4 tests, stable across 3 consecutive clean full-suite runs (all 4 specs, 12 tests total) | ✅ Implemented & verified — found and fixed a real pre-existing `LeadWorkspacePage.tsx` sort-column bug and a real `useLeadDetail`/`useAssessmentDetail` audit-trail race condition (`DECISION_LOG.md`); not yet wired into CI, same reason as the existing E2E specs |
| `OrganizationRepository` extended — `status`/`industry`/`region`/`tags`/`updatedAt` fields, `findById`/`update`/`search` (search: filter by status/industry/region/tag, substring on name/slug/industry/region, sort, paginate; `updatedAt` repository-owned, refreshed on every `update`), in-memory + D1 (EAP-4) | `@titan/platform` | 23 shared contract tests × 2 implementations = 46 | ✅ Implemented & verified against real local D1 |
| `LeadRepository.search()`/`AssessmentRepository.search()` gained an `organizationId` exact-match filter — backs Organization Relationships' "associated leads"/"associated assessments" panels (EAP-4) | `@titan/platform` | 1 contract test each (× 2 implementations) + covered in the worker end-to-end test | ✅ Implemented & verified against real local D1 |
| `POST /api/organizations`, `GET /api/organizations/search`, `GET /api/organizations/:id`, `PATCH /api/organizations/:id` — Platform-Administrator-only, CSRF-checked writes, real audit events on create/view/update/archive/restore/note (EAP-4) | `@titan/platform` | 21 (router) + 1 (worker, real D1 end-to-end) tests | ✅ Implemented & verified against real local D1 via `wrangler dev` |
| Organization Workspace — real server-side search/filter (status)/sort/pagination, saved filters and column selection (`localStorage`-backed, per-browser), a real inline create-organization form (auto-slugging identifier from name), honest per-state loading/forbidden/error/empty rendering (EAP-4) | `@titan/web` (`features/admin/organizations`) | 8 tests (`OrganizationWorkspacePage.test.tsx`) | ✅ Implemented & verified against real D1-backed data in a real browser |
| Organization Health — current risk/average score/risk distribution/score trend, all computed client-side from this organization's own real linked assessments (EAP-4) | `@titan/web` (`features/admin/organizations/OrganizationHealthPanel.tsx`) | 4 tests | ✅ Implemented & verified against real D1-backed data in a real browser |
| Organization Relationships — real linked leads/assessments, deep-linked into the existing Lead Intelligence/Assessment Center modules rather than duplicated (EAP-4) | `@titan/web` (`features/admin/organizations/OrganizationRelationshipsPanel.tsx`) | 3 tests | ✅ Implemented & verified |
| Organization Administration — real name/industry/region/tag edits, archive/restore, notes, every control a direct `PATCH` (EAP-4) | `@titan/web` (`features/admin/organizations/OrganizationAdministrationPanel.tsx`) | 11 tests, incl. axe a11y check | ✅ Implemented & verified against real D1-backed data in a real browser |
| Organization Details — Metadata, Health, Relationships, Administration, Activity & audit history (EAP-4) | `@titan/web` (`features/admin/organizations`) | 7 (`OrganizationDetailPage.test.tsx`) + 4 (`OrganizationAuditPanel.test.tsx`) + 2 (`OrganizationStatusBadge.test.tsx`) tests | ✅ Implemented & verified against real D1-backed data in a real browser |
| Playwright E2E — Enterprise Organization Management Platform: unauthenticated redirect, non-admin honest-forbidden messaging, real create-via-form→search→detail-navigation, real Health/Relationships/Administration(archive)/audit history (EAP-4) | `@titan/web` (`e2e/organization-workspace.spec.ts`) | 1 spec, 4 tests, stable across 2 consecutive clean full-suite runs (all 5 specs, 16 tests total) | ✅ Implemented & verified — found and fixed a real pre-existing `leadApi.ts` query-string-forwarding bug from EAP-3 (`DECISION_LOG.md`); not yet wired into CI, same reason as the existing E2E specs |
| `UserRepository` — read-only view over Auth.js's own `users` table (`findById`/`search` by name/email substring, sort, paginate; no `save` — identity is created only by a real sign-in), in-memory + D1 (EAP-5) | `@titan/platform` | 8 shared contract tests × 2 implementations = 16 | ✅ Implemented & verified against real local D1 |
| `UserProfileRepository` extended — `findById`/`list`/`update`(role)/`remove` alongside the pre-existing `save`/`findByUserId` (RC1); `remove` is this system's first real deletion (EAP-5) | `@titan/platform` | 7 new shared contract tests × 2 implementations = 14 (24 total, up from 10) | ✅ Implemented & verified against real local D1 |
| `LeadRepository`/`AssessmentRepository.search()` — `AssessmentSearchOptions` gained a `createdBy` exact-match filter, backing User Relationships' "assessments created by this user" panel; combined with the pre-existing `LeadSearchOptions.assignedTo` (EAP-2) for "leads assigned to this user" (EAP-5) | `@titan/platform` | 1 new contract test × 2 implementations = 2 | ✅ Implemented & verified against real local D1 |
| `GET /api/users/search` — Platform-Administrator-only server-side search (name/email substring)/sort(name, email)/pagination (EAP-5) | `@titan/platform` | 4 router tests | ✅ Implemented & verified against real local D1 via `wrangler dev` |
| `GET /api/users/:id` — identity composed with the user's own `UserProfileRecord[]`, records a real `user.viewed` audit event (EAP-5) | `@titan/platform` | 4 router tests | ✅ Implemented & verified against real local D1 |
| `POST /api/users/:id/profiles` (grant), `PATCH /api/users/:id/profiles/:profileId` (change role), `DELETE /api/users/:id/profiles/:profileId` (revoke) — Platform-Administrator-only, CSRF-checked writes, real `user.role_granted`/`user.role_changed`/`user.role_revoked` audit events; `PATCH`/`DELETE` guarded by `wouldRemoveLastPlatformAdministrator` (EAP-5) | `@titan/platform` | 9 (grant) + 8 (change) + 7 (revoke) + 1 (worker, real D1 end-to-end) = 25 router/worker tests | ✅ Implemented & verified against real local D1 via `wrangler dev` — first real `DELETE` request and first real deletion in this codebase, both verified in a real browser |
| Enterprise User Directory (Workspace) — real server-side search/sort/pagination over Auth.js's own `users` table; deliberately no `FilterPanel`/saved filters/column toggle/create-form (`DECISION_LOG.md`'s EAP-5 entry explains each omission) (EAP-5) | `@titan/web` (`features/admin/users`) | 6 tests (`UserWorkspacePage.test.tsx`) | ✅ Implemented & verified against real D1-backed data in a real browser |
| User Details — Identity (name/email/verified/id), Role Assignment (grant/change/revoke, real server round-trips, never optimistic-only), Relationships (real assigned leads + real created assessments, deep-linked into their own modules), Activity & audit history (EAP-5) | `@titan/web` (`features/admin/users`) | 7 (`UserDetailPage.test.tsx`) + 9 (`RoleAssignmentPanel.test.tsx`, incl. axe a11y check) + 3 (`UserRelationshipsPanel.test.tsx`) + 4 (`UserAuditPanel.test.tsx`) + 4 (`RoleBadge.test.tsx`) tests | ✅ Implemented & verified against real D1-backed data in a real browser |
| Playwright E2E — Enterprise Identity & User Management: unauthenticated redirect, non-admin honest-forbidden messaging, real search→detail-navigation, real grant→change→revoke round trip through Role Assignment (including this codebase's first real `DELETE` request), real audit history (EAP-5) | `@titan/web` (`e2e/user-management.spec.ts`) | 1 spec, 4 tests, stable across 3 of 4 consecutive full-suite runs (all 6 specs, 20 tests total) — the one exception a `workerd` CLI-contention flake, reproduced-clean on immediate retry | ✅ Implemented & verified — found and fixed a real environmental gap (missing `.dev.vars`/`AUTH_SECRET` in a fresh container, `DECISION_LOG.md`); not yet wired into CI, same reason as the existing E2E specs |
| `AuditRepository.search()` — filtered/paginated/sorted search (`search`/`actorId`/`organizationId`/`action`/`entityType`/`entityId`/`dateFrom`/`dateTo`/`sortDirection`/`page`/`pageSize`), additive alongside the pre-existing `record`/`list`, in-memory + D1 (EAP-6) | `@titan/platform` | 9 new shared contract tests × 2 implementations = 18 (32 total, up from 14) | ✅ Implemented & verified against real local D1 |
| `GET /api/audit/search` — Platform-Administrator-only server-side search over every audit event, same filter set as the repository method above (EAP-6) | `@titan/platform` | 5 router tests | ✅ Implemented & verified against real local D1 via `wrangler dev` |
| `GET /api/audit/export` — CSV/JSON file download of the same filtered set, capped at 10,000 rows, `Content-Disposition: attachment`; CSV/formula-injection risk investigated and verified not present in this data model (EAP-6) | `@titan/platform` | 6 router tests (incl. a CORS `Access-Control-Expose-Headers` regression test — see the CORS finding below) | ✅ Implemented & verified against real local D1 via `wrangler dev` |
| Enterprise Audit Workspace — real server-side search/filter (entity type, action, actor id, organization id, date range)/sort/pagination/column preferences/saved filters over every audit event, plus a Table/Investigation view toggle and CSV/JSON export (EAP-6) | `@titan/web` (`features/admin/audit`) | 9 (`AuditWorkspacePage.test.tsx`) + 3 (`AuditEntityBadge.test.tsx`) + 4 (`auditApi.test.ts`) tests | ✅ Implemented & verified against real D1-backed data in a real browser |
| Audit Details — an inline panel (not a new route) showing actor/action/timestamp/entity/organization/raw metadata/related events for any event in a loaded search result; honestly states this event log has no request-context or result-status columns rather than fabricating either (EAP-6) | `@titan/web` (`features/admin/audit`) | 5 (`AuditEventDetailPanel.test.tsx`) tests | ✅ Implemented & verified against real D1-backed data in a real browser |
| Investigation View — groups the currently-loaded page of search results by entity/actor/organization, reusing the shared `Timeline` component (EAP-6) | `@titan/web` (`features/admin/audit`) | 4 (`AuditInvestigationView.test.tsx`) tests | ✅ Implemented & verified against real D1-backed data in a real browser |
| Playwright E2E — Enterprise Audit Center: unauthenticated redirect, non-admin honest-forbidden messaging, real seeded-event search via the entity-type filter with Audit Details' real metadata rendering, Investigation View grouping, and a real CSV file download verified by its actual downloaded content (the test that caught the CORS `Access-Control-Expose-Headers` gap below) (EAP-6) | `@titan/web` (`e2e/audit-center.spec.ts`) | 1 spec, 5 tests, clean across 2 consecutive full-suite runs (all 7 specs, 25 tests total) — one transient, unrelated flake on an already-shipped EAP-1 sign-out scenario during the first run, reproduced-clean on retry | ✅ Implemented & verified — found and fixed a real cross-origin CORS gap (`http/cors.ts` never exposed `Content-Disposition` to cross-origin JS, `DECISION_LOG.md`); not yet wired into CI, same reason as the existing E2E specs |
| `Metrics.getCounts()`/`.getDurations()` — promoted from the in-memory recorder's own extra methods onto the interface itself, so any `Metrics` backend can be read, not just written to (EAP-7) | `@titan/platform` | 4 new tests (`metrics.test.ts`, this repository's first dedicated test file for this module) | ✅ Implemented & verified |
| `GET /api/operations/summary` — Platform-Administrator-only aggregate of real per-service reachability (`leads`/`assessments`/`organizations`/`users`/`userProfiles`/`audit`, each a genuine `search()`/`list()` call), real request/repository-operation metrics, and a static system overview (EAP-7) | `@titan/platform` | 4 router tests | ✅ Implemented & verified against real local D1 via `wrangler dev` |
| Enterprise Operations Workspace — Platform Health, Readiness, Service Status, Runtime Metrics, Background Operations (honest "none exists" note), and System Overview, six independently-loading panels reusing `Panel`/`DataTable`/`Badge`/`EmptyState`/`LoadingSkeleton`/`Alert` (EAP-7) | `@titan/web` (`features/admin/operations`) | 3 (`OperationsWorkspacePage.test.tsx`) + 3 (`ServiceStatusPanel.test.tsx`) + 3 (`RuntimeMetricsPanel.test.tsx`) + 1 (`SystemOverviewPanel.test.tsx`) tests | ✅ Implemented & verified against real D1-backed data in a real browser |
| Playwright E2E — Enterprise Operations Center: unauthenticated redirect, non-admin honest-forbidden messaging with role-agnostic health still visible, and a Platform Administrator view whose Runtime Metrics table reflects a real prior request this same test sent through the running Worker (EAP-7) | `@titan/web` (`e2e/operations-center.spec.ts`) | 1 spec, 3 tests, clean across 2 consecutive full-suite runs (all 8 specs, 28 tests total), zero flakes | ✅ Implemented & verified; not yet wired into CI, same reason as the existing E2E specs |
| `countPlatformAdministrators` — a system-wide sibling to the existing per-caller `isPlatformAdministrator`, same predicate (EAP-8) | `@titan/platform` | 2 tests (`rbac.test.ts`) | ✅ Implemented & verified |
| `GET /api/reports/summary` — Platform-Administrator-only `ExecutiveSummary`: organization/lead/assessment/identity/audit counts and breakdowns, composed live from every prior module's own `list()`/`search()` reads, no new table (EAP-8) | `@titan/platform` | 4 router tests | ✅ Implemented & verified against real local D1 via `wrangler dev` |
| `GET /api/reports/trends` — Platform-Administrator-only per-day `TrendSeries` for one of leads/assessments/organizations/audit/identity over a caller-chosen 1–90 day window (EAP-8) | `@titan/platform` | 7 router tests | ✅ Implemented & verified against real local D1 via `wrangler dev` |
| `GET /api/reports/export` — the same `ExecutiveSummary` as a real CSV/JSON file download via `Content-Disposition`; contains only aggregate counts, no individual-record fields, so needs no row cap the way Audit Export's does (EAP-8) | `@titan/platform` | 5 router tests | ✅ Implemented & verified against real local D1 via `wrangler dev` |
| `TrendSparkline` — a minimal, dependency-free inline SVG polyline with a real, data-derived `aria-label`, not a charting library dependency (EAP-8) | `@titan/design-system` | 4 tests, incl. axe a11y check | ✅ Implemented |
| Enterprise Reporting & Analytics Workspace — Executive Dashboard (real KPI `MetricCard`s, honest "—"/"Not configured" for an unwired report), Business Reports (real `DataTable` breakdowns), Analytics (entity/date-range selector, `TrendSparkline` + a real accessible per-day table), Export CSV/JSON (EAP-8) | `@titan/web` (`features/admin/reporting`) | 2 (`ExecutiveSummaryPanel.test.tsx`) + 2 (`BusinessReportsPanel.test.tsx`) + 3 (`AnalyticsPanel.test.tsx`) + 3 (`ReportingWorkspacePage.test.tsx`) tests | ✅ Implemented & verified against real D1-backed data in a real browser |
| Playwright E2E — Enterprise Reporting & Analytics: unauthenticated redirect, non-admin honest-forbidden messaging, and a Platform Administrator view whose Executive Dashboard/Business Reports/Analytics all reflect a real, freshly seeded lead, plus a real downloaded-CSV-content assertion (EAP-8) | `@titan/web` (`e2e/reporting-center.spec.ts`) | 1 spec, 3 tests, clean across 2 consecutive full-suite runs (all 9 specs, 31 tests total), zero flakes | ✅ Implemented & verified; not yet wired into CI, same reason as the existing E2E specs |
| `SupportRequestRepository` — save/listByUser, in-memory + D1; the one genuinely new entity CPP-1 needed (CPP-1) | `@titan/platform` | 6 shared contract tests × 2 implementations = 12 | ✅ Implemented & verified against real local D1 |
| D1 migration — `support_requests` table (`migrations/0010_support_requests.sql`), 10 migration files total | `@titan/platform` | Applied and round-tripped against a **real local D1 SQLite instance** (`wrangler d1 migrations apply --local`) | ✅ Implemented & verified |
| `resolvePortalOrganizationId` — derives the caller's own organization id server-side from their real `UserProfileRecord[]`, never from a client-supplied id/query-string/path param; 403s an authenticated caller with no real organization membership (CPP-1) | `@titan/platform` | Covered across every `GET/POST /api/portal/*` router test below, incl. an explicit "ignores any organizationId the client tries to supply" case | ✅ Implemented & verified |
| `GET /api/portal/organization`, `GET /api/portal/assessments`, `GET /api/portal/reports/summary`, `GET /api/portal/reports/export`, `GET /api/portal/activity`, `GET/POST /api/portal/support` — 7 new customer-scoped routes, each deriving its organization id server-side; reuses the existing `AssessmentRepository`/`OrganizationRepository`/`AuditRepository` reads with `organizationId` forced to the caller's own, no new cross-tenant surface added to any admin route (CPP-1) | `@titan/platform` | 22 router tests, incl. cross-organization-isolation assertions on assessments/reports/activity, a CSRF-mismatched-Origin 403 on the one write route, and a "no individual assessment fields" PII check on the CSV export | ✅ Implemented & verified against real local D1 via `wrangler dev` |
| Customer Portal shell — `/portal`, a real sibling route tree to `/admin` (not nested under it), reusing `Header`/`Sidebar`/`Footer`/`Breadcrumbs`/`useSession`/`RequireAuth` unchanged; `portalNavItems` renders no navigation at all, and the layout shows an honest "No organization membership" message rather than a broken shell, for an authenticated caller with no real organization profile (CPP-1) | `@titan/web` (`features/portal/layout`) | 6 (`portalNavItems.test.ts`) + 4 (`PortalLayout.test.tsx`) tests | ✅ Implemented & verified against real D1-backed data in a real browser |
| `AssessmentSummaryCard` — the one new shared component CPP-1 needed, 2 real consumers (Dashboard's Compliance Summary and the Reports page), reuses the existing `RiskBadge`/`MetricCard` rather than duplicating either (CPP-1) | `@titan/web` (`features/portal`) | 3 tests, incl. axe a11y check | ✅ Implemented |
| Customer Dashboard — Organization Overview (name/industry/region/status), Compliance Summary (`AssessmentSummaryCard` + link to Reports), Recent Activity (this organization's own real audit events, reusing the existing `auditActionLabel`), each section loading and failing independently (CPP-1) | `@titan/web` (`features/portal/dashboard`) | 3 tests (`PortalDashboardPage.test.tsx`) | ✅ Implemented & verified against real D1-backed data in a real browser |
| Customer Assessments — read-only paginated table of this organization's own assessments (`DataTable`/`RiskBadge`/`FrameworkBadge`/`Pagination`/`EmptyState`, no admin editing controls at all) plus Assessment Detail, which reuses the *existing* `GET /api/assessments/:id` and its pre-existing `requireAssessmentAccess` gate directly — no new backend route — showing a real, honest "This assessment is not available to your organization." message for any assessment outside the caller's own organization (CPP-1) | `@titan/web` (`features/portal/assessments`) | 5 (`PortalAssessmentsPage.test.tsx`) + 4 (`PortalAssessmentDetailPage.test.tsx`) tests | ✅ Implemented & verified against real D1-backed data in a real browser |
| Customer Reports — a real, organization-scoped compliance summary (assessments by risk level, by framework, latest assessment date) with real CSV/JSON export downloads, reusing the reporting endpoints' own CSV/export conventions (CPP-1) | `@titan/web` (`features/portal/reports`) | 2 tests (`PortalReportsPage.test.tsx`) | ✅ Implemented & verified against real D1-backed data in a real browser |
| Customer Account — Profile (email, organization role) and Session (email + a real sign-out link via the existing `signOutUrl`); deliberately does not build password change, notification preferences, or multi-session management — no credentials provider, settings/notification service, or `SessionRepository` beyond Auth.js's own single-token model exists to back any of them (CPP-1) | `@titan/web` (`features/portal/account`) | 3 tests (`PortalAccountPage.test.tsx`) | ✅ Implemented & verified against real D1-backed data in a real browser |
| Customer Support — submit a request (subject/message, CSRF-checked write) and view its own history via the shared `Timeline`; no ticketing platform — `status` starts and stays `"open"`, there is no admin-side resolution endpoint anywhere in this codebase (CPP-1) | `@titan/web` (`features/portal/support`) | 4 tests (`PortalSupportPage.test.tsx`) | ✅ Implemented & verified against real D1-backed data in a real browser |
| Playwright E2E — Enterprise Customer Portal: unauthenticated redirect to the real sign-in page, an honest "no organization membership" message (nav hidden entirely, not shown-then-blocked) for an authenticated caller with no profile, and a real organization member's Dashboard/Assessments/Reports (incl. a real CSV download) all reflecting only their own organization's data — including the core cross-tenant proof: navigating directly to another organization's assessment by id never returns its data — plus a real support-request submit-and-view-history flow (CPP-1) | `@titan/web` (`e2e/customer-portal.spec.ts`) | 1 spec, 4 tests, clean across 2 consecutive full-suite runs (all 10 specs, 35 tests total), zero flakes | ✅ Implemented & verified; not yet wired into CI, same reason as the existing E2E specs |
| Plan catalog — a real, versioned, code-defined list (Starter/Professional/Enterprise), the same "ships with the app, not authored through an admin UI" pattern `@titan/assessment-core`'s DPDP question bank already established; `priceDisplay` is display text only, never a chargeable amount (COM-1) | `@titan/platform` (`commercial/planCatalog.ts`) | 7 tests (`planCatalog.test.ts`) | ✅ Implemented |
| `resolveEntitlements` — a pure function of a plan's own entitlements and a subscription's current status; collapses to nothing for a `canceled`/`expired` subscription, grants the full plan during `trialing` (a trial is full-featured, not crippled); computed and displayed, not yet wired as an enforcement gate on any existing route (COM-1) | `@titan/platform` (`commercial/entitlements.ts`) | 4 tests (`entitlements.test.ts`) | ✅ Implemented & verified |
| `SubscriptionRepository`/`LicenseRepository` — save/findByOrganizationId/findById/search/update, in-memory + D1; License is deliberately 1:1 with Subscription (the seat grant itself), never a per-user assignment table — seat *usage* is the live count of an organization's own real `UserProfileRecord`s (COM-1) | `@titan/platform` | 16 + 14 shared contract tests × 2 implementations = 60 | ✅ Implemented & verified against real local D1 |
| `UserProfileRepository.findByOrganizationId` — every real member of one organization, the mirror image of the pre-existing `findByUserId`; backs live seat-usage counts on both the Customer Portal's own Subscription page and the admin Commercial Detail page (COM-1) | `@titan/platform` | 3 new shared contract tests × 2 implementations = 6 | ✅ Implemented & verified against real local D1 |
| D1 migrations — `subscriptions`/`licenses` tables (`migrations/0011_subscriptions.sql`, `0012_licenses.sql`), 12 migration files total; provider-agnostic — no payment amount, invoice, or card/token column anywhere in either table (COM-1) | `@titan/platform` | Applied and round-tripped against a **real local D1 SQLite instance** (`wrangler d1 migrations apply --local`) | ✅ Implemented & verified |
| `GET /api/commercial/plans` (any authenticated caller — non-sensitive catalog data), `GET`/`POST`/`PATCH /api/portal/commercial/subscription` (customer-scoped, `resolvePortalOrganizationId`), `GET /api/commercial/subscriptions/search`, `GET`/`PATCH /api/commercial/subscriptions/:id`, `GET /api/commercial/licenses/search` (Platform-Administrator-only) — 8 new routes; the customer-facing `PATCH` refuses the sales-assisted "enterprise" plan (`isSelfServicePlan`), the admin `PATCH` doesn't — both share one internal state machine (`applySubscriptionPatch`) that derives real `subscription.upgraded`/`.downgraded`/`.canceled`/`.renewed`/`license.activated`/`.expired` audit events from what actually changed (COM-1) | `@titan/platform` | 35 router tests, incl. cross-organization-isolation on the portal summary, a CSRF-mismatched-Origin 403 on every write route, and the sales-assisted-plan-blocked-for-customers-but-not-admins property | ✅ Implemented & verified against real local D1 via `wrangler dev` |
| Shared commercial components — `PlanCard`, `SubscriptionStatusBadge`, `SeatUsageMeter` (a real `progressbar` role, not a colored `<div>`), `EntitlementBadge`; each with two real consumers, the Customer Portal's own Subscription page and the admin Commercial Workspace/Detail pages (COM-1) | `@titan/web` (`features/admin/commercial`) | 5 (`PlanCard.test.tsx`, incl. axe a11y check) + 1 (`SubscriptionStatusBadge.test.tsx`) + 2 (`SeatUsageMeter.test.tsx`) + 1 (`EntitlementBadge.test.tsx`) tests | ✅ Implemented & verified |
| Commercial Workspace/Subscription Detail — `/admin/commercial`, real server-side search/filter (status)/sort/pagination over every organization's own subscription, no create-form (a subscription is created by self-service or an admin override on an existing organization, never authored from a blank form); Detail shows Plan/License/seat-usage plus an admin override (any known plan incl. "enterprise", cancel/renew) (COM-1) | `@titan/web` (`features/admin/commercial`) | 3 (`CommercialWorkspacePage.test.tsx`) + 2 (`CommercialSubscriptionDetailPage.test.tsx`) tests | ✅ Implemented & verified against real D1-backed data in a real browser |
| Customer Subscription — `/portal/subscription`, combining Commercial Dashboard content (Subscription Overview, Current Plan, License Summary, Usage Summary, Entitlements, Renewal Information, Trial Status) and lifecycle actions (Plan Selection, Upgrade, Downgrade, Cancellation, Renewal) on one page; an honest "no active subscription yet" empty state with real plan cards, never a fabricated default plan (COM-1) | `@titan/web` (`features/portal/subscription`) | 4 tests (`PortalSubscriptionPage.test.tsx`) | ✅ Implemented & verified against real D1-backed data in a real browser |
| Playwright E2E — Enterprise Commercial Platform: unauthenticated redirect, a real organization member subscribing to a real plan and seeing it reflected, a real cancel-then-renew round trip, and a Platform Administrator reviewing real cross-organization subscriptions and assigning the sales-assisted "enterprise" plan directly — the one thing the customer-facing path never even renders a button for (COM-1) | `@titan/web` (`e2e/commercial-platform.spec.ts`) | 1 spec, 4 tests, clean across 2 consecutive full-suite runs (all 11 specs, 39 tests total), zero flakes | ✅ Implemented & verified; not yet wired into CI, same reason as the existing E2E specs |
| Real latency percentile (p50/p95/p99, nearest-rank) and 4xx/5xx error-rate aggregation over the existing runtime metrics counters — nothing computed either before this phase (OPS-1) | `@titan/platform` (`observability/aggregate.ts`) | 12 tests (`aggregate.test.ts`) | ✅ Implemented & verified |
| Real, evidence-backed alert evaluation — a pure function against named thresholds (readiness, service reachability, configuration validity, 5xx error rate, p95 latency), every alert carrying the real value that triggered it; never delivered to a human (no paging provider/deployed environment exists) (OPS-1) | `@titan/platform` (`observability/alerts.ts`) | 17 tests (`alerts.test.ts`) | ✅ Implemented & verified; paging delivery Blocked (no deployed environment, no provider decision) |
| Readiness (`GET /health/ready`) now also fails for an invalid `staging`/`production` configuration, via a function shared with `GET /api/operations/summary` so both report the identical real answer (OPS-1) | `@titan/platform` (`router.ts`'s `computeReadiness`) | 3 new `router.test.ts` tests | ✅ Implemented & verified |
| `GET /api/operations/summary` gains `requestSummary` (real error rate + latency percentiles) and `alerts` (real evaluated alerts) — always present, unlike the optional `configuration` field (OPS-1) | `@titan/platform` | 3 new `router.test.ts` tests | ✅ Implemented & verified against real local D1 via `wrangler dev` |
| Operations Center dashboard — Alerts panel (honest "No alerts firing" success state when empty), a "Request health" section in Runtime Metrics, and an Operational Summary banner — every one composed from data the endpoint already returns (OPS-1) | `@titan/web` (`features/admin/operations`) | 4 (`AlertsPanel.test.tsx`) + 3 new (`RuntimeMetricsPanel.test.tsx`) + 1 new (`OperationsWorkspacePage.test.tsx`) tests | ✅ Implemented & verified against real D1-backed data in a real browser |
| `check-operational-thresholds.mjs` — a real, no-auth-required single-request latency check against `/health`/`/health/ready`, classified against documented thresholds; deliberately independent of `smoke-test.mjs` (OPS-1) | `@titan/platform` (`scripts/`) | No automated test file (same as the other 5 release scripts); run for real against a live local `wrangler dev` Worker, both the passing path and the unreachable-URL failure path | ✅ Implemented & verified locally |
| `frame-ancestors 'none'` added explicitly to both CSP variants — closes a real gap where `default-src 'none'` provided zero clickjacking protection on its own (CSP3's `frame-ancestors` doesn't inherit from `default-src`) (SEC-1) | `@titan/platform` (`http/finalizeResponse.ts`) | 1 new `router.test.ts` regression test | ✅ Implemented & verified |
| Rate limiting extended to `POST /api/portal/support` and `POST`/`PATCH /api/portal/commercial/subscription` — closes a real inconsistency where `POST /api/organizations` (a peer authenticated write) already had it and these didn't (SEC-1) | `@titan/platform` (`router.ts`) | 3 new `router.test.ts` tests, one per route | ✅ Implemented & verified |
| Explicit least-privilege `permissions: contents: read` on `titan-ci.yml`/`titan-deploy.yml`, matching `deploy.yml`'s existing convention (SEC-1) | `.github/workflows/` | YAML syntax verified (`python3`'s `yaml.safe_load`); no automated test (CI configuration, not application code) | ✅ Implemented & verified |
| Security findings register — 12 findings, each classified by real-world severity in this system's actual context, with status and evidence, including a fresh `npm audit` result (3 high-severity CVEs, honestly deferred, not force-fixed) (SEC-1) | `docs/titan/THREAT_MODEL.md` | N/A (documentation) | ✅ Implemented |
| Penetration-test preparation — 12 real attack scenarios, each cited to an existing, passing, committed test (not a claim a pentest was performed) (SEC-1) | `docs/titan/THREAT_MODEL.md` | Cites `router.test.ts`/`apps/web/e2e/*.spec.ts` tests that already exist | ✅ Implemented |
| `billing_transactions` table + `BillingTransactionRepository` — save/findById/findByProviderOrderId/search/update, in-memory + D1; deliberately independent of `subscriptions`/`licenses` (Razorpay/DPDP Scanner) | `@titan/platform` (`migrations/0013_billing_transactions.sql`) | 14 shared contract tests × 2 implementations = 28 | ✅ Implemented & verified against real local D1 |
| `commercial/razorpay.ts` — `createRazorpayOrder` (real `fetch` to Razorpay's own `/v1/orders`, HTTP Basic Auth, server-resolved amount only) and `verifyRazorpaySignature` (real HMAC-SHA256 via Web Crypto, constant-time comparison); no `razorpay` npm SDK dependency (Razorpay/DPDP Scanner) | `@titan/platform` | 9 tests, incl. real cross-implementation HMAC vectors verified independently against Node's own `node:crypto` | ✅ Implemented & verified |
| `Plan.priceInPaise` — the one real, server-resolved chargeable amount in the catalog (₹9,999/mo Starter, ₹29,999/mo Professional, `null` for the sales-assisted Enterprise plan); `priceDisplay` stays display-only, unchanged (Razorpay/DPDP Scanner) | `@titan/platform` (`commercial/planCatalog.ts`) | 2 new tests | ✅ Implemented & verified |
| `hasVerifiedDpdpScannerAccess` — the scanner's real payment gate: `subscription.status === "active"` **and** a real `billing_transactions` row with `status: "paid"`, deliberately independent of `resolveEntitlements` (which grants full access during a trial, by COM-1's own design) (Razorpay/DPDP Scanner) | `@titan/platform` | Covered across the router tests for the access/scan endpoints below, incl. an explicit "402 even while trialing" case proving independence from `resolveEntitlements` | ✅ Implemented & verified |
| `POST /api/portal/commercial/razorpay/orders` (rate-limited — the only authenticated portal write that calls a real, costable third-party API per request), `POST /api/portal/commercial/razorpay/verify` (real HMAC recomputation, idempotent on an already-paid transaction, org-scoped 404-not-403), `GET /api/portal/dpdp-scanner/access`, `POST /api/portal/dpdp-scanner/scan` (402 without access; otherwise reuses `scoreAssessment`/`AssessmentRepository.save` unchanged) — 4 new routes (Razorpay/DPDP Scanner) | `@titan/platform` | 27 router tests, incl. cross-organization 404s, signature-verification success/failure, idempotency, and rate-limit enforcement (first request 201, second 429) | ✅ Implemented & verified against real local D1 via `wrangler dev` |
| DPDP Scanner Dashboard card — a premium-feature promotion placed first, above Organization Overview, its call to action driven by the real access check above, not a client-side guess (Razorpay/DPDP Scanner) | `@titan/web` (`features/portal/dpdp-scanner/DpdpScannerCard.tsx`) | Covered via `PortalDashboardPage.test.tsx` (2 new tests: unlock vs. run-a-new-scan states) | ✅ Implemented & verified against real D1-backed data in a real browser |
| DPDP Scanner page (`/portal/dpdp-scanner`) — a real Razorpay-checkout paywall (dynamically-loaded Checkout widget, real order creation, real signature verification on success) or the scanner itself, decided by the one real access check; the scanner-taking flow reuses `ProgressBar`/`QuestionStep`/`RiskResults`/`pdfReport.ts` from the pre-existing anonymous flow unchanged — a paid scan needs zero new results/history/export UI (Razorpay/DPDP Scanner) | `@titan/web` (`features/portal/dpdp-scanner/DpdpScannerPage.tsx`) | 4 tests (`DpdpScannerPage.test.tsx`) driving the full paywall→checkout→verify→scanner→PDF flow, plus 6 tests (`razorpayCheckout.test.ts`) for the Checkout-widget script-loading mechanics | ✅ Implemented & verified against real D1-backed data in a real browser |
| Playwright E2E — DPDP Compliance Scanner: the premium card rendering near the top of the Dashboard, a real organization's paywall plus a real, honest 503 attempting to unlock it (no real Razorpay credentials exist in this environment), and a full real scan-and-save flow for an organization with a real, seeded paid `billing_transactions` row, confirming the result appears in CPP-1's own pre-existing Assessments history (Razorpay/DPDP Scanner) | `@titan/web` (`e2e/dpdp-scanner.spec.ts`) | 1 spec, 3 tests, clean across repeated full-suite runs (all 12 specs, 42 tests total) | ✅ Implemented & verified; not yet wired into CI, same reason as the existing E2E specs |

## Explicitly not yet features (don't add rows for these until they're real)

AI insights, booking, real (deployed) Cloudflare infrastructure, email delivery (Auth.js's Email provider logs instead of sending), enterprise SSO, organization-scoped query filtering for `GET /api/leads`/`GET /api/leads/search`/`GET /api/organizations`/`GET /api/organizations/search`/`GET /api/assessments`/`GET /api/assessments/search`/`GET /api/users/search`/`GET /api/audit`/`GET /api/audit/search`/`GET /api/audit/export`/`GET /api/operations/summary` (all still Platform-Administrator-only — `SECURITY_GUIDE.md`'s "Known, accepted gaps"), a self-service way to grant the *very first* Platform Administrator (still provisioned via direct SQL — every grant *after* the first is now a real `POST /api/users/:id/profiles` call, EAP-5, `OPERATIONAL_RUNBOOK.md`), server-side saved views (saved filters are `localStorage`-backed, per-browser only), virtualized table rendering (not justified by real data volume), an assessment lifecycle/status workflow (an assessment is one immutable row per completed run — no draft/in-progress state exists to manage, `DECISION_LOG.md`'s EAP-3 entry), an evidence/attachment panel (no file-upload concept exists in this data model — the closest real analogue, free-text answers, is already in Assessment Results' question responses), organization search by risk level or assessment status (no cross-repository join exists anywhere in this codebase's architecture, and assessments have no status field to filter by — `DECISION_LOG.md`'s EAP-4 entry), organization-scoped delegation of Organization Administration (create/update/archive/restore stay Platform-Administrator-only, the same line drawn around every other cross-organization route — `ARCHITECTURE.md`'s EAP-4 section), a self-service way to create a user's own identity (Auth.js's real sign-in is the only path — no admin-facing "create user" form exists or should, `DECISION_LOG.md`'s EAP-5 entry), an Organization Details "Members" panel (the reciprocal view of User Details' own Role Assignment — real, cross-repository-join-limited, deferred, `ARCHITECTURE.md`'s EAP-5 section), a real assignee picker in Lead Lifecycle (the user directory it needed now exists, EAP-5, but the picker itself isn't wired into the already-shipped `LeadLifecyclePanel.tsx` yet), investigation notes/annotations on Audit Center events (no backing column exists on `audit_events`, a real schema addition, not built speculatively ahead of one — EAP-6, `ARCHITECTURE.md`'s EAP-6 section), a truncation signal on an Audit Export beyond 10,000 rows (the cap itself is the real control — EAP-6, `SECURITY_GUIDE.md`'s known gaps), background job/queue/scheduled-task infrastructure (verified not to exist anywhere in this codebase — no `[[queues]]`/`durable_objects`/`[triggers]` in `wrangler.toml` — EAP-7, `ARCHITECTURE.md`'s EAP-7 section), a dynamically-read build hash/timestamp (no CI step injects one — `SystemOverview.version`/`.environment` are real but manually-maintained constants instead — EAP-7), a cross-isolate/durable metrics backend (`Metrics` stays in-memory and per-isolate — needs Workers Analytics Engine, needs a deployed Cloudflare account that doesn't exist — EAP-7, `SECURITY_GUIDE.md`'s known gaps), organization-scoped delegation of `GET /api/reports/summary`/`GET /api/reports/trends`/`GET /api/reports/export` (all three stay Platform-Administrator-only, the same line every prior cross-organization endpoint already drew — EAP-8, `SECURITY_GUIDE.md`'s known gaps), a materialized/persisted report record via the pre-existing `reports` table (`migrations/0006`) — a different roadmap item (server-side PDF generation/storage/email delivery), still genuinely blocked on an undecided email/storage pipeline and untouched by EAP-8's own, different reporting layer (`DECISION_LOG.md`'s EAP-8 entry), an organization switcher for a caller with more than one real organization membership (`resolvePortalOrganizationId` resolves to the first found — CPP-1, `DECISION_LOG.md`'s CPP-1 entry), a support-request ticketing/resolution workflow (no admin-side endpoint reads or updates `support_requests` yet — CPP-1), and password change/notification preferences/multi-session management on the Customer Portal's Account page (no credentials provider, settings/notification service, or `SessionRepository` beyond Auth.js's own single-token model exists to back any of them — CPP-1). Entitlement enforcement as a gate on any existing route (computed and displayed, real, correct — but not yet wired into the Assessment/Reporting Engine or the pre-existing Customer Portal routes, both named do-not-redesign systems — COM-1), a multi-subscription-per-organization concept (Subscription is 1:1 with Organization — COM-1), a support-request-style resolution workflow for Commercial Administration beyond the admin override PATCH already built (COM-1), Accounting, Tax Engines, Marketplace, Affiliate Programs, CRM, and ERP (all explicitly out of COM-1's own brief). Self-service organization signup (every org membership has always required a Platform Administrator grant — a real, named prerequisite still missing for a brand-new anonymous visitor to reach the DPDP Scanner's paywall end to end, Razorpay/DPDP Scanner), idempotency-key deduplication of concurrent Razorpay order-creation requests (bounded today by a real rate limit, not a security gap, Razorpay/DPDP Scanner), a second payment provider (Stripe was explicitly excluded by this phase's own directive — Razorpay only), and a real successful Razorpay charge (no real `RAZORPAY_KEY_ID`/`RAZORPAY_KEY_SECRET` exists in any environment this project has run in — the integration is real and will work the day real credentials exist). **Admin portal**: the application shell, Dashboard (EAP-1), Lead Management (EAP-2), Assessment Management (EAP-3), Organization Management (EAP-4), Enterprise Identity & User Management (EAP-5), Enterprise Audit Center (EAP-6), Enterprise Operations Center (EAP-7) — every module the original EAP-1 brief named — Enterprise Reporting & Analytics (EAP-8), an eighth module beyond that original list, added because the roadmap itself recommended it once all seven were real, and now Commercial (COM-1), are all real. **Customer portal**: a separate, real `/portal` application (CPP-1) — Dashboard, Assessments, Reports, Support, Account, Subscription (COM-1), and now a real Razorpay-gated DPDP Compliance Scanner (Razorpay/DPDP Scanner) — reusing the admin portal's own services, repositories, and security model rather than duplicating any of them, is also real. Any compliance certification (SOC 2, ISO 27001, GDPR, DPDP) or any real, external/independent penetration test (SEC-1's own `THREAT_MODEL.md`/`COMPLIANCE_GUIDE.md` are real preparation material, explicitly not a claim either has occurred — `COMPLIANCE_GUIDE.md`'s own opening line). Nothing above should be read as implying progress on anything not explicitly listed.
