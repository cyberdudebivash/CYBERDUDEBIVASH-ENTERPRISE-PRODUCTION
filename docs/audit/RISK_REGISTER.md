# Risk Register

Living document — active risks and their mitigations, distinct from `PROGRAM_BACKLOG.md` (concrete action items). A risk stays here even once "understood"; it leaves only when actually mitigated or accepted.

| # | Risk | Severity | Likelihood | Current mitigation | Status |
|---|---|---|---|---|---|
| 1 | Cloudflare never gets reconfigured; platform stays permanently split between two deployment targets with different content | Critical if permanent | Depends entirely on the project owner acting on `PROGRAM_BACKLOG.md` item 1 | None available from any session — fully specified, documented, actionable instructions are the only lever this program has | Open, external |
| 2 | Promoting to Phase 2 (bridge/root-legacy removal) before Cloudflare cutover is confirmed | Critical if it happened | Low — explicitly gated, documented in three separate places (`DECISION_LOG.md`, `PROGRAM_BACKLOG.md` items 11–12, `LIVE_OPERATIONAL_DASHBOARD.md`) | Every report since Stage 2 has refused to recommend this until Gate/Action 1 is verified; the bridge remains untouched | Open, well-guarded |
| 3 | Raw application source (`src/**`, `server.ts`, config files) directly downloadable from production | Medium | Certain — confirmed, ongoing, as long as risk 1 is open | Attempted `_redirects` mitigation does not work (see `DECISION_LOG.md`). No secrets found in exposed content, so impact is IP/implementation disclosure, not credential leak | Open, unmitigated |
| 4 | Six artifacts (`manifest.json`, `404` handling, `sitemap.xml`, `robots.txt`, `sw.js`, `security.txt`) silently wrong on production | Low–Medium | Certain, ongoing | None currently possible without risk 1 resolving; each was deliberately not patched individually at root (see `DECISION_LOG.md` — patching root is throwaway work superseded by the real fix) | Open, by design not patched |
| 5 | HSTS `max-age` below the value `_headers` authors | Low | Certain, ongoing | None — likely a separate Cloudflare zone-level setting outside repository control | Open, low priority |
| 6 | GA4 conversion data may not be real (unverified property ID) | Medium | Unknown — genuinely unverifiable from this repository | None possible without `analytics.google.com` access | Open, unverifiable here |
| 7 | No application monitoring, logging, or error reporting | Medium | Certain — the gap exists today | None — nothing implemented claims to provide this | Open, operational backlog |
| 8 | Bundle JS filename varies across otherwise-identical builds (non-deterministic hash) | Low | Confirmed to occur, root cause not conclusively identified | Verification methodology already adjusted to check content/behavior, not filenames, for build artifacts (see `DECISION_LOG.md`) | Open but fully mitigated by process — not a functional risk |
| 10 | 41% of mobile tap targets under WCAG minimum size | Medium | Certain — measured | None yet | Open, backlog |
| 11 | Cloudflare Pages' zone-level Content-Signal injection wraps whatever `robots.txt` production serves | None | N/A | Confirmed independently decoupled from anything this repository or deployment controls — a platform-level setting | Informational only, not a real risk |
| 12 | Three different "official" contact emails live simultaneously: `iambivash.BN@gmail.com` (`CORPORATE_REGISTRATION.email` in `ecosystemData.ts`, also the "legal" `contactPoint` in `organization.config.ts`'s schema.org data), `bivash@cyberdudebivash.com` (the actual rendered mailto: link in the footer and the address used throughout `LegalPages.tsx`), and `contact@cyberdudebivash.in` (the retired SEO script's value, and also what this program's own operating brief names as "Corporate Email") | Low–Medium — a personal Gmail address is currently modeled as the legal/DPO contact point in structured data | Certain, ongoing | None — a prior session already found this exact drift and deliberately declined to silently pick one (see the header comment in `organization.config.ts`); this is a business decision about which address is canonical for which purpose, not a technical defect | Open, needs owner decision |
| 13 | Unverified commercial stats presented as fact: "2,500+ Engagements Completed" / "97% Critical Findings Rate" (Penetration Testing page), "500K+ Threat IOCs Tracked" / "99.9% Platform SLA Uptime" (About page), "50+ Countries Protected", etc. | Medium — reputational if challenged by a customer, journalist, or regulator during due diligence | Unknown — genuinely unverifiable from this repository | None — no source data or methodology for these numbers exists in-repo | Open, unverifiable here |
| 15 | Correction to this session's own earlier entry: `App.tsx`'s `?redirect=` handler (read via `useEffect`, line ~304) is not orphaned dead code — it's a deliberate `404.html`→SPA fallback covering every static page type (about/privacy/terms/copyright/soc/dpdp/owasp/vciso/pentest/tools/blog), not just legal pages, so the SPA can still render *something* reasonable if the static file isn't found. Given risk 1 (Cloudflare's root-mirror can go stale), this is a legitimate safety net, not something to redirect away — doing so would remove the fallback for exactly the failure mode it exists to catch. The real, narrower risk: `LegalPages.tsx`'s in-app About/Privacy/Terms/Copyright content and the real `about.html`/`privacy.html`/`terms.html`/`copyright.html` static pages are two independently-maintained copies with no shared source, so they can drift out of sync if only one is updated | Low | Certain, latent | None — noting it so a future content update remembers to touch both copies | Open, informational — not a code defect, just a "remember both copies exist" note |
| 16 | Titan (separate sub-project, `titan/`, its own `docs/titan/DECISION_LOG.md`) — the marketing site now links to Titan (footer, "Titan Compliance Platform"), but a real customer who clicks it still can't complete onboarding today: (a) production magic-link email was dev-mode-only (logged, never sent) until this session — now code-complete via Resend (`titan/packages/platform/src/auth/resendEmail.ts`) but not yet live in any deployment, since no environment has a real Resend account/verified domain or the `RESEND_API_KEY`/`EMAIL_FROM` Worker secrets `titan-provision.yml` needs to be run with; (b) independently, even a successfully signed-in user has no self-service path to create an organization — every organization and role grant requires an existing Platform Administrator to act, by deliberate standing design, not oversight (`docs/titan/DECISION_LOG.md`'s Razorpay/DPDP-monetization entry names this explicitly) | High for the stated goal ("customers can use Titan in production") — sign-in is necessary but not sufficient | Certain, ongoing, until both are addressed | (a) is fully coded and tested (unit tests + a real local `wrangler dev` session that hit Resend's live API and got a genuine, correctly-handled `401`) — see `docs/titan/DECISION_LOG.md`'s 2026-07-23 Resend entry — but needs a real Resend account/domain plus a provisioning run to go live. (b) has no mitigation; it needs a product decision (self-service signup vs. an invite flow), which is out of scope for an engineering session per Titan's own "DO NOT redesign authentication/onboarding" constraints | Open — (a) code-complete, blocked on real-world account setup + a deploy action; (b) needs an explicit owner decision |

## Risks retired

| # | Risk | How it closed |
|---|---|---|
| — | Favicon silently served as HTML on production (SPA-fallback, no real icon) | Fixed Stage 2.6, confirmed live every stage since — genuinely closed, not just documented |
| — | `_headers` would have silently stopped working the instant Cloudflare cutover happened | Fixed Stage 5 (`assemble-site.mjs` now copies it into `dist/`, `verify-dist` gates on its presence) — this was a live landmine specifically defused before it could detonate |
| — | `server.ts`'s `/api/security/analyze` system prompt told every AI provider (Gemini, Groq, DeepSeek, OpenRouter) that "CyberDudeBivash Private Limited is ISO 27001 certified, SOC 2 Type II compliant, and DPDP aligned" as flat fact — directly contradicting `COMPLIANCE.md` and the `COMPLIANCE_DISCLOSURE` constant built specifically to prevent this drift. Any user asking the AI Security Copilot "are you certified?" got an authoritative false "yes" | Fixed this session — `server.ts` now imports and composes `COMPLIANCE_DISCLOSURE` into the prompt instead of asserting certification, and explicitly instructs the model to answer accurately if asked about certification/audit status |
| — | "CERT-In Empanelled Organization" stat card (`ServicePages.tsx` Penetration Testing page) and "CERT-In Notified" trust badge (`LegalPages.tsx` About page) asserted a specific, formal, publicly-checkable government status with no supporting evidence anywhere in the repo, contradicting the "no formal certifications yet" posture `COMPLIANCE.md` otherwise states | Fixed this session — softened to "CERT-In / Guidelines Aligned" (matching the penetration-testing methodology list already on the same page) and routed the About-page badge through the existing `aligned()` helper (`aligned("certIn")`), matching how ISO/SOC2/GDPR/PCI-DSS are already handled correctly |
| — | Interactive Exploit Mitigation Lab (`AiSocDashboard.tsx`) interpolated raw, unvalidated user input directly into generated `iptables`/`dig`/`nginx`/YARA text offered as copy/download (`.sh`), with zero format validation — a crafted "IP"/"domain"/"hash" string could inject shell syntax into a script a security engineer trusts and runs elsewhere (an OWASP LLM Top 10 "Insecure Output Handling"-class issue, notable given the same page displays an OWASP LLM Top 10 badge) | Fixed this session — added strict IPv4 / bare-domain / 64-char-hex SHA-256 format validation before any interpolation; malformed input is rejected with an inline error instead of silently generating a script |
| — | `terms.html` and `copyright.html` did not exist anywhere in the repository — no static page, no sitemap entry, no dedicated URL. The SPA footer's "Terms of Service"/"Copyright & IP" links were `<button onClick>` state-changers (not real `<a href>` links, not crawlable, no stable URL, lost on refresh). Across the 16 pre-existing static pages, the shared footer's "Terms" link pointed at raw `TERMS.md` (unstyled markdown) instead of a real page, in 3 different footer template variants; 2 of those variants also linked "Privacy" to raw `PRIVACY.md`. Reported by an external party (business/ad-network due-diligence) as a P0 brand-credibility blocker | Fixed this session — created production-grade `terms.html` and `copyright.html` matching `about.html`/`privacy.html`'s SEO/design standard (canonical URL, OG/Twitter tags, JSON-LD, matching nav/footer), content adapted from the already-written (but unreachable) `LegalPages.tsx` copy. Fixed the broken/missing links across all 12 affected static pages (3 different footer templates) and pointed the SPA footer (`Footer.tsx`) at the real static URLs instead of in-app state. Added both new pages to `sitemap.xml` |
| — | Found while shipping the fix above: the `.gm-footer*` class system used by every static page sharing that footer template (about, apps, bug-bounty, contact, platforms, pricing, privacy, research, services, soc-services, status, terms, copyright — 13 pages) had **zero CSS definitions anywhere in the codebase**, confirmed by grepping all of `assets/css/*.css` (only `.navbar.gm-nav` and `.btn-portal.gm-portal-btn` exist; nothing else with a `.gm-` prefix). Every one of these footers rendered as an unstyled bullet list — no background, no grid, default blue underlined links, the animated matrix-rain canvas visibly bleeding through behind the text — screenshot-confirmed before and after. A site-wide, immediately visible defect on every static marketing page; likely the real substance behind (or a major contributor to) the customer escalation, independent of the missing-pages issue above. Also affected `threat-intel.html`'s outer footer wrapper | Fixed this session — added `assets/css/gm-footer.css`, a new stylesheet defining the full footer system (grid layout, brand column, cert badges, column headers, link list, social card grid, bottom bar) matching the dark navy/cyan/emerald palette already established elsewhere on the site and the React SPA's own footer. Linked into all 14 affected pages. Also used to give `compliance.html`, `dark-web-monitor.html`, `vciso.html` a real, correctly-styled footer for the first time (retires risk 14). Verified via computed-style checks (background color, grid display) and a full-page screenshot across all 6 legal/compliance pages post-fix |
